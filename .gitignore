target/
target-ci-isolated
vscode-addon/node_modules/
config.toml
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
acp_autotune_state*.json
*.log

# Runtime data files (auto-generated)
*.ndjson
config/acp_cache.sqlite3
config/acp_vector.sqlite3
config/audit_chain.ndjson
tmp_basic_test_config.toml
tmp_provider_fail_config.toml
tmp_provider_fail_config2.toml
tmp_review_collision.toml
.goon/
dist/
sdk/typescript/node_modules/
sdk/typescript/dist/
sdk/typescript/.vite/
sdk/typescript/gen/
sdk/typescript/target/
sdk/rust/target/
sdk/python/dist/
sdk/python/build/

# Python bytecode cache (generated by test runs; should not be tracked)
__pycache__/
*.pyc

# VS Code extension build output
vscode-addon/out/

# Documentation build output (mdBook default build dir)
# NOTE: previously this was `DOC/book/` — the actual output dir is
# `cookbook/book/` (see cookbook/book.toml build-dir).
cookbook/book/

# Release and package artifacts
*.vsix
*.tar.gz

# GUI build and dependencies
gui/node_modules/
gui/dist/
gui/.vite/
gui/src-tauri/target/
gui/src-tauri/gen/

# Temporary and output files
# Stale build artifacts in project root
scheduler_queue.db
libchat_tests.rlib
libbarcode.rlib
librust_out.rlib
go-on_check*.txt

*.tmp
*.temp
*.bak
*.backup
cargo_errors.txt
cargo_check_errors.txt
clippy_out.txt
cc_s11.txt
RELEASE_GATE_OUTPUT.txt
test_out.txt
check*.log
clippy.log

# Kubernetes local secrets (never commit real secrets)
.secrets.local.env

# Archive directories (should not be committed)
archive/
tests/artifacts/
tests/requests/

# Generated artifacts
artifacts/
# NOTE: `snapshots/` is intentionally NOT ignored: screenshots under
# snapshots/ are tracked and referenced by the READMEs. Only untracked
# generated artifacts are ignored.

# OS generated files
nul
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE and editor files
.trae/
.vscode/
.zed/
.idea/
*.swp
*.swo
*~
