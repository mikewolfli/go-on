[package]
name = "go-on-latex"
version = "1.5.0"
edition = "2021"
description = "LaTeX math rendering for go-on - converts LaTeX expressions to SVG"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
