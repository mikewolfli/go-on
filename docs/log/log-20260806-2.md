# 第33轮 — 第32轮遗留 13 项全额收口 (2026-08-06)

承接第32轮遗留问题清单,按原则逐项「接线或删除」,全部收口。

## 一、13 项处理明细

| # | 遗留项 | 处置 | 依据 |
|---|--------|------|------|
| 1 | **AlertManager 双实例**(内存告警写入全局单例 vs server 消费另一实例) | **统一为单一权威**:全局单例改为 `OnceLock<Arc<StdMutex>>` + `shared_alert_manager()`;`ServerBuilder::build` 的 server 实例改用该单例(webhook env 配置仍在同一实例上执行)。内存监视器写入与 session/evolution/security-advisor 消费现在命中同一实例 | 消除"告警写入无人读"失效链路 |
| 2 | **双语义缓存**(semantic_cache Jaccard vs token_cache L2 cosine) | **复核为非重复,记录不合并**:`SemanticResponseCache` 是活跃的请求级响应缓存(chat `act_phase` 有真实读写),`L2SemanticCache` 是 agent 输出级 token 缓存,层级不同、均接线。强行统一会改变命中行为且无收益;两套嵌入器(bigram-Jaccard vs TF+cosine)记入低优先维护项 | 实事求是:层级不同,非冗余 |
| 3 | **cache/vector 路径默认值双源**(defaults.rs 带 `sqlite3/` 前缀 vs config.toml 无前缀) | **统一 `sqlite3/` 前缀为权威**(消费端 `ResponseCache`/`VectorStore` 均 `create_dir_all` 父目录,带前缀安全):修正 `config/config.toml`、`config_gen.rs` 模板、`lifecycle_pack.rs` 存储报告、`main/tests.rs` 夹具、GUI `actions.rs` 生成模板共 5 处漂移方 | 消除同默认值两处定义 |
| 4 | **ToolBus 执行侧 / CapabilityBus::execute_tool(act.rs)** 生产未接线 | **删除写侧,收敛为纯查询总线**:删 `act.rs` 整文件 + `pub mod act`;删 `ToolBus::execute_tool`/`dispatch_tool`/`lookup_skill`/`record_tool_call`/`tool_stats`/`ToolStats`/`ToolBusInner`;`ToolBusProfile` 裁剪为 `total_tools`/`total_skills`;`CapabilityBusProfile.tool_bus_calls` 删除;保留读侧(`capability_matrix`/`agent_tool_match`/`skill_registry_ref`/`profile`)。生产唯一工具执行路径为 `execute_tools_concurrent` | §11:未接线即删除;消除双执行入口 |
| 5 | **OptimizerRegistry::optimize_all** 无生产调用 | **删除 registry 聚合层**:删 `OptimizerRegistry`/`ConcurrencyOptimizer`/`HistoryBasedOptimizer` 及 server/capability_bus/wire_server 三处字段接线;保留热路径 `CostOptimizer`(被 `OptimizationBus::recommend` 直接委托)与 `WorkflowOptimizerPlugin` trait(简化为仅 `optimize()`);`RegistryContext` 只剩 `schema_registry` | §11:构造即接线但永不运行 |
| 6 | **hyper_resilience P3-2/7/8**(persist/fault_consensus/plan_store)内部接线但生产不激活 | **整体删除**:删 3 个字段 + 3 个 builder + 4 处 persist 分支 + health_check_cycle 共识分支 + execute_healing 计划存储块 + `persist_to_db`/`load_from_db` + `FaultVote`/`FaultConsensus` + `RecoveryStep`/`RecoveryPlan`/`RecoveryPlanStore`;保留 `TestMetrics`(被 new 使用)与 fault_tolerance 模块自己的 `RecoveryPlan` | 无配置来源激活,删除彻底消除惰性扩展点 |
| 7 | **WorldModel 只写不读** | **补读接口并接线**:新增 `WorldModel::profile()` 返回 `WorldModelProfile`(实体/事件计数、类型分布、最近事件);`capability_bus_profile` 新增 `world_model_entities`/`world_model_events`/`world_model_last_update_ms` 3 字段,status 端点可见 | 使已积累的世界状态可观测 |
| 8 | **EmbeddingTaskClassifier** 生产恒 keyword fallback | **删除假 embedding 分支**:确认全仓无 `task-classification` 文档写入,embedding 分支即使注入 store 也恒 Miss;`classify_task` 收敛为纯 keyword(逻辑与 `Planner::analyze_task` 逐字一致,含 `len>300` 分支),测试改真实断言 | §13:无法生效的假功能分支删除 |
| 9 | **i18n/watcher** 生产无调用 | **接线启动**:`perform_bootstrap` 在 `init_i18n` 后调用 `start_watcher(lang_dir, 5s)`,磁盘语言文件(en-US/zh-CN/zh-TW.json)编辑 5 秒内热重载,无需重启 | 激活现成完整功能 |
| 10 | **HubClient** 未接线 | **删除 + reserve 收敛**:删 `hub/client.rs` 整文件 + re-export;删配套死 API(`discovery::read`/`is_alive`、`server::api_token`/`hub_id`/`with_discovery_path`/`new`);**移除 hub 两文件 `#![allow(dead_code)]`**——simple-server profile 下零警告。HubServer(CLI `go-on hub` 已接线)与 discovery 保留 | §13:消除 allow 保留;reserve 收敛为已接线面 |
| 11 | **background.rs mTLS 证书监控 TODO** | **复核为已实现,修正过时文档**:`security::wire_cert_monitor`(初始检查 + 每日重检 + x509 过期警告)完整实现且已在 `wire_server` 接线;background.rs 的"未实现"注释是文档欺骗,按实况重写 | §18:文档与代码一致 |
| 12 | **evict_oldest_conversation** 仅测试使用 | **删除未接线的 LRU 会话逻辑**:`evict_oldest_conversation`/`touch_conversation_order`/`checkpoint_message_chars` 三个 `#[cfg(test)]` 辅助 + 对应 3 个测试删除(生产会话 state 单例,checkpoint 容量由 `enforce_checkpoint_capacity` 管理,LRU 驱逐无对应生产功能) | §11:测试覆盖的是未接线逻辑 |
| 13 | **full 下 extended 工具被 MCP 隐藏** | **复核为完整设计,记录无需修复**:`tool_search`(Direct 暴露)经 `ToolRegistry::deferred_tool_names()` 按名称/描述搜索发现全部 deferred 工具;`skill-finder` 同理——Direct/Deferred/Hidden 三级暴露机制闭环,deferred 工具可达可发现(默认列表隐藏仅为收敛默认面) | 实事求是:非死代码,是分级暴露设计 |

## 二、验证证据(原则 §16)

```
=== 编译 ===
cargo check 4 profile(local/simple-server/multi-users-server/full)  → 零警告零错误
cargo check --workspace(gui/sdk/crates)                             → 零警告
cargo clippy --all-targets -- -D warnings                          → 零警告
=== 测试 ===
cargo test --all-targets                                            → 全绿
   lib: 1661 passed / 0 failed(较上轮 -18:随删除逻辑对应的测试移除)
   集成/结构/文档测试:全部 passed / 0 failed
=== 三端生成物 ===
scripts/gen-provider-catalog.py --check                            → 双输出 OK(37 providers)
=== 代码量 ===
71 files changed,+521 / -5698(净 -5177 行);删除 3 个源文件(hub/client.rs、capability_bus/act.rs、--)
```

行为一致性说明:
- #1 统一单例后,首次 webhook env 配置经 server 构建路径执行,行为等价;内存告警现在能被消费端读取(行为变化为**修复**而非回归)。
- #8 分类逻辑与旧 keyword 分支逐字一致(经 git diff 对照旧 `classify_with_keywords` 核实),仅去除不可达 embedding 分支。
- #4/#5/#6/#10/#12 均为零调用点删除(grep 全仓核实),删除前已确认无生产/测试引用(除被一并移除的对应测试)。
- #3 路径统一后 SQLite 文件落点从"工作目录根"迁移至 `sqlite3/` 子目录(与 defaults 默认行为一致);消费端自动建目录,无失败风险。

## 三、完成率回写(原则 §9)

- 收口项:13 项(删除 5、接线 3、复核保留 3、文档修正 2)。
- 复核保留项(非死代码,记录依据):#2 双语义缓存(层级不同)、#13 extended 工具分级暴露(设计闭环)、#11 mTLS 监控(已实现)。
- 修复项数:10 项(1/3/4/5/6/7/8/9/10/12 实质性改动 + 11 文档)。
- 遗留问题数:0 项(原 13 项全部处置完毕)。
- 收敛判据:4 profile + workspace + clippy 全目标零警告、全测试套件 0 failed、生成物一致,本轮未引入新编译/测试失败。
