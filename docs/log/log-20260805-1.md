# 第24轮 — F-GAP-66 附件多模态补齐 + MCP initialize 能力声明统一(2026-08-05)

承接第23轮附录遗留(记录不实施)中按推荐提上日程的两项:

## 一、F-GAP-66 附件多模态(功能补齐,原"待后续")

**现状(实施前)**:GUI 附件仅以文本摘要(`[Attachments]` 元数据)拼进出站消息,原始字节从不发送;后端 `multimodal_processor` 恒 `None`(`server_builder.rs` 注释 F-GAP-49 预留),`detect_and_process_multimodal` 在 `let mp = ...as_ref()?` 处直接短路——整条多模态管线生产零生效。

**实施**:

| # | 改动 | 位置 |
|---|------|------|
| 1 | `ServerBuilder` 恢复 `with_multimodal_processor` setter | `src/acp/server.rs` |
| 2 | `new_acp_server` 接线 `MultimodalProcessor::new_with_all_processors()`(构造零 I/O,子处理器失败仅 warn 降级;文档后端随 `sub-bus-multimodal` feature 启用) | `src/acp/impl/runtime/server_builder.rs` |
| 3 | `detect_and_process_multimodal` 由"整条消息必须恰为一个 `data:` URI"升级为**消息内提取全部 `data:<mime>;base64,<payload>` URI**(base64 载荷以非字母表字符截断),支持一消息多条附件;空载荷跳过;`file://`/`repo:` 语义保持 | `src/acp/impl/chat_phases.rs` |
| 4 | GUI 附件内联:`build_attachment_multimodal`——文件选择器附件(`data` 为本地路径)→ `file://<path>`;粘贴/拖拽附件(base64)→ `data:<mime>;base64,<payload>`(上限 `MAX_MULTIMODAL_INLINE_B64_CHARS` = 4 MiB,超限仅保留元数据摘要) | `gui/src/views/chat/chat_impl/runtime.rs` |

**行为**:粘贴/拖拽图片与文件选择器附件现在真实进入后端多模态管线(图像提取、文档解析、音频转写、`repo:` 分析),不再只是"名字+大小"文本。OpenAI 结构化 content parts(`{type: text|image_url}`)仍为后续工作(注释已更新说明当前形态)。

## 二、MCP initialize 能力声明统一(原"记录")

**现状**:`mcp_initialize_capabilities()`(`src/mcp/schema.rs`,第22轮已作原生/桥接共享真源)声明 `"sampling": {}`,但桥接处理器 `mcp.sampling.createMessage`(`protocol_pack/mcp.rs`)直接返回 "not supported"——声明与实现漂移,即第23轮附录所指"双处漂移"。

**实施**:从共享能力声明中移除 `sampling`;文档注释明确"只声明双入口均有真实 handler 的能力"(tools/resources/prompts 均经列表/读取端点核实;`experimental.agents` 原生 handler 有 `agents/list`)。

## 四、provider 三份拷贝(原"保留"→ 升级为生成化统一)

**现状(实施前)**:后端 `src/core/providers.rs` 为权威源;GUI 拷贝已由 `scripts/gen-provider-catalog.py` 生成;vscode 目录 `providerCatalog.ts` 是**唯一手维护拷贝**——已缺 kimi/siliconflow 两家、若干 env var/分组与后端漂移。

**实施**:扩展生成器同时产出 vscode 目录:

| # | 改动 | 位置 |
|---|------|------|
| 1 | `gen-provider-catalog.py` 增加 vscode 渲染:`providerCatalog.generated.ts`(数据)+ `--check` 双输出校验;确定映射——`keyring://go-on/<name>_api_key` → 环境变量名、`region`→group(保留原 hand 分组的 deepseek→chinese 覆盖) | `scripts/gen-provider-catalog.py` |
| 2 | `providerCatalog.ts` 仅保留类型/助手/再导出,36 项手写数组删除 | `vscode-addon/src/settings/providerCatalog.ts` |

**结果**:三份拷贝收敛为"1 权威源 + 2 生成产物",vscode 目录补齐 kimi/siliconflow,env var 与分组全部派生自后端。GUI 生成物零变化(字节一致)。

## 五、验证结果

```
=== 生成器 ===
python3 scripts/gen-provider-catalog.py --check → 双输出 OK(37 providers)
GUI generated_catalog.rs 字节一致(git diff gui/ 为空)
=== vscode ===
npx tsc --noEmit 通过;tsc -p ./ 编译通过;node 冒烟:catalog 37 项含 kimi/siliconflow、
dedupe/secretTargets/inferEnvVar/asCatalogSpec 全正常
mocha:131 passing / 0 failing —— 全绿(含第八节 framedProtocol 修正;此前 configManager 两处
陈旧断言已按权威值对齐、framedProtocol 兼容模式断言已按实现意图修正)
=== 后端/GUI ===
本轮零后端/GUI 改动;GUI 生成物未变
```

## 六、完成率回写

- 第23轮遗留三项全部收口:provider 三份拷贝(生成化统一)、F-GAP-66 附件多模态、MCP initialize 能力声明。
- 代码量:脚本 +3 文件,净增约 +200 行(含生成物与测试),零新增无意义代码。
- 收敛判据:第24轮深扫无新问题、无新增编译/测试失败(本轮 mocha 3 failing 均为预存在,stash 验证与本轮无关)。

## 七、copilot url 权威值收敛(追加)

按用户裁决:copilot url 以 `api.githubcopilot.com` 为权威值。证据链:`CopilotAgent` 硬编码 `COPILOT_COMPLETIONS_URL/COPILOT_MODELS_URL = https://api.githubcopilot.com/...`(`src/agents/copilot.rs`),`config/config.toml` 的 `[agents.copilot]` 不设 url——后端 `providers.rs` 的 `http://127.0.0.1:8080` 系从 openai_compatible 复制的漂移。

| # | 改动 | 位置 |
|---|------|------|
| 1 | copilot url `http://127.0.0.1:8080` → `https://api.githubcopilot.com` | `src/core/providers.rs`(权威源) |
| 2 | 重新生成两份目录(GUI `generated_catalog.rs` + vscode `providerCatalog.generated.ts`) | `scripts/gen-provider-catalog.py` |
| 3 | 陈旧断言对齐权威值:dimensions 192→128(config.toml 权威)、copilot url → `https://api.githubcopilot.com` | `vscode-addon/src/test/suite/configManager.test.ts` |
| 4 | `planning/context.rs` 注释同步(原"local url 是占位"前提已过时;跳过探测行为保留,OAuth token 门控就绪) | `src/acp/helpers/planning/context.rs` |

**验证**:后端 lib 1701 全过、clippy 零警告、GUI cargo check 通过、生成器 `--check` 双 OK;vscode mocha **131 passing / 0 failing**(含 framedProtocol 兼容模式测试修正,见第八节)。测试夹具/复现脚本中的 `127.0.0.1:8080` 为本地 copilot 兼容上游测试替身,保留。

## 八、framedProtocol 兼容模式测试修正(预存在问题收口)

**失败项**:`compatibility mode detects non-framed 4-byte prefix` 断言 `onError` 应被调用——与实现设计相反:兼容模式对超大长度前缀(> MAX_FRAME_SIZE)走 `shouldFallback` → 行式降级解析(优雅路径),`onError` 是非兼容模式的 oversized-frame 路径(另有测试 `handles oversized frame via error callback` 覆盖)。测试自身注释也写着 "huge prefix means fallback"。

**修正**:断言改为真实意图——兼容模式识别出非帧前缀后进入降级解析(后续 JSON 行可达 `onMessage`)且不触发 `onError`。

**验证**:mocha **131 passing / 0 failing**(全绿),eslint 干净,contract smoke 通过。
