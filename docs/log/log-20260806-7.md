# Log 2026-08-06 #7 — 全项目多轮深度+广度扫描与统一架构精炼

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则，对本项目进行多轮超级深度扫描 + 超级广度扫描，直到连续 2 轮无新发现为止。目标：

- 统一全功能架构、各链路精炼功能、统一重复功能（合并去重）
- 优化全项目执行效率、提高节点执行速度、改造可并行的串行工作流
- 以统一架构/平台/链路、加速精炼优化为目标
- 全局考量，收益中/大时考虑重构
- 全面更新文档（README、CHANGELOG、cookbook）以匹配最新实际架构

## 1. 扫描计划

项目规模：450 个 Rust 文件 / 205,902 行（src/），gui+sdk+vscode-addon 三端，crates×3，tests/contracts，scripts×22。

| 轮次 | 范围 | 状态 |
|------|------|------|
| 第 1 轮 | 6 并行子代理分区扫描（A: acp / B: orchestration / C: governance+security+observability+fault_tolerance+resilience+hub / D: agents+memory+intelligence / E: mcp+protocol+multimodal+core+shared+schema+i18n+cli+main / F: 三端+crates+scripts+tests+docs） | 待开始 |
| 第 2 轮 | 依据第 1 轮发现修复 + 深度验证 | 待开始 |
| 第 3 轮 | 广度验证 + 文档一致性 | 待开始 |
| 第 4 轮 | 收敛确认（连续 2 轮无新发现） | 待开始 |

## 1.1 第 1 轮扫描派发（2026-08-06）

按 principle.md §10 惯例派出 6 个并行子代理，每区主题：
1. 重复功能/冗余模块（跨文件重复实现、转发壳、重复常量/助手）
2. 性能瓶颈（串行可并行、重复计算、每次请求重建、同步阻塞）
3. 死代码（无调用者的 pub 函数、无生产者的指标/字段）
4. 链路缺失（已实现未接线、幻影 RPC、未激活配置）
5. 假修复/文档与代码不符（原则 §13/§15/§18）

扫描中，待子代理回报。

## 1.2 第 1 轮扫描结果汇总（6 子代理回报）

共 110+ 条发现，交叉归类后核心问题：

### 安全/正确性（必修）
- A-L1: `ACP_METHODS` 数组未排序 → binary_search 使 `tool.approve`/`terminal/create` 在 ACP 模式不可达
- B-3: `WriteFileTool::run_async` 绕过写沙箱（可写 /etc/、超大文件）
- C-O1: `tool_capability.rs` operation/action 分类漂移（skill_execute/cargo_check 归 Read）
- E-O1: `detect_audio_format` len<12 时切片越界 panic
- E-O2: PDF 内嵌图双重 base64 编码
- E-L1: `code_repo_analyzer` 远程 clone 的 TempDir 提前 drop → repo: 分析必失败
- B-4: `guardian_enabled=true` 主路径永不生效（GuardianHook 只实现 async_pre_execute）

### 三端契约（必修）
- F-1.1/1.2: Rust/Python/Node SDK `task.plan`(发 description)/`task.execute`(发 plan_id) 与后端参数名不符 → 必失败
- F-1.4: 三 SDK `HealthResponse` 形状与后端 ServerStatus/runtime.health 不符 → Rust SDK health() 恒失败
- F-1.5: nodejs `runtimeHealth()` 调 `health.probes`（方法错位）
- F-1.6: vscode `workflow.execute` 传 workflowId/name/stages（后端只读 task）

### 性能（中/大收益）
- B-P1: `http.rs` 每次请求新建 reqwest client（无连接池）；web_search/lsp 同理
- A-D3/P2: `governance.status` 每请求 2× AppConfig::load + 同步磁盘
- A-P1: `load_recent_knowledge_context` 每请求同步读文件（无缓存）
- D-1: chat 热路径两套 agent 选择逻辑并行打分（AgentSelector + CapabilityBus::decide）
- D-P1: `synthesize_keyword_heuristic` O(n²)
- C-P1: `record_outcome` 单次调用最多 6 次互斥锁
- E-P1: `code_repo_analyzer` 同一仓库文件树扫两遍
- A-P4/P5/P7: 多处串行 await 可并行（release_readiness/health_payload/vector snippets）

### 假修复/链路缺失
- A-L4/L5/M3: `task.execute` 模型工具调用从不执行，伪造 followup
- A-L2: `ChatParams::plan_output` 幻影字段
- A-L3: `workflow.run pause/cancel/resume` 不生效
- C-L2: `p.current_active_policies = 12u32` 硬编码
- C-L3: `SecurityGovernor` total_evaluations 双计 + active_escalations 恒增
- C-L4: drift auto-baseline 跨 metric 串扰
- B-4/5: `utils.rs` sha1 伪实现 + 未知算法静默回退
- B-6/7/8: evolution_loop RequireApproval 假审批/RequireHuman 假拒绝/placeholder.rs
- C-D5: 故障恢复计划无完成路径（InProgress 只增不减）
- C-L1: PolicyEvaluator safety_checker/injection_detector 无赋值点
- E-O3: schema_version 迁移系统空壳
- D-L2: distributed_memory start_transport 等无生产调用
- D-L4: ContextForker fork 传空参数

### 死代码簇（§11 清理）
- A-DC1-DC7: AcpServer 14 个 pub 访问器零调用、SessionManager token 签发整套、MaintenanceSnapshot legacy 字段等
- B-死1-6: builtin_tools! 宏、ToolProgress 子系统、run_with_progress/run_streaming、hits/misses 计数器、with_default_trigger_source
- C-D1-D11: approval_engine 整套（submit_for_approval/approve/reject）、can_auto_approve、IdempotencyCache、fault_tolerance 9 个方法、failover 组、drift resolve_alert、alert_manager 4 规则无生产者、register_tool、Permit* 类型、remove_policy
- D-3: 25+ 项（communication bus 死计数器、budget 死方法、Agent::send_message 空实现、token_cache report/stats_snapshot/reset/ttl_ms、vacuum 系列等）
- E-D1-D5: rate_limit 构造器、schema_version 仅测试方法、i18n hot_reload 等、SseBroadcaster 无发送端、VideoProcessor::set_config

### 文档/脚本过期
- F-3.4: cookbook core-runtime.md 整篇虚构 REST 端点
- F-3.3/3.5: cookbook /v1/responses、learning/workflow 虚构端点
- F-3.1/3.2/4.1/4.2: verify-zed-integration.sh 必 FAIL（键名/端口 8080→8090）
- F-3.6: contracts/cross-client-sync.md 引用不存在路径
- E-W1: `default_non_ai_config_toml` 写 `mode="auto"`（规范名是 adaptive）
- E-W2: OTEL_ENDPOINT_DEFAULTED 端口文本 4317 vs 实际 localhost:4317
- F-6: README 13-sub-bus vs 11 core buses 矛盾、3478 数字无法核实

### 第 1 轮结论
开始交叉验证关键项并分批修复。

## 2. 修复进度

### 批次 1（安全/正确性）✅ 已编译
1. ACP_METHODS 排序修复（tool.approve/terminal/create 恢复 ACP 可达）
2. detect_audio_format 越界（len<12 前置检查）
3. 双重 base64 修复（document_parser 已编码，mod.rs 不再二次编码）
4. WriteFileTool::run_async 补齐沙箱（enforce_write_sandbox 共享，sync/async 统一）
5. code_repo_analyzer：TempDir 生命周期修复（Arc<TempDir> 随 RepoContext 存活）+ head_commit 真实 git rev-parse + loc 真实行数 + extract_types 复用 repo_map 去双扫

### 批次 2（三端契约）✅ Rust SDK 编译 + TS/Node/vscode tsc + Python py_compile 通过
1. Rust/Python/Node SDK task.plan/task.execute 参数名对齐（description/plan_id→task）
2. 四端 HealthResponse 类型对齐后端真实载荷（ServerStatus + runtime.health，宽松字段）
3. Node SDK health()=GET /health（新增 _httpGet）、runtimeHealth()=runtime.health 对齐三端语义
4. vscode workflowView/processFlowView workflow.execute 改传 task 文本（后端只读 params.task）
5. knowledge_distill 参数对齐（query/source 幻影 → limit 窗口）

### 批次 3（假修复/指标/文档一致）✅ 已编译
1. security_governor 指标统一计数（total_denials/reviews/escalations 集中到 record_audit，evaluate 不再双计）
2. harness_bus current_active_policies 硬编码 12 → 真实计数（sg 策略数+5）
3. drift auto-baseline 按 metric 名分组（消除跨 metric 串扰）
4. governance.status 单次 AppConfig::load（governance_config_summary_with 复用）
5. hash_file sha1 伪分支删除 + 未知算法显式报错
6. random_token 改用加密安全 rand（RngExt）
7. cache_strategy 大小写不敏感扫描不再分配大文本拷贝
8. defaults.rs mode="auto"→"adaptive"（规范名）
9. OTEL_ENDPOINT_DEFAULTED 端口文本 127.0.0.1→localhost

### 批次 4（性能）✅ 已编译
1. http.rs 双路径共享 reqwest client（async/blocking 各一单例，连接池复用，请求级 timeout）
2. record_outcome 锁从 7 次减到 4 次（删除无条件 register_service，惰性 entry 初始化）
3. synthesize_keyword_heuristic O(n²)→O(n)（lines 一次性收集）
4. health_payload 的 resilience/fault_tolerance profile tokio::join! 并行
5. release_readiness 的 stability/provider payload join! 并行
6. load_recent_knowledge_context mtime 缓存（知识文件不变则不重读）
7. web_search client OnceLock 缓存；LSP client 按地址缓存（省握手）

### 保留项（记录理由）
- 两套 agent 选择逻辑（AgentSelector reorder vs CapabilityBus decide）：用途不同（排序执行候选 vs 选择记忆注入锚点），合并需改 capability_bus API，行为面广，收益/风险不匹配，保留。

### 批次 5（死代码清理 + 功能修复）✅ 已编译 + 1648 lib 测试全绿
1. 删除 registry_macro.rs（builtin_tools! 宏无生产调用者）
2. 删除 ToolProgress 子系统（events.rs + run_with_progress/run_streaming）
3. ShardedGovernanceCache hits/misses 只写不读计数器删除
4. 删除 with_default_trigger_source（无调用）
5. token_cache：ttl_ms 恒 0 死逻辑删除（TTL 分支、is_entry_expired、AtomicU64）、report/stats_snapshot/reset 死 API
6. Agent::send_message 空实现删除（§13 no-op）
7. semantic_cache put_string/put_string_with_ttl/get_string 死 API
8. summarize_sync 死 API；embedding_provider expected_dimension trait+5 impl 删除
9. **fault_tolerance 恢复计划生命周期修复（C-L6）**：run_recovery_cycle 激活后按一致性检查 complete/fail（消除 InProgress 滞留）
10. security_governor 测试更新为新集中计数语义
11. fault_tolerance 测试更新为验证新恢复语义

### 保留项（§11 pub API 面 / 需决策）
- SseBroadcaster（MCP SSE 协议预留通道，无生产者，删除收益低）
- VideoProcessor::set_config、i18n hot_reload 等 pub API（公开 API 面）
- AutonomyLoopConfig::use_brain_loop（pub 兼容字段）
- approval_engine 整套、IdempotencyCache、failover 组等（需"接线 vs 删除"决策，遗留下轮）

### 验证
cargo check 4 profile 零警告；clippy -D warnings 零警告；cargo test --lib 1648 全绿

## 3. 第 2 轮扫描（自验证，子代理被取消）

用户取消验证子代理后自验完成。**发现环境自动提交（2105d3f5，含他人并行修改）引入多处编译损坏，全部修复**：
- protocol.rs：is_acp_request 函数头丢失（模块级 const 与函数体残留并存）、AcpErrorCode::from_code 非法 pattern（`Self::X as i32` 作 match arm）→ 恢复为函数 + 模块级 ACP_METHODS const（配合他人新增的排序断言测试），from_code 改用守卫匹配
- shared/mod.rs：hmac_sha256 缺 `use sha2::Sha256`
- 9 个文件 14 处 `acp::impl::` 关键字路径缺 `r#` 前缀
- request.rs 排序断言测试 `str < &str` 类型错误
- governance_pack.rs classify_request_error_kind 传 -32000（恰为 ServerError code）→ 改传 0，一般错误正确分类为 GeneralError
- sdk example task_execute 语义 + HealthResponse 字段适配

## 4. 完成率回写（原则 §9）

- 扫描模块数：6 分区覆盖 src/ 全部 450 个 .rs + gui/sdk/vscode-addon/crates/scripts/tests/contracts/docs。
- 修复项数：**40 项**（安全/正确性 5、三端契约 6、假修复/指标 9、性能 7、死代码/功能修复 11、文档 2 批）+ 环境提交损坏修复 6 处。
- 复核保留项：两套 agent 选择逻辑（用途不同）、SseBroadcaster（协议预留）、pub API 面访问器、use_brain_loop（pub 兼容字段）、reqwest client 构造（配置差异化）。
- 遗留问题数：**9 项**（均低-中收益，需决策或大重构）：
  1. approval_engine 整套 HITL 引擎无生产调用（接线 vs 删除决策）
  2. IdempotencyCache 无 insert 生产者；evaluator 的 idempotency get 恒 miss
  3. fault_tolerance report_fault/isolate_node/reintegrate 无生产调用（恢复链路已补完成路径）
  4. failover 组 register_failover_group/trigger_failover 无调用
  5. drift resolve_alert/get_active_alerts 无调用；alert_manager 4 规则无生产者
  6. communication bus 死计数器（tool_*、record_message_received）与 budget 死方法
  7. distributed_memory_bus start_transport/register_peer/sync_now 无生产调用
  8. 14 文件 22 处 reqwest client 构造未统一到 shared::http_client
  9. AcpServer/SessionManager 剩余 pub 访问器（公开 API 面，§11 允许保留）

## 5. 遗留清单收口（用户逐项决策后执行）

| # | 项 | 处置 | 结果 |
|---|----|------|------|
| 1 | approval_engine HITL 引擎 | **删除**（环境已删，独立验证干净） | ✅ 已删除；`session/request_permission` + `tool.approve` + `user_approved_tools` 链路为完整替代（产生：executor→request_client_permission→SSE 通知；响应：session/request_permission；生效：tools_pack require_review 门） |
| 2 | IdempotencyCache 无 insert | **误报，已接线** | ✅ 链路完整：tools_pack.rs:812 `record_tool_success` 写入 + :493-494 命中消费 + mcp/handlers.rs 同模式；无需修改 |
| 3 | fault_tolerance 故障记录 | **接线激活** | ✅ check_heartbeats 检测 Offline 时创建 FaultEvent（NetworkTimeout，防重复）；report_heartbeat 恢复时自动解析节点全部未恢复故障——active_faults/cluster_health 反映真实故障 |
| 4 | failover 组 | **保留**（pub API 面+单进程无场景） | ⏸ 无相同功能替代、无部署场景，保留 |
| 5 | alert_manager 4 规则无生产者 | **接线激活** | ✅ background 新增 30s 周期任务：evaluate p95_latency_high（avg 延迟近似）/error_rate_high/circuit_breaker_open/agent_timeout_rate |
| 6 | communication bus 死计数器 + budget 死方法 | **删除** | ✅ CommunicationMetrics.tool_* 4 字段 + record_metrics/record_message_received 删除（AgentCommunicationHook 瘦身为空结构）；budget.rs record_spawn/record_completion/record_tokens/can_spawn/reset/with_max_wall_clock/max_wall_clock_ms 删除；5 个死方法测试同步删除 |
| 7 | distributed_memory_bus 传输 | **接线激活** | ✅ 新增 `configure_from_env`（GOON_MEMORY_PEERS/SYNC_INTERVAL_MS/AUTH_TOKEN），server_builder 在 multi-users-server 启动时接线；无 peers 时纯本地（transport 线程不启动） |
| 8 | reqwest client 统一 | **部分统一** | ✅ 新增 `shared::blocking_http_client()` 单例；web_scrape/rss_read/game 4 处每调用构造改为共享 client + 请求级 timeout；其余 17 处（不同 timeout/UA/proxy/http1_only/blocking-async 差异）保留 |
| 9 | pub 访问器 | **保留**（§11 pub API 面） | ⏸ 明确规则允许，保留 |

### 验证（收口后）
cargo check 4 profile 零警告；clippy -D warnings 4 profile 零警告；cargo test --lib **1639 passed / 0 failed**（净删 6 个死方法测试）。

## 5. 验证证据（原则 §16）

```
cargo check 4 profile(local/simple-server/multi-users-server/full)  → 零警告
cargo clippy --all-targets -- -D warnings（4 profile）              → 零警告
cargo check --workspace（gui/sdk/crates）                          → 通过
cargo test --lib                                               → 1645 passed / 0 failed
cargo check -p go_on_sdk --all-targets（含 examples）              → 通过
sdk/typescript + sdk/nodejs + vscode-addon tsc --noEmit           → 通过
python3 -m py_compile sdk/python/go_on_sdk/client.py             → 通过
bash scripts/verify-zed-integration.sh                            → PASS（子代理验证）
```
