# Log 2026-08-14 #4 — 全项目多轮深度+广度扫描与统一架构精炼（R48+）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则（§4 四种 profile 零警告 / §6 clippy 零警告 / §8 禁占位 / §9 完成率回写 / §10 多轮收敛 / §11 禁死代码 / §12 禁 test fail/ignored / §13 禁假修复 / §14 完整闭环 / §16 验证证据 / §18 文档与代码一致 / §19-20 block_on 清零 / §21 独立验证），按 `docs/log/log-20260814-3.md` 的 M0–M4 里程碑推进。

用户指令：多轮超级深度+广度扫描；以统一全功能架构、各链路精炼功能、统一重复功能为目的；合并去重直到简洁高效；优化全项目执行效率、改造可改造的串行工作流；不产生新 issues；实事求是、不浮夸、不草率。

## 1. 执行计划

| 阶段 | 内容 |
|------|------|
| P0 | 基线确认（git 工作树干净 / cargo check / 1652 测试基线） |
| P1 | 5 个并行子代理分域深扫（ACP 流式链 / agents 供应商层 / orchestration 工具层 / governance+resilience / memory+intelligence+cli） |
| P2 | 亲自验证 P0 级发现并修复（mode 策略别名、file_diff schema、双 /v1、ernie 能力声明、N+1 热点、死字段/死变体/死配置） |
| P3 | 第 2 轮：1 个独立复核代理逐项审查全部改动 + 1 个全新视角扫描代理 |
| P4 | 修复复核发现（**1 处我引入的回归：GUI 生成目录丢 default_url → openai 启动失败**）+ 第二轮新发现（cohere 双 /v1、http_request 三源、bwrap degrade 12 份拷贝、edit_file 缺写锁、chaos 死 pub） |
| P5 | 全量验证矩阵（lib / clippy / 4 profile / GUI / 13 集成测试）并回写 |

## 2. 本轮记录（初始）

- 基线：log-20260814-1.md（R45-R47，1652 lib 测试全绿 → 本轮基线实测 1646）。HEAD 71b3ea09。
- 共完成 **2 轮五代理/双代理扫描 + 1 轮独立复核**，修复 **25 项**（正确性 5、重复合并 9、死代码/死配置清理 7、性能/并行 3、文档一致 1），新增 9 个测试。

## 3. 执行记录

### R48 第一轮（5 并行子代理分域深扫 → 亲自验证后修复）

**正确性缺陷（亲自验证，含探针测试实证）**

1. **F1 mode 策略表用旧别名**（`orchestration/mode.rs`）：`all_exec_tools` 列 `file_move`/`file_delete`，而注册表规范名是 `move_path`/`delete_path`（`file_move` 只是别名）；`filter_tool_calls_by_policy` 裸 `allowed.contains` 无别名解析 → **Edit/FullAuto 模式下模型发出的 `move_path`/`delete_path` 被策略误拦**。改为规范名（GUI `types.rs` 同步）。新增防回归测试 `test_policy_lists_use_registered_canonical_names`（策略名单必须能在注册表解析；该测试顺带抓到 `svg_export` 是 feature-gated 合法工具）。
2. **F2 file_diff schema 双源漂移**（`shared/tool_descriptors.rs` + `tool/extended/diff.rs`）：LLM function-calling 走表（`file1`/`file2`），MCP 走工具 override（`file_a`/`file_b`），实现读 `file_a`/`file_b` → **模型按表调用必失败**。表改为与实现一致（file_a/file_b/context_lines），删除 override（单一来源）。新增一致性测试（含 camelCase 序列化键 `inputSchema` 的坑）。
3. **F3 双 `/v1` 端点**（`agents/agent.rs`，探针测试实证）：spec url 含 `/v1`（openai/siliconflow/xai/openai_compatible 默认）拼接默认 `/v1/chat/completions` → `…/v1/v1/chat/completions`。新增 `default_openai_chat_path(base)`：base 以 `/v1` 结尾 → `/chat/completions`，否则 `/v1/chat/completions`。**同一缺陷 cohere 也有**（`join_url(base, "v1/chat")` + spec `…/v1` → `…/v1/v1/chat`），一并修复并测试。
4. **F4 ernie 能力声明与实现不符**（`agents/ernie.rs`）：`available_models` 声明 `function_calling` 但 `build_payload` 不转发 `tools`/`tool_choice`。补齐转发（仅这两个键，排除 ERNIE 不认的 response_format 等）+ 2 个测试。
5. **F5 select_best_agent N+1 热点**（`intelligence/discovery.rs` + `capability_bus/decide.rs`）：每候选一次 `search`（全表扫描 + 各自加锁 + O(n·m) touch）。新增 `best_success_rates`（单趟单锁），decide.rs 一次性取回全部候选的 discovery 分。**过程中我的查表 key 写错（用 name 而非 state_{name}）被隔离测试抓住**。

**重复合并去重**

6. **F6 StreamFrame.status 死字段**（`acp/impl/chat/streaming.rs` 等 7 文件）：`status` 字段全项目零消费（HTTP 只写 event+payload、JSON-RPC 转发 payload、GUI 只读 payload.message）。删除字段 + 21 处构造点 + `emit_status_event` 的 status 参数。
7. **F7 审计错误死变体**：`governance/audit.rs` 的 `AuditError::Integrity` + `From<audit_integrity::AuditError>`（零消费）；`security/audit_integrity.rs` 的 `ChainFileNotFound`/`IntegrityViolation` 枚举变体（零构造；同名 struct 是另一类型仍活）。全部删除。
8. **F8 code_repo_analyzer 死错误变体**（`RepoAnalyzerError::{BuildMapFailed, TypeExtractionFailed, AnswerFailed, Internal}` 零构造）→ 删除。
9. **F9 verification.rs 括号平衡双份**（`run_syntax_check` 与 `run_quality_compass_checks` 各自手写 `{}[]()` 计数）→ 共享 `bracket_balance_ok`。
10. **F10 quality_compass 与 base_checks 重复**（`governance/pua.rs`：前 3 项描述逐字重复）→ `quality_compass()` 从 `DynamicQualityCompass::default().base_checks` 派生。
11. **F11 validate_stage 与 collect_missing 重复过滤** → 共享无锁纯函数 `missing_actions`（**注意**：曾误用 `collect_missing`（二次加锁）引入死锁，测试超时当场暴露，改为无锁助手）。
12. **F12 锁中毒误报 MissingEvidence**（`pua.rs`：把"plan 锁中毒"当成"缺证据"上报）→ 新增 `LockPoisoned` 变体，删除语义死的 `MissingEvidence`。
13. **F13 semantic_cache 写锁 fallback 判定不一致**（主路径 exact→similarity，fallback 只 exact → 相似度命中变 miss）→ 共享 `find_exact_index`/`find_similar_index`，fallback 与主路径同判定（保留主路径惰性 embedding）。
14. **F14 code_repo_analyzer 双遍解析**（`extract_definition`+`infer_kind` 对同一行两次扫关键字表）→ 合并 `parse_definition`（`impl`→Other("unknown") 保留原语义；畸形行 kind 归因更自洽，无测试钉旧行为）。
15. **F15 fault_tolerance 故障事件创建双份**（`report_fault` 与 `check_heartbeats` 各自 fault_counter++ + id 格式化）→ 共享 `insert_fault`。
16. **F16 agent default_model 冗余 override ×2**（`openai_compatible.rs`/`copilot.rs` 与 trait 默认 `find(is_default)` 逐字段等价）→ 删除。
17. **F17 cli/chat.rs 内部去重**：`mode_kind_str`（3 处 match + serialize_session 的 Debug 派生 `fullauto` 不一致 → 统一规范名，加载端兼容）、`spawn_agent_chat`（4 处 channel+spawn 样板）、`record_turn_usage`（2 处）、`truncate_chars`（2 处手写 `char_indices().nth(N)` 截断，语义逐字等价）。
18. **F18 providers 模型名单矛盾**（`deepseek-r1` 建议 vs `deepseek.rs` 注释明言虚构）→ 删除建议项；重跑 `gen-provider-catalog.py` 使 GUI/VS Code 生成目录与生成器一致。

**性能/并行**

19. **F19 collect_git_diffs 串行双 git 子进程** → `tokio::join!` 并行。
20. **F20 bwrap degrade 逻辑 12 份拷贝**（`exec_common.rs` 5 个 runner + shell.rs）→ 收敛为 `should_degrade_after_bwrap`/`should_degrade_after_spawn_failure` 两个助手（零行为变化）；删除 shell.rs 的 `is_bwrap_setup_failure` 私有副本（改调 `security/sandbox.rs` 单源）。
21. **F21 cargo audit 逐路径串行**（`security/vulnerability_scan.rs`）→ `buffer_unordered(3)`（有界：共享 advisory-db 检出）。**async 闭包引用捕获触发 rustc 推断 bug** → 改用持有 PathBuf 的 `scan_one_owned` 助手。

**文档一致**

22. **F22 video_processor 注释与代码矛盾**（"Use blocking reads" vs `tokio::fs::read`）→ 修正注释。

**第二轮新发现（扫描代理 2，已修）**

23. **F23 http_request schema 三源 enum 漂移**：LLM 表（无 OPTIONS）vs MCP baseline（有 OPTIONS）vs 执行器（有 OPTIONS）→ 表改为与执行器一致（+OPTIONS/auth/query，timeout 对齐 15000，描述含 PATCH/HEAD/OPTIONS）。
24. **F24 edit_file 无写锁**（read-modify-write 竞态/lost-update；write_file 有 `try_acquire(Write)`）→ 补写锁（读前取锁，与 write_file 同语义）。
25. **F25 hyper_resilience 死 pub**：`with_chaos_engine`/`chaos_engine` 字段/record_execution 注入分支（零调用、永不触发）与 `stop_health_checks`（零调用）→ 删除。

### R49 独立复核（§21）

- **复核代理 1** 逐项审查 15 项改动 → 14 项 PASS，**FAIL 1 项（F3 连带回归）**：
  - **回归**：重跑生成器后 `gui/.../generated_catalog.rs` 对 openai/anthropic/deepseek/openai_compatible 的 `default_url` 变 None（生成器只解析字符串字面量，不解析 `DEFAULT_*` 常量）；GUI `generate_backend_config` 只从 `default_url` 写 url 行 → 生成的 `[agents.openai]` 缺 url → 后端 `required_field(url)` → **服务器启动失败**。修复：`openai`/`openai_compatible` 分支加 spec url 回退（与 deepseek/claude 同模式）+ 回归测试。
  - 另报告同类既有问题：cohere 双 /v1（已修，见 F3）。
- **复核代理 2**（全新视角）产出下一轮建议：WriteOp 统一（M1.3）、bwrap degrade 收敛（已做 F20）、build_agent 表驱动（M0.2，已评估：**不新建 profile.rs**，应扩展现有 ProviderSpec）、http_request 三源合一（已做 F23）、ChaosEngine 注入源裁决（已做 F25 删除部分）。

### P5 flaky/回归排查

- 全量并行与串行 lib 均全绿；`cargo audit` 并行改造的 async 闭包推断问题通过 `scan_one_owned` 修复（详见 F21）。

## 4. 完成率回写（principle §9）

| 项 | 状态 |
|----|------|
| 扫描轮次 | R48 五代理分域 + 亲自验证 / R49 独立复核（抓到 1 处我的回归并修复）+ 全新视角扫描；**本轮收敛：复核确认全部修复正确** |
| 修复项 | **25 项**（正确性 5、重复合并 13、死代码/死配置 7、性能并行 3、文档一致 1，含回归修复） |
| 新增测试 | 9 个（mode 策略规范名、diff schema 一致性、default_chat_path、openai spec-url 回退、ernie tools 转发 ×2、best_success_rates 对等性、cohere 端点、probe 实证） |
| 验证矩阵 | lib **1654/0**（基线 1646，+8）；clippy 0 ✅；4 profile 零错误 ✅；GUI 34 ✅；13 集成测试全过 ✅；chaos-testing feature 编译 ✅ |
| 改动面 | 39 文件，+799 / -543 行 |
| 收敛判定 | R49 复核 PASS（除已修的回归）；未提交 git commit |

## 5. 说明（如实记录）

1. **重要修正（M0.2 前提部分过时）**：`src/agents/` 供应商文件已从 37 收敛到 8 个；37 家表在 `src/core/providers.rs` 的 `ProviderSpec` 已是单一事实源。**不应新建 profile.rs 第二张表**；表驱动化应扩展 ProviderSpec（compression/chat_path_policy/url_required）——留待后续轮（中等风险，需锁住 available_models 契约）。
2. **回归自述**：重跑 provider 目录生成器暴露了一个既有同步问题（提交版生成文件与生成器不同步），我的操作把它从"潜在"变成"启动失败"——独立复核 §21 抓到并已修复（openai/openai_compatible 加 spec url 回退）。这是本轮最重要的教训：**生成器产物必须以 --check 为门禁**。
3. **行为变化提示**：Edit/FullAuto 模式下 `move_path`/`delete_path` 从"被策略误拦"变为可用（修复）；openai/openai_compatible 缺 url 配置时回退 spec 默认（启动不再失败）；`http_request` LLM schema 现在含 OPTIONS/auth/query；edit_file 并发写同一文件时返回 transient 锁冲突（与 write_file 一致）；cohere 端点从 `…/v1/v1/chat` 修正为 `…/v1/chat`。
4. **生成器根因修复（追加）**：复核发现生成目录丢失 default_url/default_model 后，除后端 spec 回退（纵深防御）外，还修了生成器本身——`gen-provider-catalog.py` 新增 `parse_consts`，从 providers.rs 与 shared/http_client.rs 解析 `pub const …: &str` 定义并解析 spec 中的常量型 url/model 字段（此前这类字段回退为 None/"auto"，导致 openai/anthropic/deepseek 的 GUI 离线默认值丢失、deepseek 会生成 `model = "auto"`）。修复后 gui 目录 diff 仅剩 deepseek-r1 一行，vscode 目录零 diff，`--check` 通过。
5. **记录未改（均有依据）**：双语义缓存合并（M2：两缓存 key 粒度不同——完整对话 vs 末条 user 消息，合并需先裁决语义，本轮不动）；build_agent 表驱动（M0.2，中等风险，需契约测试）；WriteOp 统一（M1.3，已做 edit_file 补锁这一子步，完整收口留后续）；shell.rs 换 runner（M1.1，已做 degrade 收敛子步）；validate_required_arguments 表驱动（M1.5，行为增强需同步测试）；fault_tolerance 人工操作方法（isolate/reintegrate/resolve：运维侧入口缺失，保留 pub API 面）；update_contamination 半接线（注入检测，触碰有风险，保留记录）。
6. **验证证据**：探针测试实证双 /v1（`join_url("https://api.openai.com/v1", "/v1/chat/completions")` = `…/v1/v1/chat/completions`）；隔离测试抓到我自己的查表 key bug 与一次死锁；lib 1654 全绿（含新增 9 测试）、clippy 零警告、4 profile 零错误、GUI 34、13 集成测试全过。
