# 第37轮 — 深度+广度第6轮：反假修复 + 死链清理 + 链路激活 + 三端对齐 (2026-08-06)

第36轮收尾后按用户指示继续超级深度+超级广度扫描（5 并行子代理分区扫描 `src/acp`、`src/orchestration`+`cli`、`src/governance`+`security`+`observability`+`fault_tolerance`+`resilience`、`src/agents`+`memory`+`intelligence`、`src/mcp`+`protocol`+`multimodal`+`core`+`shared`+三端跨端），全部发现经逐条代码核实后修复。**本轮主题：消灭剩余的"假成功"路径，激活已配置但未接线的链路，统一三端 RPC 契约。**

## 一、处理明细

### A. 反假修复 / 诚实性（原则 §13/§15/§18）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 1 | **导入技能假成功**：`mcp.tools.call` 执行导入技能返回 `{"ok":true,"executed":false,"code":"NOT_IMPLEMENTED_EXECUTOR"}`（假成功占位） | `acp/impl/request/tools_pack.rs` | **实现真实执行器**：manifest 带 `prompt_template` 时构造 `PromptBasedSkill` 走已接线的 LLM 执行路径（`set_prompt_skill_agent` 已在 server_builder 接线）；无执行内容/无 manifest 时明确报错，不再返回假成功 |
| 2 | **`health.check` 吞错**：探针失败仍返回 `{"ok":true}` | `acp/impl/request.rs` | 失败时返回 `{"ok":false,"error":...}` |
| 3 | **`workflow.execute` 伪造评审**：硬编码 `reviewer_N/APPROVE/approved` 评审项 | `acp/impl/request/exec_pack/execute.rs` | **真实评审**：对执行摘要跑确定性校验（`run_enhanced_verification`），verdict 由校验信号+失败子任务数决定；`review_status` 改为 `passed/rejected/not_required`；自治契约的 `corrective_actions_applied_total`/`effectiveness` 改由真实修复轮次计算（此前恒 0） |
| 4 | **`game_monitor` 假窗口状态**：`window_active=true` 硬编码 | `orchestration/tool/extended/game.rs` | 改由真实 `/proc/<pid>/stat` 进程状态推导（R/S/D 视为活跃） |
| 5 | **`game_screen_capture` 假成功**：无截屏工具时 `success:true` | 同上 | 无工具时 `success:false` + 明确错误 |
| 6 | **`game_replay_recorder` 假录制**：仅返回"ready"+命令提示 | 同上 | **真正调用 ffmpeg x11grab 录制**；成功报告文件大小，失败报告 stderr 尾部 |
| 7 | **审计链轮换丢签名**：`rotate_chain_if_needed` 重建未签名 auditor，签名链在 100MB 轮换后变未签名 | `governance/audit.rs` | 改用 `open_chain()` 重读 `GOON_AUDIT_SIGNING_KEY` |
| 8 | **分布式记忆模拟传输**：multi-users-server `do_sync` 本地自吞并报 `Completed`（"Simulated HTTP POST"） | `intelligence/capability_bus/distributed_memory_bus.rs` + `hub/server.rs` | **真实 HTTP 传输**：向每个 peer 的 `/rpc` 端点 POST JSON-RPC `memory.ingest`（支持 `auth_token` 配置）；hub 新增 `memory.ingest` 处理器（vault 真实存储，`hub.list`/`hub.retrieve` 可观测）；全失败时如实 `Failed`；模块文档同步重写 |
| 9 | **`initialize_postgres_backend` 文档/代码不符**：文档称 3 次指数退避重试，代码只跑 1 次 | `acp/transport_factory.rs` | **实现重试**：1s/2s/4s 退避循环在阻塞池上按文档执行（closure 改 `Fn`，vector store 的 provider 改为每次尝试重建，env 确定） |
| 10 | **`chat_pack::send_error` 双重平台上下文注入**（`io::send_error` 已注入，`inject_platform_profiles_if_absent` 幂等故结果相同但重复计算） | `acp/impl/request/chat_pack.rs` | 删除本地注入，统一由 `io::send_error` 注入 |

### B. 死代码清理（原则 §11）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 11 | `IDEMPOTENCY_HIT_TOTAL` 无生产者计数器（快照字段恒 0） | `acp/helpers/autonomy/autonomy_metrics.rs` + 2 个读取点 | 删除计数器+快照字段+读取子句 |
| 12 | `agent.rs` 私有 `routing_handles` 重复实现 | `acp/impl/agent.rs` | 删除，导入 `runtime::routing_handles` 权威版；`extra_u64` 包装一并内联 |
| 13 | `dispatch_server` 死参数 `_client`（从未读取） | `acp/transport_factory.rs` + `main/server.rs` + 测试 | 删除参数与调用点 |
| 14 | `GovernanceStatus::to_json` 无调用者 | `governance/status.rs` | 删除 |
| 15 | `record_audit_threadsafe` 无调用者（仅文档示例） | `governance/audit.rs` | 删除 |
| 16 | `mcp/tools.rs` 纯转发壳（27 行） | `mcp/tools.rs` + `mcp/mod.rs` + `mcp/handlers.rs` | 删除文件；`error_codes` 移入 `mcp/mod.rs`；`validate_required_arguments` 直连 `shared::tool_descriptors`（tools_pack 的二次转发一并内联） |
| 17 | `McpServer.logging_level` 字段永不读取 | `mcp/mod.rs` + `mcp/handlers.rs` | 删除字段与写入（`logging/setLevel` 的真实行为 RUST_LOG+filter reload 保留） |
| 18 | `SESSION_UPDATE` 常量+ALL 表项（无分发 arm，仅服务端→客户端通知） | `acp/method_names.rs` | 从 `ALL` 删除并删除常量（`is_known("session/update")` 不再伪装已知） |
| 19 | `harness_bus::types::AuditEntry` 中间类型（两处调用点仅作转换） | `governance/harness_bus/{types,mod}.rs` + `capability_bus/core.rs` | 删除类型，两处直接构造 `AuditLogEntry`（时间戳统一 `chrono_now()`）；`evaluate` 中 `tokio::join!` 两个同步操作改为顺序调用 |
| 20 | `AUDIT_ENTRY_COUNT` 静态计数重复 sink 自身计数 | `intelligence/hub.rs` | 删除静态，`hub_metrics` 改读 `global_audit_log().len()` |
| 21 | 3 处私有 `sha256` 包装 | `shared/mod.rs`（新增权威）+ `security/{audit_integrity,request_signing}.rs` + `shared/provenance_helpers.rs` | 统一为 `shared::sha256_bytes`/`sha256_hex` |
| 22 | `time.rs::unix_timestamp` 重复实现 | `orchestration/tool/extended/time.rs` | 改委托 `shared::timestamps` |
| 23 | `exec_common.rs` 文档引用已删除工具 `compile_and_run` | `orchestration/tool/exec_common.rs` | 修正文档 |

### C. 链路激活（原则 §14，用户要求"补充完整接线激活"）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 24 | **工具回退链从未生效**：executor 主分发路径（自治循环+ACP agent runtime）直调 `run_async`，`fallback_chain`（read_file→search_files、grep→search_files 等 10+ 条）只有 CLI 路径生效 | `orchestration/tool/executor.rs` | 新增无钩子回退助手 `run_tool_with_fallback`，主路径在 `success=false` 时按 profile 回退链执行（保持重试/钩子/SSE/审计语义不变） |
| 25 | **故障容忍恢复周期只在测试中运行**：`run_recovery_cycle` 生产无调度（对比 `hyper_resilience.start_health_checks()` 已接线） | `acp/background.rs` | 在 `start_background_tasks` 增加 30s 间隔调度（跳过首拍，事件级日志） |
| 26 | **`state_sync` 模型/agent 事件无发布者**：`ModelsChanged`/`AgentsChanged` 变体 GUI/VS Code 订阅端永远收不到 | `acp/impl/request/config_pack.rs` | `config.reload` 对比 registry agent 集合与配置 agent 集合发布 `AgentsChanged`；对比上轮配置模型集合发布 `ModelsChanged` |
| 27 | **第二条未链式审计文件**：`governance.plan.update` 写 `.goon/governance/audit.ndjson`（非链式、不轮转），`governance.audit.recent` 逐请求整文件解析 | `acp/impl/request/governance_handlers/audit.rs` | 并入 canonical sink（`global_audit_log()`，链式+轮转）；`load_governance_audit_events` 改读内存 sink 并按 `phase=="governance"` 过滤（新增 ISO→ms 解析）；响应结构保持不变（SDK/VS Code 兼容） |
| 28 | **漂移监控无指标源**：`record_metric` 无生产调用者，60s 监控恒扫空集，告警永不触发 | `governance/harness_bus/mod.rs` | `validate_action`/`verify_output` 上报真实延迟指标（Performance 漂移）；`HarnessBus::new` 注册默认性能漂移策略 |

### D. 重复统一 / 正确性（原则 §8）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 29 | **`web_scrape`/`rss_read` SSRF 绕过**：绕过 `http_request` 的私网 IP 沙箱 | `orchestration/tool/extended/{http,web,rss}.rs` | `validate_url` 改 `pub(crate)`，两个工具强制调用（同 SSRF/私网防护） |
| 30 | `is_low_risk_tool` 过期工具名：`time_util`/`diff`/`rss_feed` 非注册名（`file_diff`/`rss_read` 因此拿不到低风险快路径） | `orchestration/tool/governance_gate.rs` + `executor.rs` 测试 | 修正为 `date_time`/`file_diff`/`rss_read` |
| 31 | **CLI 硬编码别名表与注册表冲突**：`"grep"=>"search_files"` 使 `/grep` 跑文件名搜索而非注册的内容 `grep` 工具；`bash`/`execute_command` 与注册表别名重复 | `cli/chat.rs` | 先查注册表（含别名），命中按原名执行；仅未注册名走 CLI 别名 |
| 32 | gzip 解压双实现 | `orchestration/tool/extended/{compress,archive}.rs` | 共享 `decompress_gzip_bytes` 助手 |
| 33 | MCP `filter_tools_by_exposure` 过期名单：`container_build/run/stop`、`compile_and_run` 非注册名，`qrcode_` 前缀无匹配 | `mcp/handlers.rs` | 修正为 `docker_build`/`docker_push`，删除无效项 |
| 34 | harness_bus `evaluate` 中毫秒字符串时间戳与 canonical sink ISO 格式不一致 | `governance/harness_bus/mod.rs` | 统一 `chrono_now()` |

### E. 性能优化（用户要求加速）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 35 | 配置校验重复 `AppConfig::load`（启动路径双读 TOML） | `core/config_validation.rs` + `main/server.rs` | 新增 `validate_config_with` 复用已加载配置 |
| 36 | Copilot 客户端逐请求构建（串行探测最多 7 个代理端口 + 新建 client） | `acp/impl/request/runtime_pack.rs` | `ProxyEnvSnapshot`+`OnceLock` 缓存（env 未变走快路径），新增单元测试 |
| 37 | data-URI 附件串行处理、`observe_phase` 逐 URL 新建 client、多代理安全网伪造 `cache_hit=true` | `acp/impl/chat_phases.rs` | `join_all` 并行；共享 `http_client()`；删除 `cache_hit=true` 行 |
| 38 | 能力总线选择与向量上下文串行（无依赖） | `acp/impl/chat/agent_selection.rs` | `tokio::join!` 并行 |
| 39 | 同步文档解析在 tokio worker 上执行 | `multimodal/mod.rs` | `spawn_blocking`（对齐音频路径） |
| 40 | MCP HTTP JSON-RPC batch 串行分发 | `protocol/mcp_server.rs` | `join_all` 并行（保序、通知过滤不变） |
| 41 | `/proc` 内存/CPU 每次状态请求 3 次同步文件读取 | `observability/performance.rs` + `acp/server.rs` | 5s TTL 缓存；`get_status` 复用单次快照 |

### F. 三端统一（原则 §2，跨端 RPC 契约）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 42 | **VS Code 插件 12 个幻影 RPC**（后端无处理器，恒 `-32601`，审批面板功能损坏） | `vscode-addon/src/{approvalPanel,chatView,commands/agent,commands/config,commands/workflow,commands/workspaceContext,coreCommandRegistry,runtimeManager}.ts` | 全部映射到真实后端方法（`session/request_permission`、`conversation.checkpoint.create`、`skill.import`、`config.baseline`、`config.reload`、`session/delete`、`vector.clear`、`tools/call`）；无对应方法的命令（`config.reset`/`agent.remove`）改为明确失败+warn；`tsc --noEmit` 通过 |
| 43 | **TypeScript SDK 4 个幻影方法** | `sdk/typescript/src/client.ts` | 改名对齐其余 3 个 SDK 与后端：`workflow.plan→task.plan`、`summary.get→learning.summary`、`knowledge.search→knowledge.distill`、`rl.optimize→rl.alignment.offline_eval`；`tsc` 通过 |

### G. 复核保留项（非死代码，记录依据）

- **`PolicyEvaluator.safety_checker`/`injection_detector` 字段与分支**：`server_builder.rs` 有明确注释（默认配置会误伤合法内容，字段为文档化扩展点）；与第 35 轮"扩展点锚定"先例一致，保留。
- **`AutonomyLoopReport` 的 `corrective_actions_applied_total=0` 等字段**：该循环路径确实不执行纠错动作，0 为真实值非伪造；`workflow.execute` 路径的真实纠错计数已在 #3 修复。
- **evolution_loop `placeholder.rs` 兜底补丁与 `RequireApproval` 自动批准**：生产路径为 `AutoApproval` + 已接线 `SelfEvolutionAgent`（启发式补丁生成），stub 分支仅在无 agent 时可达（测试场景）；第 35 轮已确认该子系统为文档化扩展点。
- **`idempotency_pending_continuation_*` 快照字段**：调试面板透传展示字段，无消费方逻辑，保留（面板显示 0 属真实值）。

## 二、验证证据（原则 §16）

```
=== 编译 ===
cargo check --all-targets（local）                                → 零警告零错误
cargo check 4 profile(local/simple-server/multi-users-server/full) → 零警告
cargo check --workspace(gui/sdk/crates)                           → 零警告
=== Lint ===
cargo clippy --all-targets -- -D warnings（4 profile）             → 零警告
=== 测试 ===
cargo test --all-targets → 全绿：3486 passed / 0 failed
   （rpc_workflow_execute_enforces_dual_review 单独复跑通过，真实评审生效）
=== 生成物 ===
scripts/gen-provider-catalog.py --check                            → 双输出 OK（37 providers）
=== 跨端 ===
vscode-addon tsc --noEmit 通过；sdk/typescript tsc 通过
=== 代码量（本轮） ===
56 个 src/ 文件 + 2 个测试/文档面改动；净 +582 行（反假修复与链路激活为主，死代码清理 -~700 行）
```

行为一致性说明：
- #3 评审语义变化：`workflow.execute` 的 `reviews` 数组从恒 APPROVE 变为反映真实校验与执行结果；配套集成测试仍绿（正常执行+校验通过→APPROVE）。
- #8 分布式传输语义变化：无 peer 或 peer 不可达时不再"假装送达"；`shared_entries` 仅由 `share_with_peers`/`ingest_shared` 真实路径填充。
- #24 回退链激活：仅在主工具 `success=false` 时尝试回退，成功路径行为不变。
- #26/#27/#28 为纯增量接线，无既有行为回退。

## 三、完成率回写（原则 §9）

- 扫描模块数：5 分区覆盖 `src/` 全部 451 个 .rs 文件 + gui/sdk/vscode-addon 跨端面。
- 修复项数：**43 项**（A 反假 10、B 死代码 13、C 接线 5、D 统一 6、E 性能 7、F 三端 2）。
- 复核保留项：4 组（扩展点锚定/真实零值/文档化兜底/面板透传字段）。
- 遗留问题数：**7 项**（均低-中收益，留待后续轮）：
  1. `requirement.rs` 3× 自动恢复逻辑合并（深逻辑，收益中等）；
  2. `agent_runtime.rs`/`autonomy.rs` 3× agent token 分类提取（低收益风格项）；
  3. ACP bridge 与原生 MCP 的 resources/prompts 处理器去重（中等）；
  4. `document_parser` 与 `extended/pdf.rs`/`docx.rs` 解析去重（中等）；
  5. `TokenMultiLevelCache` 主路径写入 + `cache_strategy` 每 L2 命中 3× embedding（中等）；
  6. 三套 embedding/similarity 栈统一（大重构）；
  7. 自进化子系统 LLM 接线（`SelfEvolutionAgent` llm_agent 注入 + `MemorySummarizer` LLM 路径，大特性）。

---

# 第38轮 — 遗留 7 项收口：重复合并 + 三栈统一 + 自进化 LLM 接线 (2026-08-06)

第37轮遗留的 7 项优化本轮全部完成并一次验证（先改完，最后统一跑完整验证）。主题：统一架构（重复逻辑唯一收敛）、消除隐藏重复（embedding 三栈、文档解析、MCP 处理器、token 分类）、激活已配置未接线的自进化 LLM 链路。

## 一、处理明细

### A. 重复功能合并（原则 §8）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 1 | **requirement 自动恢复 3× 合并** | `acp/helpers/requirement/{requirement,requirement_continuation}.rs` + `acp/impl/request/workflow_pack.rs` | `evaluate_requirement_gate_facade` 改为纯求值；synthesize→inject→re-evaluate 序列唯一收敛到 `try_auto_recover_requirement_gate`（成功处为唯一指标记录点）；删除 `RequirementContinuationKind::ClarificationInProgress` 死变体（守卫条件与 Case 2 相同，为死代码）；**顺带修复双计数**：`workflow_pack` 两处手动 `record_requirement_auto_recovery()` 删除（该指标已在 `try_auto_recover_requirement_gate` 成功路径记录一次，且原代码会把纯 `Confirmed` 续流误记为自动恢复） |
| 2 | **agent token 分类 3× 提取** | `orchestration/autonomy_runtime.rs` + `acp/impl/chat/agent_runtime.rs` + `acp/helpers/autonomy/autonomy.rs` | 新增 `AgentToken` enum + `classify_agent_token()` 统一分类器；3 个收集循环（`collect_agent_responses`/`run_agent_collecting`/`run_followup_after_tool_observation`）改用同一分类器；`autonomy_loop.rs` 的 SSE 转发循环保留内联处理（需区分 finish_reason/usage、回填工具调用文本、按 SSE 帧语义转发，与分类器折叠语义不同，非重复） |
| 3 | **ACP bridge 与原生 MCP resources/prompts 去重** | `acp/impl/request/protocol_pack/mcp.rs` + `mcp/handlers.rs` | 新增 5 个共享函数（`mcp_resources_list_value`/`mcp_resources_read_value`/`mcp_prompts_list_value`/`mcp_prompts_get_template_value`/`mcp_prompts_get_agent_value`）；bridge 的 `mcp_*_payload` 与原生 `handle_*` 全部委托共享函数；删除原生侧 3 个重复构造方法（`build_prompt_list`/`resolve_template_prompt`/`resolve_agent_prompt`） |
| 4 | **document_parser 与 pdf/docx 解析去重** | `multimodal/document_parser.rs` + `orchestration/tool/extended/{pdf,docx}.rs` | `read_pdf`/`read_docx` 改委托 `DocumentParser::default().parse_bytes(...)`；保留 `page_count`/`paragraph_count` 兼容字段并新增 `images`/`tables`/`metadata`（纯增量 key，兼容既有调用方）；`pdf_merge`/`pdf_split`（对象级文档操作）与 `office.rs`（文档生成）不属文本提取，保留各自实现 |
| 5 | **TokenCache 主路径写入 + 消除每 L2 命中 3× embedding** | `intelligence/token_cache/mod.rs` + `acp/helpers/cache_strategy.rs` + `acp/impl/chat_phases.rs` | `lookup`/`peek_similar` 返回预计算置信度（L1/L3=1.0，L2=cosine 得分），`decide_from_entry(level, entry, confidence, is_execution_like)` 不再内部重新 embedding；主路径（非 fallback）语义命中块新增 `store_async` 写入 token cache（此前主路径从不填充自己读取的缓存）；`CachedAgentWrapper::chat` 命中时同样套用执行类 bypass 门 |
| 6 | **三套 embedding/similarity 栈统一（大重构）** | `memory/embedding_provider.rs`（权威）+ `intelligence/token_cache/mod.rs` + `memory/semantic_cache.rs` + `orchestration/skill/execution.rs` + `shared/math.rs` | 三套独立实现全部收敛到 `local_hash_embed` + `shared::math::cosine_similarity_f32`：token cache `simple_embedding` 委托（256 维）；`semantic_cache` 删除 bigram/Jaccard 路径改存预计算 embedding + cosine（128 维）；skill 语义匹配删除 f64/DefaultHasher 桶实现（96 维）；删除已无调用者的 f64 `cosine_similarity` 及 4 个死代码测试 |

### B. 链路接线激活（原则 §14，大特性）

| # | 项 | 位置 | 处置 |
|---|----|------|------|
| 7 | **自进化子系统 LLM 接线** | `acp/background.rs` + `agents/self_evolution_agent.rs` + `acp/server.rs` + `memory/{summarization,memory_persistence}.rs` | `SelfEvolutionAgent::with_llm(..., llm_agent)`：后台任务从 `agent_registry` 解析（assistant→summarizer→首个）注入，`generate_patch` 走真实 LLM 路径（`analyze_code` 为确定性静态分析，文档修正不再声称调用 LLM，原则 §18）；`MemorySummarizer` 加 `Clone`、`llm_agent: Option<Arc<dyn Agent>>`，`server_builder` 用注册表 LLM agent 构造并复用（`get_or_init_memory_persistence` 不再新建无 LLM 的默认实例）；`summarize_hot_entries` 改 async：快照→await 真实 `summarize`→重锁替换（跨 await 不持 std Mutex），`auto_migrate` 唯一调用点 `.await` |

### C. 复核保留（非死代码，记录依据）

- `autonomy_loop.rs::run_autonomy_loop` 的内联 token 处理：需区分 finish_reason/usage、把工具调用文本回填进 `round_response`、按 SSE 帧语义转发，与 `classify_agent_token` 的折叠语义不同，保留。
- `pdf_merge`/`pdf_split` 的 lopdf 直接使用：对象级文档操作（页树重排/重组），`DocumentParser` 只负责文本提取，不构成解析重复。
- `learning_pack.rs::signature_similarity`/`semantic_matcher.rs::tokenize`：字符串签名/关键词匹配，非 embedding 栈，保留。

## 二、验证证据（原则 §16）

```
=== 编译 ===
cargo check --all-targets（local）                                → 零警告零错误
cargo check 4 profile(local/simple-server/multi-users-server/full) → 零警告
=== Lint ===
cargo clippy --all-targets -- -D warnings（4 profile）             → 零警告
=== 测试 ===
cargo test --all-targets → 全绿：3478 passed / 0 failed
   （上轮记录 3486；本轮明确删除 4 个 f64 cosine 死代码测试，其余差异为跨轮工作区变动）
=== 行为说明 ===
- #1 指标语义修正：auto-recovery 计数不再双计/误计（纯 Confirmed 续流不再 +1）。
- #6 相似度语义统一：skill 匹配/语义缓存/向量检索使用同一 embedding + cosine，阈值语义一致。
- #7 自进化与记忆汇总走真实 LLM：无 LLM agent 注册时行为与之前一致（回退启发式/截断）。
```

## 三、完成率回写（原则 §9）

- 修复项数：**7 项**（重复合并 6 + 链路接线 1，含顺带修复的 auto-recovery 指标双计数）。
- 上轮遗留 7 项全部关闭；无新增遗留。
- 全项目 4 profile 编译 + `clippy -D warnings` 零警告，3478 测试全绿。
