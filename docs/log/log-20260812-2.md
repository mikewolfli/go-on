# Log 2026-08-12 #2 — 全项目多轮深度+广度扫描与统一架构精炼（第 17–40 轮，已收敛）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则（§8 禁占位 / §9 完成率回写 / §10 多轮收敛 / §11 禁死代码 / §13 禁假修复 / §14 完整闭环 / §16 验证证据 / §18 文档与代码一致 / §19-20 block_on 清零 / §21 独立验证）。

用户指令：多轮持续超级深度+超级广度扫描，以统一平台、统一架构、统一路线、统一链路为目标，反复深度分析功能模块重复/冗余并合并精炼，核查已完成/未完成项并补齐继续实施，链路功能重复冗余全面分析合并，全链路完整性扫描（中/大收益可重构或补齐接入激活），反复扫描直到**连续 2 轮无新发现**为止，完成后回写本日志。**本轮只处理 backend 部分（src/、crates/、config/、tests/），gui、vscode-addon、文档（除本日志）一律不动。** 本轮专项：**恒值路径清理与合法性核实**（恒值若合法不得静默，必须有提示）、重复功能合并、效率优化、串行工作流改造。

## 1. 执行计划

| 阶段 | 内容 |
|------|------|
| P0 | 回写本记录（本轮日志创建，先记录后执行） |
| P1 | 基线确认（git 状态 / cargo check / lib 测试数）+ 在案债务现状核实 |
| P2 | 逐轮并行扫描（只读子代理分域：协议链路 / 编排智能链路 / 记忆治理韧性 / 核心入口共享 / 工具扩展 crates 测试；R18–R26 共 9 轮收敛轮） |
| P3 | 独立验证（principle §21）：逐项 grep + 通读 + 独立程序实测核实，不信任自我报告 |
| P4 | 修复执行（主代理逐一核实后修复），全部闭环并附验证证据 |
| P5 | 反复扫描直至收敛（R18–R26 每轮都有新发现；R27 扫描启动时由用户终止；本会话续跑 R27–R40，R39+R40 连续两轮零发现达成收敛） |
| P6 | 全量验证矩阵（cargo test --lib / clippy -D warnings / 4 profile / game-* 单 feature / gis-gpx / 三个 crate） |
| P7 | 回写执行结果（完成率、遗留问题、收敛判定） |

## 2. 本轮记录

- 创建时间：2026-08-12（P0 先行回写）
- 基线：log-20260812-1.md（第 16 轮）仅创建了记录，执行未完成；本会话从第 17 轮 R17 收尾开始，继续 R18–R26 共 9 轮完整收敛扫描
- **启动基线实测**：R17 收尾修复了 rss.rs 回归测试失败（XML 标签提取独占区间 `0..len.saturating_sub(n)` 漏掉末尾位置），并统一 XML/大小写不敏感文本原语（`src/shared/text.rs`、`extended/utils.rs::extract_xml_tag`），rss/gpx 消除 `to_lowercase()` 全量拷贝，`cache_strategy.rs` 重复的 contains 函数合并
- 本轮重点：**backend 全深度扫描**（9 轮收敛轮，每轮换新角度穷举），恒值路径清理与合法性核实、重复功能合并、安全边界（OOM/路径穿越/SSRF/日志注入）、效率优化
- 续轮（本会话续跑）：R27–R28 两轮收敛（超长行排空语义 / MCP 日志注入 sanitize / learning-records 封顶+轮转 / sampling 输出上限与超时 / ArtifactLedger 原子写 / JSON-RPC batch 形态对齐 / grep 命中才转 String）；R29 起继续扫描直至**连续 2 轮零发现**

## 3. 执行记录

### 验证矩阵（R26 结束时全绿）

- `cargo test --lib`：**1584 passed / 0 failed**（含本轮新增 ~22 个回归测试）
- `cargo clippy --all-targets -- -D warnings`：**0 警告**
- 4 profile（local / simple-server / multi-users-server / full）`cargo check --no-default-features --features X --lib --bins`：**全部零错误**（multi-users-server 的 --all-targets 有既有的 chat_tests compile_error 门控，非本轮引入）
- game-* 7 种单 feature（game-online/process/screen/input/agent/state/modding）：**全部编译通过**（R23 修复，此前全部失败）
- gis-gpx feature：编译 + 3 个新增测试通过
- go-on-latex：24 测试通过（含 3 个 CJK 字节偏移回归）；go-on-web-search：编译通过

### R18（3 项）— 收敛轮

1. `strip_trailing_incomplete_escape`（agents/mod.rs）：扫描报 P1 字节切片 panic，**独立程序实证为误报**（右向左回退只落在 ASCII `"`/`\` 位置或字符边界）；补 10 用例回归测试锁定不变式
2. `generate_skill_name_from_conversation`/`generate_workflow_name`（acp/impl/chat.rs）：lowercase 副本坐标提取技能名导致小写化 + 绕过注册表重名检测；改原文提取（反引号 ASCII 边界安全）+ 共享 contains 原语，消除 to_lowercase 拷贝
3. `health_interval_seconds`（config）恒定义不上报不调度：**接线**为 background.rs 周期健康自检循环（变化检测式日志，非每周期噪声；与上轮 maintenance_interval_seconds 同模式）

### R19（8 项）

1. **P1** crates/go-on-latex：`MathExpression.start/end` 是 char 索引却被当字节偏移切片（CJK 前置即 panic）→ 改 char_indices 记录字节偏移 + 3 个 CJK 回归测试
2. **P1** `TOOL_CALL_ACC`（agents/mod.rs）：thread_local 累积器在 tokio 多线程下被 worker 迁移拆散、异常结束/重试污染下一条流 → 改为随流 `ToolCallAccumulator`（stream_sse_to_sender/copilot 持有，流结束即丢弃）
3. **P2** http.rs `max_response_bytes` 在 `response.text()` 全量读入后才检查 → Content-Length 预检 + 流式截断（async bytes_stream / sync `Read::take`）
4. **P2** http.rs SSRF：`is_private_host` 只查字面 IP，主机名型内网地址（如 metadata.google.internal）全放行 → 加 best-effort DNS 复检（ToSocketAddrs，任一地址私网即拦截）
5. **P2** 进化沙箱 glob：`src/**/*.rs` 白名单被 `src2/` 绕过、`src/../../x` 逃逸 workdir → 拒绝 `..` 组件 + 段边界匹配 + 精确匹配去前缀漂移（+2 回归测试）
6. **P2** `blocking_runtime()` 单例 current-thread runtime 被并发 `block_on`（LSP 工具并行时 UB）→ 新增 `with_blocking_runtime` 互斥守卫，6 处调用点全部改走守卫
7. **P3** hub/server.rs Bearer 鉴权 `contains` 子串匹配（`Bearer <token>garbage` 通过）→ 精确匹配（strip_prefix + trim == token）
8. **P3** sse_compressor gzip 解压无输出上限（zip bomb）→ `Read::take(16MiB)` 截断 + 显式 warn

### R20（3 项）

1. **P1** archive.rs `extract_zip` zip-slip（`..`/绝对路径/反斜杠条目逃逸 output_dir）→ 拒绝 + 4 用例回归测试（zip 2.x 写入端不拒绝，提取端是唯一防线）
2. **P2** compress.rs `decompress_gzip_bytes` 解压无上限 → 1GiB 上限（take + 超限报错）
3. **P3** diff.rs `builtin_diff` LCS 表 O(m×n) 无上限（5 万行 → 20GB）→ 5000 行/文件护栏，超限 bail（改返回 Result）

### R21（4 项）

1. **P1** skill_import.rs：manifest `version`/`name` 是路径组件但无校验（`version="../../x"`、`name=".."` 逃逸 skills 缓存目录，CLI 市场安装默认启用）→ `validate_skill_version` 新校验 + `validate_skill_name` 拒绝 `.`/`..`
2. **P2** `RemoteSkill::new` endpoint 无 URL 校验（import 时即 SSRF 面）→ http/https scheme + `is_private_host` 私网拦截
3. **P3** stdio.rs `spawn_stdin_lines` 单行无上限（ACP/MCP stdio 唯一无界入口）→ `Read::take` 截断读取 + 超限丢弃消息（10MiB 对齐 MAX_BODY_SIZE）
4. **P3** git 工具描述承诺 add/commit/branch/tag 但白名单只有只读 5 项 → 描述与 MCP schema 收敛为实际白名单

### R22（5 项 + 1 个附带真 bug）

1. validate_skill_version 拒绝合法 semver `+` 构建元数据 → 放行（仍拒绝 `/` `\` `..` 控制字符 超长）
2. `is_private_host` 阻塞 DNS 在 async worker 上执行无超时 → 新增 `validate_url_async`/`is_private_host_async`（spawn_blocking + 2s 超时），http.rs run_async 与 chat_phases 预取改走 async 变体
3. session.rs `user_auth_token_ttl_seconds as i64 * 1000` 在 u64::MAX 下回绕 → `AuthConfig::from` clamp(1, 1 年)
4. **附带真 bug**：sandbox.rs `CodePatch::apply_to_file` 对 `ln==0` 走 else 分支追加内容到文件尾 → 修复为跳过 + 回归测试
5. 测试缺口补齐：skill_import 3 个（name/version 拒绝矩阵 + RemoteSkill localhost 拒绝）、gpx 3 个（feature 门控）

### R23（7 项）

1. **P1** tar 提取路径穿越（tar crate `Entry::unpack` **不校验路径**，仅 unpack_in 校验；R20 假设 unpack 自带防护是错的）→ 与 zip 同款拒绝 + 手工构造 ustar 回归测试
2. **P2** game-* 单 feature 全部编译失败（pub use 整体门控但工具按各自 feature 定义）→ mod.rs 按 7 组拆分 cfg + game.rs UNIX_EPOCH 门控补 game-modding；7 组合全部编译通过
3. **P2** chat_phases 预取 `resp.text()` 无字节上限 → bytes_stream 边读边限（8KB）
4. **P2** `RemoteSkill::new` 同步阻塞 DNS 残留 → import 路径 spawn_blocking + 超时
5. **P3** 并发导入 index.json 丢失更新（每调用方新建 store）→ save() 静态锁 + 磁盘记录合并 + 语义注释
6. **P3** zip/tar 解压无条目数/总字节上限 → MAX_ARCHIVE_ENTRIES(10k) + MAX_ARCHIVE_EXTRACT_BYTES(1GiB)，zip 用 take 强制、tar 普通文件手动 copy+take+保留 mode
7. **P3** spawn_blocking 超时后任务仍占用阻塞池线程 → 文档化取舍（补注释）

### R24（11 项）

1. **P1** tar symlink/hardlink 条目链接目标不校验（`ln→/etc` + 后续条目 `ln/evil` 穿透写）→ fail-closed 拒绝 symlink/hardlink/特殊条目
2. **P2** audit `rotate_file` 固定归档名 `.1.gz` 每次轮转覆盖上一份 → 递增序号 + 流式 gzip 压缩（去 200MB 内存峰值）
3. **P2** `sanitize_path_for_write` 悬挂 symlink 叶子 canonicalize 失败回退绕过 base 检查 → 叶子 symlink_metadata 检查拒绝
4. **P2** 工具列表 HashMap 序进入 LLM 请求/MCP 资源（每次启动顺序随机）→ names()/all_names()/tools_by_exposure() 排序
5. **P3** `names().first()` 兜底随机 → 已随排序确定（无需改）
6. **P3** skill index.json 非原子写 + 写序随机 → temp+rename 原子写 + 按 name 排序
7. **P3** `copy_dir_recursive` 目标在源内 → 无限递归嵌套 → 拒绝 dst.starts_with(src)
8. **P3** 可观测性 3 处静默吞错（archive mode、预取流错误、metacognitive save）→ 补 debug 日志
9. **P3** council list_members/list_proposals 零生产调用 → 跳过（记录）
10. **P3** go-on-web-search `search_custom` 注释声称未实现的回退 → 注释修正

### R25（11 项）

1. **P1** `sanitize_path_for_write` fallback 返回原始字符串（含 `..`），`Path::starts_with` 组件比较不归一化 → 解析后仍含 ParentDir 组件即拒绝（3 个回归测试，含悬挂 symlink）
2. **P2** web_scrape/rss_read 响应体无上限 → 抽 `read_blocking_body_capped` 共享 helper（content-length 预检 + take），三处复用
3. **P2** audit hash chain 归档从不清理（不同 stem）→ chain 旋转后 cleanup("audit_chain.ndjson", ...)
4. **P2** compress/decompress/archive_extract/cargo_check 描述与实现参数名不一致（source/destination vs path/output_path；谎称 zip/tar.gz/features）→ 描述与校验对齐实现
5. **P3** tar 条目上限按已提取文件数计数（目录/filter 跳过不计数）→ enumerate 全量计数
6. **P3** MCP client 响应无上限（stdio 行 + HTTP body）→ read_until+take(10MiB) + bytes_stream 累计上限
7. **P3** security_governor `regex_match` 每次评估编译正则 → 编译缓存（LazyLock Mutex HashMap，128 上限）
8. **P3** 输入侧 fs::read 无上限（compress/decompress/gzip 提取）→ `read_file_capped`；search.rs grep 改逐行流式
9. **P3** cargo_check features 参数实现忽略 → 从描述移除

### R26（10 项）

1. **P2** JSON-RPC batch 无数量上限（≤10MB 可携 ~27 万请求并发放大）→ MAX_BATCH_SIZE=100（stdio + HTTP 两传输，-32600 响应）
2. **P2** read_file 无大小上限（最常用工具漏封）→ sync/async 用 `read_file_capped(+async)`（1GiB，提升到 exec_common 共享，compress/archive/builtin_tools 复用）
3. **P2** read_file_lines 全文件缓冲（宣称大文件定向读取）→ 流式逐行 + 单行 1MiB 上限 + 超长行排空报告
4. **P2** grep 单行无上限（R25 流式化残余）→ 单行 1MiB 上限 + 排空（行号语义保持）
5. **P3** apply_patch 无内容大小上限（绕过 write_file 沙箱）→ MAX_WRITE_PAYLOAD_BYTES 共享常量 50MB（sync/async）
6. **P3** archive_inspect 条目数无上限 → 复用 MAX_ARCHIVE_ENTRIES
7. **P3** JSON-RPC method/id 换行注入日志（未认证客户端伪造日志行）→ handle_request 入口拒绝 C0/C1 控制字符
8. **P3** cleanup_old_archives 前缀误匹配（会删 audit.ndjson.backup.gz）→ 严格匹配 `<stem>.<digits>.gz` + 回归测试
9. **P3** go-on-web-search 响应体无上限 → capped_json（reqwest chunk 循环 + 10MiB）
10. **P3** 审计 writer 无界 mpsc channel → sync_channel(4096) + try_send 满时丢旧计数（内存缓冲的 dropped_count 语义一致）

### R27（9 项）

1. **P1** 超长行排空语义修复（read_lines / search / stdio 三处）：旧排空用 `read()` 循环会过度消费内部缓冲，静默吞掉下一行/下一条消息的前缀（数据损坏）→ 改为只排空到换行符为止；且 read_lines 窗口（start_line）**之前**的超长行同样必须排空（否则 1MiB 分段被误计为多行，行号静默错位）
2. **P2** MCP 日志注入面：新增 `sanitize_log`（剥离 C0/C1 控制字符），handlers 内 5 处客户端可控值进日志前过 sanitize（cancelled requestId / logging level / completion ref_name+arg_name / 版本协商 / 未知工具与未知资源 uri+错误）
3. **P2** 控制字符 id 回显：method/id 含控制字符被拒时响应**回显原 id**（此前回 None，传输层会误判为通知而吞掉错误 → 客户端永久等待）；serde_json 序列化自动转义，回显安全
4. **P2** 传输层 4 点错误 sanitize（stdio 批/串行 + HTTP 批/单）：`handle_request` 错误信息含客户端参数回显，进日志前过 sanitize_log
5. **P2** learning-records 读侧封顶：`load_learning_records`（governance/pua.rs）与 `scan_learning_records_with_parseability`（acp/learning_pack.rs）`read_to_string` → `read_file_capped`（每次 summary/replay 全量重读，行追加文件可涨至数百 MB → OOM）
6. **P2** go-on-latex 解析栈溢出：`\frac` 递归不要求花括号，超长 `\frac` 链使线程栈溢出（Rust abort 进程）→ `MAX_PARSE_DEPTH=256` 深度守卫 + 2 个回归测试（病态输入优雅降级 / 正常嵌套不受影响）
7. **P3** HTTP 传输 batch 形态对齐 JSON-RPC 2.0 §6：批超限错误以**数组**响应（此前单对象，客户端解析错位）；空数组批非合法请求 → 单个 -32600 错误对象
8. **P3** ArtifactLedger 并发写：归档名改毫秒时间戳（秒级并发写同 stem 互相覆盖）+ latest 文件 tmp+rename 原子替换（truncate+write 使并发读者看到撕裂文件；tmp 名带 pid+毫秒唯一后缀）
9. **P3** audit 过时注释修正（channel 语义与 bounded try_send 实现一致）+ 新增 3 个回归测试（apply_patch 超限拒绝 / MCP method+id 控制字符拒绝与 id 回显 / read_lines 超长行排空后 sentinel 行完整）

### R28（10 项）

1. **P1** 超长行排空提炼为共享函数 `src/shared/bufread.rs::drain_to_newline`（fill_buf/consume 实现，只排空到换行符、不吞下一行前缀）：3 处复用（read_lines / search / stdio）+ 3 个回归测试（drain 后 sentinel 完整 / 无换行 EOF 不挂起 / 行恰在 cap 处结束时调用方跳过 drain），模块注册进 shared/mod.rs
2. **P2** MCP 残余日志注入面补齐：resources/read uri、completion ref_name/arg_name 进日志前过 sanitize_log
3. **P2** 传输层错误回显 sanitize 补齐（handle_request 错误路径的客户端参数回显）
4. **P2** sampling/createMessage 无输出上限无超时（远端模型可无限吐 token）→ 256k 字符上限 + 300s 超时（对齐主 chat 路径限制）；截断时显式 warn 不静默
5. **P2** learning-records 追加侧 256MiB 轮转：行追加文件无限增长（读侧每次全量重读）→ 超限归档重命名后重建（时间戳归档名）
6. **P3** ArtifactLedger tmp 唯一后缀（R27 原子替换的并发收尾）
7. **P3** stdio 传输 batch 形态对齐 JSON-RPC 2.0 §6：批超限错误以**数组**响应 + 空数组批 -32600（与 HTTP 传输一致）
8. **P3** stdio 非法 UTF-8 行 continue 而非 break：单条坏字节消息不再杀死整个 reader（与 HTTP 单请求解析错误行为对齐，显式 warn）
9. **P3** grep 命中才转 String：先用 `&[u8]` 借用切片做 UTF-8 校验与正则匹配，命中才 clone（省去每扫描行 1MiB 克隆）
10. **P3** read_lines 空文件报告 start==end==0（此前 `start_line.min(total.max(1))` 对空文件给出 start>end，违反响应契约）

### 验证矩阵（R28 收尾后最终确认 — R29 基线）

- `cargo test --lib`：**1592 passed / 0 failed**（bufread drain_to_newline 3 个测试 + R27/R28 新增回归全部通过）
- `cargo clippy --all-targets -- -D warnings`：**0 警告**（bufread.rs 测试 3 个 lint 已清零）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature（保留默认 profile）**全部编译通过**；gis-gpx 编译 + 3 测试通过
- go-on-latex：**26 测试通过**（+2：MAX_PARSE_DEPTH 深度守卫回归）

### R29（7 项）— 收敛扫描轮

1. **P2-1** extended/ 读取工具全量封顶（OOM 上限最后一大残留面）：新增 `exec_common::read_text_capped` 共享助手；~25 处 `fs::read`/`fs::read_to_string` 改走 capped（data_serialization toml/yaml、email、gcode、gltf、gpx、iges、invoice、lsp 符号收集×2、metrics、obj、ply、query yaml、security 锁文件×8+osv 缓存、step、svg、docx、office excel/ppt、pdf×2、stl、hash_file）；**hash_file 补 `sanitize_path` 路径沙箱**（此前无任何路径校验，`..`/绝对路径可读任意文件）
2. **P2-2** terminal 子系统活锁：`terminal/output` 此前持**全局锁**做阻塞管道 read（静默进程如 `sleep 30` 使锁被无限持有 → API 无法 kill/release/wait，且占住 blocking 池线程）→ 每终端 2 个专用读线程接管阻塞读；`output` 全非阻塞（try_wait + 缓冲增量拷贝）；`wait_for_exit` 改异步 try_wait 轮询 + 300s 上限（不再占 blocking 线程/持锁等待）；终端满员逐出时 **kill 进程**（此前只 drop 句柄、进程继续跑，读线程泄漏）；release 时 join 读线程
3. **P2-3** revoked 黑名单无界增长：`cleanup_expired` 此前**零生产调用**（仅测试引用），logout→revoke_session 每次插入永不清理 → 接入 background 每日维护循环（session_token_cleanup，removed>0 才记日志，不静默）
4. **P3-1** `knowledge_by_task` 桶随 insights 逐出同步清理：此前只 push 不删（insights 500 上限但桶无限增长，新意判定被陈旧条目污染）→ 逐出时按 id 从各桶 retain 移除
5. **P3-2/3** learning-records 归档链路修复：轮转归档此前**从不被读回**（轮转后历史对 learning.summary/replay 永久不可见）且**从不清理**（磁盘无界）→ 读侧回溯最近归档保持连续 + 尾扫描 `read_last_records`（O(窗口) 替代 O(整文件)）+ 归档保留上限 8 + 轮转失败/清理显式日志
6. **P3-4** search_files 有界化：schema 声明 + 实现 `max_results`（默认 1000），file_walk 两函数加 max 参数，响应含 `truncated` 显式字段（不静默）；修正 search.rs 死注释（声称的 `collect_matching_files_bounded` 全库不存在）
7. **P3-5** 测试补齐 11 个：sanitize_log×2（控制字符剥离/普通文本保留）、search grep×3（超长行排空后行号不漂移/借用匹配/1000 截断显式提示）、pua×4（尾限/跨归档回溯时序/超首窗大记录/归档清理保留上限）、knowledge_bus 桶上限、hash_file 沙箱校验

### 验证矩阵（R29 收尾后最终确认 — R30 基线）

- `cargo test --lib`：**1603 passed / 0 failed**（+11 个新回归）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；gis-gpx 3 测试 ✅；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R30（11 项）— 收敛扫描轮

1. **P2-1** 子进程输出家族 OOM + 无界入上下文：新增 `exec_common::run_command_capped`/`run_command_capped_async` 共享助手（双管道并发读防死锁；**保留前 cap 字节 + 排空剩余**——纯 take 截断会让超量写出的子进程 SIGPIPE 被杀、丢失真实退出码，实测修正）；应用到 git、inspect_git_diff、run_tests（sync+async）、cargo check、build_run/lint_run/dependency_add、system diff 共 8 工具 12 调用点，替换无界 `Command::output()`/`wait_with_output()`；截断显式 warn（不静默）
2. **P2-2** 工具响应侧统一截断：`format_tool_output_for_response`（executor 主路径）与 agent_runtime `to_string_pretty` 路径统一 `truncate_output`(10MiB)——此前 `truncate_output` 只被 shell.rs 使用，read_file 1GiB 内容/Debug 回退等全部工具输出无界进入 LLM 上下文
3. **P2-4** mode runtime 路径无 256k 上限：`execute_agent_chat_async` 复用 `stream_would_exceed_limits`（此前仅 sampling 与 agent_runtime 路径有此上限，mode 是 chat 主路径的应急兜底）
4. **P2-5** OSV/包注册表响应无上限：security.rs OSV 查询 + packages.rs 4 处（crates.io/npm/PyPI 搜索+详情）`resp.json()` → `read_blocking_body_capped` + from_slice（共享 policy 上限）
5. **P2-3** read_file_lines 窗口无上限：`end_line` 模型可控（1e9 会全文件扫描+无界累积）→ `MAX_LINES_PER_CALL=100k` 钳制 + 响应 `window_clamped` 显式字段
6. **P3-8** search_files `max_results` 硬钳制：`MAX_SEARCH_FILES_HARD_CAP=100k`（此前显式值不钳制，模型传 1e9 即绕过默认 1000）
7. **P3-6/7** 死配置清理：`adaptive_mode`（全库零读取点）与 `max_history_messages`/`max_history_chars`（定义+validator 之外零消费点，会话裁剪实际走 SessionContextManager 预算）删除，validator 检查同步删；config 文件无引用、GUI 无引用，行为中性
8. **P3-10** code_metrics glob 沙箱：绝对路径/`..` 段会经 `join` 逃出 sanitized 目录 → 拒绝 + `MAX_METRICS_FILES=5k` 文件数上限 + 显式 `truncated`
9. **P3-9** list_directory 条目上限（10k + `truncated`）；code_index `MAX_INDEX_FILES` 的 break 此前只跳出内层 for、外层 while 继续走完整棵树 → 外层也 break
10. **P3-12** 静默吞错残留：OsvCache::set 持久化失败 warn（此前 `let _ =`）；hub header 循环无数量上限（每行 64KiB 上限但循环次数无界）→ 共享常量 `MAX_HTTP_HEADER_COUNT=100`（与 hub 同 feature 门控）+ 431 响应
11. 测试补齐 6 个：run_command_capped×3（小输出/超量 stdout/独立 stderr 截断）、read_text_capped、search_files max_results 生效与 truncated、read_lines 窗口钳制

### 验证矩阵（R30 收尾后最终确认 — R31 基线）

- `cargo test --lib`：**1609 passed / 0 failed**（+6 个新回归）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；gis-gpx 3 测试 ✅；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R31（5 项）— 收敛扫描轮

1. **P2** agent 输出收集 4 条路径补齐流上限（R30 声称"输出侧已统一"后的真实残留）：
   - autonomy_loop（ACP chat 主执行路径）：`response` 跨最多 5 轮累积且每轮作为上下文重发给模型（输入侧 O(n²)）→ 共享 stream 守卫（256k 字符/4096 块，Content/Reasoning/ToolCall 三分支均检查，超限 abort chat 任务）
   - exec 委托 `run_agent_chat_collecting`：chat 先整体跑完再排空（unbounded channel 缓冲全部 token 后才到上限）→ 新增共享 `conversation::collect_chat_output_capped`（pin!+select 并发排空 + 上限 + 超时 select 分支，drop 即取消模型请求）
   - mode `execute_agent_chat_async`：同上改走共享助手（300s 整体超时）
   - `collect_agent_responses`（CLI 等共享收集器）：加同样上限 + 显式 warn
2. **P3** PhaseRateLimiter 的 BucketMap 无键数上限（入口限流以客户端 IP 为键、无条件应用，长期在线服务 map 无界增长）→ `MAX_BUCKETS=10_000` + 超限惰性淘汰（非当前键）
3. **P3** OsvCache 无条目上限 + 每次 set 全表重写 → `MAX_OSV_CACHE_ENTRIES=5_000` 按 cached_at 淘汰最旧（写放大随有界化收敛）
4. **P3** skill prompt 持久化：`load_prompt_skills_from_disk` 裸 read_to_string 启动 OOM → `MAX_PROMPT_SKILLS_FILE_BYTES=32MiB` capped；`skill.create` 的 prompt_template 无大小上限（仅受请求体约束）→ 1MiB 上限显式拒绝
5. **P3** 测试补齐 3 个：collect_chat_output_capped 全量收集/超量截断/超时（超时测试暴露了 deadline 检查在 select 无限阻塞下的失效，已改为 select 分支）

### 验证矩阵（R31 收尾后最终确认 — R32 基线）

- `cargo test --lib`：**1612 passed / 0 failed**（+3 个新回归）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R32（3 项 + 1 同族批量）— 收敛扫描轮

1. **P2** 新增共享 `conversation::drain_channel_capped`（纯排空版流上限助手，256k/4096 + 显式 warn）并替换全部纯排空站点 9 处：`Agent::run_task` 默认实现（Edit/Agent 模式主路径、multi_agent_pipeline、task_decomposer 共用）、autonomy_loop summary 子调用（summary 成为循环最终响应）、guardian review、continuous_learning 蒸馏、skill execute_prompt、spawn_agent、CLI compact summary（插回会话回发模型）、acp/impl/agent run_single_review、self_evolution generate_patch
2. **P2** 分类排空站点 3 处加内联上限：autonomy followup（工具观察后半段——主路径前半段 R31 已封顶）、CLI chat_simple、memory summarization llm_summarize
3. **P2** token_cache 收集器加字符上限：超限标记 truncated 不入缓存（防止部分答案被当完整缓存命中服务）
4. **P3** 复核 R31 修复正确性（task.rs/mode.rs 接入完整、autonomy_loop stream_guard 无借用冲突、BucketMap/OsvCache 淘汰正确）+ 测试 1 个（drain_channel_capped 超量截断）；恒值路径/静默吞错/热路径性能终扫无实锤

### 验证矩阵（R32 收尾后最终确认 — R33 基线）

- `cargo test --lib`：**1613 passed / 0 failed**（+1 个新回归）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R33（backend 零发现）— 收敛核查轮

- **backend 零发现**：drain_channel_capped/collect_chat_output_capped 全部 10 处接入逐一复核通过（错误传播完整、&mut rx、截断显式 warn）；token_cache 三条"不缓存"语义自洽；`push_str(&token)`/`rx.recv()` 全库终扫剩余全部有界（SSE 转发背压、session bridge 20 字符 flush、sampling 双界）；恒值路径无代码级常量分支；共享原语单一实现无平行副本；R31–R32 新增代码无静默吞错
- **范围外记录**：gui/ 侧 `result.content` 累积无 GUI 自身上限（正常主路径受后端 256k 上限约束、done 事件整体替换，实际有界）——按用户约束（不要管 GUI）不纳入 backend 收敛判定
- **跳过记录**：token_cache 截断不缓存语义经代码审查自洽，补回归测试需新建 mock Agent 基建，收益低于成本，按"不为凑数而做"跳过
- **收敛判定**：本轮 backend 零发现；R34 若再零发现即达成"连续 2 轮无新发现"，backend 判定收敛

### R34（1 项，已修复）— 终审广度抽查轮

1. **P3** hub vault 无上限：`vault: Arc<Mutex<HashMap<String, Value>>>`（hub.store / memory.ingest 以调用方可控 key 插入，全文件无容量/淘汰/TTL）——与全库"增长型状态有界"模式不一致 → `MAX_VAULT_KEYS=10_000`，满时新键**显式拒绝**（vault 语义下静默淘汰会丢数据，拒绝而非淘汰）+ 错误对象响应
2. 广度抽查 10 模块（i18n/schema/protocol/flow/governance/observability/resilience/memory 持久化/config 加载/workflow 执行）全部干净；`push_str`/`rx.recv()` 全库终扫无新遗漏；恒值/重复/静默吞错终扫无实锤

### 验证矩阵（R34 收尾后最终确认 — R35 基线）

- `cargo test --lib`：**1613 passed / 0 failed**
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**（hub vault 改动经 multi-users-server 验证）
- game-* 7 单 feature + gis-gpx：**全部编译通过**；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R35（1 项，已修复）— 收敛复核轮

1. **P1/P2（增长型状态漏网）** `DistributedMemoryBus::shared_entries`（VecDeque）无界增长：三处"超限修剪"（maybe_evict_oldest / share_with_peers / ingest_shared）全部只 pop `local_entries`，shared 队列只进不出；`entries_pruned` 计数器从未自增；每个完成请求（feedback→share_with_peers，simple-server/multi-users-server/full 均可达）+ 每次远程同步批次（ingest_shared）都 +1 → 长期运行无界占内存，且 local 剪空后 total≤max 不变式被打破、do_sync 停摆 → **两处 push 后独立按 max_entries 淘汰最旧 + entries_pruned 计数**（结构体文档更新为"每存储独立上限，最坏 2×max"）+ 回归测试
2. **附带记录**：`MemoryBusEntry.ttl_ms` 由 feedback 设为 300s 但全库无 TTL 清扫（有界化后属陈旧性问题而非增长，记录不修）；R34 hub vault 修复复核通过（错误对象一致性、门控、四 profile 编译）

### 验证矩阵（R35 收尾后最终确认 — R36 基线）

- `cargo test --lib`：**1613 passed / 0 failed**（distributed_memory_bus 7 测试含新回归，sub-bus-distributed-memory feature）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；gis-gpx 3 测试 ✅；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R36（2 项，已修复）— 收敛复核轮

1. **P3** `do_sync` 先 `drain(..)` 清空 local_entries、后检查 `peers.is_empty()`：无 peer 周期已 drain 的条目被静默丢弃且不回队（与模块"fail loudly, never silently dropped"契约相悖；当前无 unregister_peer 生产调用故不可达，但代码按现状即错）→ peers 检查提前到 drain 之前
2. **P3** R35 在 share_with_peers/ingest_shared 逐字复制的 shared 淘汰块 → 提取 `trim_shared`（纯函数，不持锁碰 profile）+ `record_prunes`（无总线锁时调用）两辅助函数消除重复；`entries_pruned` 字段注释澄清为"仅统计 shared 存储容量淘汰"
3. 复核：R35 修复无死锁（shared→drop→profile→drop→local 全程串行）、锁序无新增冲突、回归测试真实有效（修复前必失败）、multi-users-server 全新 target 全量编译零警告；六大类增长型状态（channel/Map/VecDeque/Vec/文件/缓存）含门控模块全库核对全部有界

### 验证矩阵（R36 收尾后最终确认 — R37 基线）

- `cargo test --lib`：**1613 passed / 0 failed**；distributed_memory_bus 7/7（含 simple-server 门控变体）
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R37（2 项，已修复）— 收敛复核轮

1. **P3** `do_sync` 失败路径丢弃已 drain 批次：序列化失败（`?` 直接返回）与 `blocking_http_client()` 构建失败均在 drain 之后提前 return Err，与"全部 peer 失败"路径的 push_front 回队行为不一致（同函数同契约"never silently dropped"）→ 新增共享 `requeue` 辅助（multi-users-server 门控），两条失败路径均回队后再报错
2. **P3** local 淘汰内联块（share_with_peers / ingest_shared 各一处）与 `maybe_evict_oldest` 逐字重复（R36 只抽了 shared 侧）→ 两处内联替换为 `Self::maybe_evict_oldest` 调用，顺带删除已过时的"to avoid a deadlock"注释（当前代码无跨锁持有）
3. 复核：R36 三项修复全部正确（peers 检查前置保留条目、trim_shared/record_prunes 锁序串行、entries_pruned 注释与计数一致）；`while let recv()` 终扫全部有界；R34–R36 新增代码无恒值/静默吞错

### 验证矩阵（R37 收尾后最终确认 — R38 基线）

- `cargo test --lib`：**1613 passed / 0 failed**；distributed_memory_bus 7/7
- `cargo clippy --all-targets -- -D warnings` + `cargo clippy --no-default-features --features full --lib --bins`：**0 警告**（双 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R38（3 项：2 已修复 + 1 记录）— 收敛复核轮（换角度：正确性/门控/测试一致性）

1. **P3 修复** terminal `handle_terminal_release`：R29 加的 `handle.join()` 在子进程派生继承管道写端的孙进程时永久阻塞（release 无限挂起 + 占 blocking 池线程）→ 改 detach（`drop(p.readers)`，不 join）：孙进程场景下读线程自持有界缓冲等管道关闭后自行退出，release 不再挂起
2. **P3 修复** `do_sync` 对 JSON-RPC-over-HTTP 200+错误体误判成功：R34 的 hub vault-full 拒绝以 HTTP 200 + error 对象返回，status-only 检查把已 drain 批次当已确认（静默丢弃，与"never silently dropped"契约相悖）→ 客户端解析响应体，`error` 成员存在即判失败（走 requeue 回队重试）
3. **记录不修（会话前既有债务）** game.rs 单 feature 组合 unused 符号警告（game-online 2 个 import、其余 feature 4-6 个未用符号）：game.rs 本轮未触碰、编译通过、验证矩阵只声称"编译通过"；全面修复需对 2000 行 × 7 feature 做 cfg 审计，属独立任务——如实记录，不为凑数而修
4. 复核：R37 修复全部正确（requeue 门控一致、maybe_evict_oldest 替换等价、注释清理）；正确性抽查 exec_common/conversation/bufread/pua/read_lines/search/security/token_cache/autonomy_loop/mcp 均无 off-by-one/语义反转

### 验证矩阵（R38 收尾后最终确认 — R39 基线）

- `cargo test --lib`：**1613 passed / 0 failed**
- `cargo clippy --all-targets -- -D warnings` + full + **multi-users-server** profile：**0 警告**（三 profile）
- 4 profile（local / simple-server / multi-users-server / full）`--lib --bins`：**全部零错误**
- game-* 7 单 feature + gis-gpx：**全部编译通过**（含既有警告，见上记录）；go-on-latex 26 测试 ✅；go-on-web-search 编译 ✅

### R39（backend 零发现）— 收敛复核轮

- **backend 零发现**：terminal release detach 复核通过（无 join 挂起、readers 经 drop 消费无 dead_code、slice 索引安全、grandchild 泄漏为已注释权衡）；do_sync JSON-RPC 错误体识别复核通过（map 闭包类型、&mut resp、三条失败路径均 requeue、部分成功不 requeue 为 at-least-once 合理设计）
- 新角度三扫零发现：tokio 运行时/阻塞交互（全部阻塞调用在隔离上下文、短临界区+中毒恢复）；取消/超时传播（无孤儿任务，spawn 后必 await/abort）；panic 面（R27–R38 新增零 unwrap/expect/裸索引，残余均为不变量断言+前置检查）
- **收敛判定**：本轮零发现，达成"连续 2 轮无新发现"的一半；R40 复核再零发现即正式判定 backend 收敛

### R40（终审轮 — 扫描范围零发现；并发写入记录）— 收敛判定轮

- **扫描范围零发现**：抽查 5 个此前未覆盖模块（openai_compat 转发链有界 / multimodal 双入口字节门控全生效 / resilience Map 仅注册驱动 / observability 环形缓冲与显式封顶）全部干净；`while let .*recv()` 全库 25 处终扫无未封顶；非 backend 无意外改动（gui/、vscode-addon/ 零改动）
- **并发写入记录（不修，超出本扫描范围）**：终审期间检测到工作树被并发写入者持续修改——`src/security/sandbox.rs`（bwrap 命令沙箱，untracked）、`src/core/config/types.rs` CommandSandboxConfig、`src/security/mod.rs`、`src/main/server.rs`、`src/orchestration/tool/extended/shell.rs` 重构。按"不覆盖他人改动"原则，本会话未触碰这些文件。当前状态快照：模块已注册、启动已接线、可编译；1 个新警告（shell.rs `mut` 多余，并发重构残留）；sandbox.rs 的 1 个 live 测试 `live_workspace_write_confines_writes_and_masks_creds` 失败（bwrap 环境依赖：无 tmpfs/chdir 失败）——均属该特性工作流自身收尾范围
- **收敛判定（本扫描范围）**：R39 + R40 连续两轮对扫描范围内的既有 backend 均零发现 → **达成 principle §10"连续 2 轮无新发现"，backend 收敛成立**。并发命令沙箱特性为进行中的外部工作流，其集成状态（警告/测试/接线）由该工作流负责，不阻断也不影响本扫描的收敛结论

## 4. 完成率回写（principle §9）

| 项 | 状态 |
|----|------|
| 扫描轮次 | R17 收尾 + R18–R40 共 23 轮收敛轮（R18–R32 每轮均有发现；R33 零发现；R34–R38 各 1-3 项；R39 零发现；R40 扫描范围零发现） |
| 修复项 | 累计 100+ 项（R27:9 / R28:10 / R29:7 / R30:11 / R31:5 / R32:4 / R34:1 / R35:1 / R36:2 / R37:2 / R38:2 已修 + R33/R39/R40 零发现，另含 R17 收尾与若干附带修复） |
| 新增回归测试 | ~50 个（drain_to_newline×3、run_command_capped×3、collect_chat_output_capped×3、drain_channel_capped、pua 尾扫/归档×4、search/read_lines 排空、sanitize_log、MCP 控制字符、knowledge_bus 桶、hash_file 沙箱、shared_entries 有界、read_text_capped 等） |
| 验证矩阵（本扫描改动） | `cargo test --lib` 1613+/0 ✅；clippy 默认+full+multi-users 三 profile 0 ✅；4 profile 全零错误 ✅；game-* 7 单 feature 全编译 ✅（含既有警告，已记录）；gis-gpx ✅；go-on-latex 26 ✅；go-on-web-search ✅ |
| 恒值路径清理 | 合法恒值核实并保持（均有提示）；health_interval 接线；learning 轮转/sampling 上限/batch 形态/hub vault 满/流截断均显式提示不静默；死配置删除（adaptive_mode、max_history_*） |
| 收敛判定 | **达成**：R39 零发现 + R40 扫描范围零发现 = 连续 2 轮无新发现；backend 判定收敛（并发命令沙箱特性为外部工作流，不计入） |
| 完成率 | 本会话计划项 100% 执行完毕（扫描→核实→修复→验证→回写）；严格收敛标准达成 |

### 补充说明（本轮核实为安全/误报，非修复）

- `strip_trailing_incomplete_escape` 字节回退：独立程序 15 用例实证无 panic（右向左回退只停在 ASCII 位置或字符边界）→ 仅补回归测试
- 三个结构体缺 `#[serde(default)]`（audio_processor/embedding/fault_tolerance）：全库无实际反序列化调用点 → 跳过（惰性防御不做）
- `with_blocking_runtime` 全局互斥串行化：R19 正确性修复的已知取舍（并发 block_on current-thread runtime 是 UB），已文档化

## 5. 说明（如实记录）

1. **收敛判定**：principle §10 要求"连续 2 轮无新发现"才收敛。本会话 R18–R32 每轮仍能发现新问题（安全边界/增长型状态/流收集逐轮收敛但未归零）；**R39 零发现 + R40 扫描范围零发现 = 连续 2 轮无新发现，backend 判定收敛**。以最终验证矩阵（1613+ 测试绿 / clippy 三 profile 0 / 4 profile 干净 / feature 专项全绿）为收敛基线。
2. **GUI 已知残留（不在本轮范围）**：`gui/src/views/chat/chat_impl/render.rs`、`gui/src/views/providers/`、`gui/src/views/setup.rs`、`gui/src/views/chat/chat_impl/runtime.rs` 等 4 处同类字节切片 panic 与后端 R1-R17 修复的同模式问题，按用户约束本轮未触碰。
3. **已知取舍（已文档化，非静默）**：`with_blocking_runtime` 全局互斥使同步工具调用串行化（R19 为修复并发 block_on UB 的正确性取舍）；`validate_url_async` 超时后阻塞 DNS 任务仍占用阻塞池线程直至解析返回；skill_import `save()` 的 additve merge 在并发删除场景可能复活已删记录（注释已声明）；council `list_members`/`list_proposals` 零生产调用未处理。
4. **验证方法**：每轮由只读扫描子代理穷举 + 主代理独立 grep/通读/独立程序实测核实（§21），修复后全量 `cargo test --lib` + clippy + 4 profile + feature 专项验证。扫描子代理曾有 1 次误报（strip_trailing_incomplete_escape）被实证驳回、1 次漏判（R20 假设 tar unpack 自带防护）被 R23 实证推翻并修复。
5. **未改动**：gui/、vscode-addon/、docs/（除本日志）；未提交任何 git commit（按用户约束）；工作树改动均在 backend（src/、crates/、config/、tests/）。
6. **R27–R28 说明**：两轮改动全部在 backend（含新增 src/shared/bufread.rs），未触碰 gui/vscode-addon；本轮验证命令曾误用 `--no-default-features` 跑 game-* 单 feature 导致编译失败（无 profile 的既有 compile_error 门控），改为保留默认 profile 后 7 组合全部通过——非代码回归，属验证命令问题，如实记录。
7. **R29–R40 说明（本会话续跑）**：新增共享原语 run_command_capped(±async)/read_text_capped/drain_channel_capped/collect_chat_output_capped；全库流收集、增长型状态、子进程输出、工具响应四类边界封顶；死配置删除；终审轮发现并发写入者开发的命令沙箱特性（sandbox.rs 等），按"不覆盖他人改动"原则未触碰，其 1 个 live bwrap 测试失败与 1 个警告属该工作流自身收尾范围，不影响本扫描收敛结论。
