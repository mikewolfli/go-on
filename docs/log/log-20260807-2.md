# Log 2026-08-07 #2 — 全项目多轮深度+广度扫描与统一架构精炼（第 2 轮总扫描）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则，对本项目反复多轮扫描，**直到连续 2 轮无新发现为止**。目标：

- 统一全功能架构、各链路精炼功能、统一重复功能（合并去重）
- 优化全项目执行效率、提高各节点执行速度和反应速度，改造可并行的串行工作流
- 以统一架构/平台/链路、加速精炼优化为目标，收益中/大时考虑重构
- 全方位考量，检查各链路精炼后功能完整、线路完整顺畅；收益中/大时补充接线激活
- 全面更新文档（README、CHANGELOG、cookbook）以匹配最新实际架构
- 所有改进实事求是，不幻想、不自吹；多轮反复核对重复冗余功能，合并去重

## 1. 项目规模（2026-08-07 实测）

- `src/`：444 个 Rust 文件 / 203,276 行
- 三端：gui（Rust Tauri）、sdk（rust + typescript + python）、vscode-addon（TS）
- crates×3：go-on-latex / go-on-web-search / mermaid-render
- tests/、contracts/、scripts/、docs/、cookbook/
- 上一轮 log-20260807-1.md 已做 9 轮扫描收敛（修复约 50 项），本轮在其成果之上继续深挖

## 2. 扫描计划

| 轮次 | 范围 | 状态 |
|------|------|------|
| 第 1 轮 | 6 并行子代理分区扫描（A: acp / B: orchestration+agents / C: governance+security+observability+fault_tolerance+resilience+hub / D: memory+intelligence+multimodal / E: core+shared+schema+i18n+cli+protocol+mcp+main / F: 三端+crates+scripts+tests+docs+cookbook+contracts） | 待开始 |
| 第 2 轮 | 修复 + 深度验证 | 待开始 |
| 第 3 轮 | 广度验证 + 文档一致性 | 待开始 |
| 第 4 轮 | 收敛确认（连续 2 轮无新发现） | 待开始 |

扫描主题（每区统一）：
1. 重复功能/冗余模块（跨文件重复实现、转发壳、重复常量/助手）
2. 性能瓶颈（串行可并行、重复计算、每次请求重建、同步阻塞、热路径 block_on）
3. 死代码（无调用者的 pub 函数、无生产者的指标/字段、恒零/恒值假指标）
4. 链路缺失（已实现未接线、幻影 RPC、未激活配置、三端契约不一致）
5. 假修复/文档与代码不符（principle §13/§15/§18）
6. 串行工作流可否并行化、节点反应速度

## 3. 执行进度

### 第 1 轮扫描（6 并行子代理分区）✅ 完成

项目规模实测：src/ 444 个 .rs 文件 / 203,276 行；gui + sdk×3 + vscode-addon 三端；crates×3。

各区回报要点：

**A 区（acp，重派后完成）**：`mcp.sampling.createMessage` 有 dispatch 但不在 ACP_METHODS（binary_search 漏检同类回归）；`review_gate_invalid_response_total` 恒 0（无写入方）；`risk_decision.escalation_enabled` 语义错配；tool_loop.governance 三字段恒 0 + 合成 budget_remaining_pct；governance.config.save 声称 applied 实际只打日志；execute.rs 重复计算同集合 2-3 次；fallback.rs feature 无关 String 分配。

**B 区（orchestration+agents）**：spawn_agent 双路径 100% 必失败（ExecutionGovernor parent-path 空树检查 + CLI 未初始化 registry/budget）；self-evolution 影子目录断裂（apply 读不存在目录恒失败 + verify 验证的是未被打补丁的真实工程 + history 双重嵌套路径）；council 假投票（所有成员投同一预选票 + record_vote_accuracy 恒准）；evolution 假审批（RequireApproval 与 AutoApproval 行为一致）；chat_phases 手写重复 TaskRouter 分类；evolution_loop blocking_lock；消息子系统只写不读；communication/lifecycle 全模块无生产读写方。

**C 区（governance/security/observability/fault_tolerance/resilience/hub）**：LifecycleState.healthy 恒 true（/health 观测不真实）；content_safety 整模块生产零构造（~450 行）；evaluator.injection_detector 恒 None（死分支）；GovernancePolicy.red_lines 只写不读；evaluate() 步骤 1/2 恒 pass 假门；HarnessBus::enforce_action 零调用；drift 告警未进 AlertManager；内存告警仅启动评估一次；fault_tolerance 伪接线（evolve 一次性节点必 Offline + 每 30s 失败风暴）；audit 计数口径不一致。

**D 区（memory/intelligence/multimodal）**：chat.rs memory_promotion 一次性丢弃假指标；feedback quality_score 恒 1.0；SensingOutput 三死字段（decide 不消费）；decide semantic matcher 仅打日志；DistributedMemoryBus 收端断链（hub memory.ingest 只存 vault）；MemoryPersistence 三层纯写；ResponseCache/WarmStore 每次 put O(n) 全表排序驱逐；AudioProcessor 每次转写重建 HTTP client + diarization 双倍转写；optimization 成本推荐恒值 claude-haiku；ConfigDrift 触发器 expected==actual 恒等；excel_processor 恒等分支；workflow_optimizer 模块生产零消费者。

**E 区（core/shared/protocol/cli/mcp/schema/i18n/main）**：RUNTIME_CONFIG 只写不读（config.reload 假热更新）；dispatch_server 不转发 app_config（启动 config 双解析）；config.reload 的 runtime_config 热更新未真正生效；governance.remediate config 分支假"reloaded"；HTTP client 8+ 份独立构造未统一；start_server/handle_validation_mode 死分支（status/diagnose 不可达）；两套配置验证层并存。

**F 区（三端/crates/tests/docs）**：三端契约 8 项（Rust/Python SDK 缺 session 系列、Rust/TS 缺 tools.list/call、三份 SDK checkpoint.list/conversation.rollback 缺必填 conversation_id、TS sessionSetConfigOption optionId→configId 参数断裂、sessionNew workDirs→work_dirs）；README 技能数字过时；cookbook/book 陈旧产物入库 + .gitignore 路径失配；sdk/typescript/node_modules 627 文件被跟踪。

### 第 1 轮修复批次 ✅ 已执行（详见下方验证）

**批次 1（三端契约）✅ tsc/cargo check/py_compile 通过**：SDK 子代理修复 8 项契约（session 系列×7、tools.list/call、checkpoint.list/rollback 补 conversation_id、optionId→configId、workDirs→work_dirs、响应形态收敛、8+7+5 契约测试），仅改 sdk/ 目录，TS 20/20 测试、Rust 11 测试、Python 20/20 全绿。

**批次 2（ACP/协议/语义）✅ 编译零警告**：ACP_METHODS 补 `mcp.sampling.createMessage` + `memory.ingest`（字母序 + 排序不变量测试通过）；review_gate_invalid_response_total 接线（新增 inc 方法 + Err 分支调用）；risk_decision enabled/used 语义分离；tool_loop.governance 接真实计数（单次快照）；governance.config.save 诚实化（acknowledged+applied:[]）；RUNTIME_CONFIG 假通道删除 + config.reload 响应如实；dispatch_server 转发 app_config；memory.ingest 收端接线到 DMB.ingest_shared。

**批次 3（governance/observability）✅ 编译零警告**：LifecycleState.set_healthy + 30s 循环按 open_circuits 更新；content_safety 模块删除（~585 行 + evaluator 双分支）；evaluator.injection_detector 恒 None 字段+分支删除；evaluate() 假门步骤 1/2 删除；GovernancePolicy.red_lines 接入 check_tool_call 真实拦截；HarnessBus::enforce_action 删除；drift 告警接入 AlertManager.report_direct；evaluate_memory_alerts 接入 30s 循环；fault_tolerance 伪接线移除（evolve_fault_tolerance 删除 + 空状态跳过持久化）；audit 计数统一走 self.audit()。

**批次 4（orchestration 链路）✅ 编译零警告**：spawn_agent 双路径修复（check_limits 前幂等注册 spawn 父节点 + CLI 三处 init）；council 假投票诚实化（reputation 排序 + 幂等 add_member + proposal 清理重试）；chat_phases 复用 TaskRouter::analyze_task；evolution RequireApproval 诚实拒绝 + 拒绝不杀循环（continue）；evolution workdir 对齐真实工程根 + history 双重嵌套修复；blocking_lock→lock().await；self-evolution 死变体删除；forker with_kv_cache_provider 删除（quick_fork 保留）。

**批次 5（memory/intelligence/性能）✅ 编译零警告**：memory_promotion 写入真实 store + auto_migrate；SensingOutput 三死字段删除（sense/decide/core 同步）；suggest_cheaper_agent 恒值修复 + workflow_optimizer 模块删除（223 行）；ConfigDrift 假信号删除；video analyze_scene confidence 真实启发式；cache SQLite/PG put 先 COUNT 超限才驱逐；audio/embedding/voter/DMB HTTP client 统一到 shared 单例；alert webhook timeout 接线；prometheus max(1) 删除；PerformanceMonitor 恒 0 cache 路径删除。

**验证基线（第 1 轮修复后）✅**：
- 4 profile（local/simple-server/multi-users-server/full）+ default + workspace 编译零警告
- clippy -D warnings 零警告
- lib 1574 测试全绿
- GUI cargo check 零警告；Rust SDK cargo check 零警告

### 第 2 轮验证（验证子代理核查 28 项修复）✅ 全部闭环

验证子代理独立核查 28 项修复：代码路径/调用链/类型/feature 门控全部核实一致，三 profile 编译通过，1574 lib 测试全绿。无阻断性遗漏。新发现 2 项已补修：
1. ✅ validator.rs optimization_modules 白名单删除已删模块 "workflow_optimizer"
2. ✅ RequireApproval 拒绝错误不再 `?` 杀整个进化循环（改 match + continue）

**遗留观察项（非阻塞）**：chat_tests 预设 compile_error 门控（既有设计）；config.reload 事件字段名 "runtime" 与 runtime_config_updated:false 并存（UI 刷新用，不冲突）。

### 待办（进行中）

- ⚠️ **集成测试 acp_runtime_rpc_integration：12 通过 / 15 失败**——需排查是否为本轮改动回归（此前该文件 27 测试全绿）
- 第 3 轮：广度扫描（三端 + crates + scripts + docs/cookbook 一致性）
- 第 4 轮：文档更新（README/CHANGELOG/cookbook）
- 第 5 轮：收敛确认（连续 2 轮无新发现）
