# 全链路扫描修复日志 (2026-07-23)

## 扫描范围
- 全项目 `src/`, `gui/src/`, `cli/`, `crates/`, `tests/`
- 5个并行代理深度扫描
- 依据 `docs/blueprints/principle.md` 规则

## 已发现并修复的问题

### P0 - 严重问题

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1 | **`session/request_permission` ACP请求格式与Zed不兼容** | `src/acp/server.rs:1692-1717` | 移除 `toolName`/`toolArgs`/`mode`/`riskScore` 冗余字段；Zed 只识别 `sessionId`/`message`/`options`/`timeoutSecs` |
| 2 | **审批超时300秒导致工具完全阻塞** | `src/orchestration/tool/executor.rs:468-502` | 超时从300s→15s；出错时返回 `true`(允许) 而非 `false`(拒绝) |
| 3 | **`extract_all_tokens` 流式tool_calls重复发送** | `src/agents/mod.rs:713-722` | 禁止流式过程中emit tool call token，改为只在 `finish_reason=="tool_calls"` 时一次性发出 |
| 4 | **Followup阶段缺少 tool_call token 过滤** | `src/cli/chat.rs:2495` | 添加 `parse_tool_call_token` 过滤器，防止工具调用 token 泄漏到 Markdown 渲染器 |
| 5 | **`__finish_reason__`/`__usage__` token 泄漏到响应文本** | 多个文件 | ACP路径(run_agent_collecting)和CLI路径(run_agent_streaming_phase/followup/chat_simple)均添加过滤 |

### P3 - 轻微问题

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 6 | 注释中 `300s` 与实际 15s 不符 | `executor.rs:493` | 更新注释 |
| 7 | `risk_score` 参数在 request_client_permission 中不再使用 | `server.rs:1670` | 加 `_` 前缀，消除编译警告 |

## 验证未发现的问题

| 类别 | 结果 |
|------|------|
| `block_in_place` + `block_on` 热路径 | ✅ 无违规（`try_current()`+fallback 模式，符合原则#24） |
| `Handle::current().block_on()` | ✅ 无实例 |
| `#[ignore]` 测试 | ✅ 无（仅1处文档注释） |
| `#[allow(dead_code)]` 生产代码 | ✅ 均带有 F-GAP 文档化原因 |
| `todo!()` / `unimplemented!()` | ✅ 仅测试字符串中 |
| 空函数体 | ✅ 仅文档化的默认 no-op |
| 测试全部通过 | ✅ 2066 passed, 0 failed, 0 ignored |

## 部署
- `go-on` backend: 22MB ✅ 已覆盖

## 验证回写
- `cargo test -p go-on --lib`: 2066 passed ✅
- `cargo build --release`: 零警告 ✅
- `cargo build --release -p go-on-gui-egui`: 零警告 ✅
