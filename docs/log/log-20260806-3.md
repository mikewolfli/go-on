# 第34轮 — 收敛复核 + 整链死链清理 (2026-08-06)

承接第33轮收敛复核结果（`94a56b78`），对全部发现逐项 grep 核实后实施。本轮主题：
**删除最后一条"设计好但从未接线"的治理死链（policy hot-reload），清空 PolicyEvaluator 全部恒 None/恒空扩展点，合并重复决策逻辑，统一重复默认值。**

## 一、处理明细

| # | 项 | 处置 | 依据 |
|---|----|------|------|
| 1 | **reloadable_policy.rs 整模块 + notify 依赖**（322 行） | **整体删除**：`ReloadablePolicy` trait 唯一 impl 是测试 `TestPolicy`；`PolicyReloader` 生产零实例化（grep 全仓核实）；`start_watching` 仅测试调用；`merge_reloadable_policies` 零调用；`HarnessBus::new`/`PolicyEvaluator::new` 的 `policy_reloader` 参数恒 `None`（两工厂 `default_harness_bus`/`config_aware_harness_bus` 均传 None）。连带：`governance/mod.rs` 删 `pub mod reloadable_policy`；**Cargo.toml 删 notify 依赖（含 macos_kqueue target override），Cargo.lock -78 行**（`tokio::sync::Notify` 完全无关，保留）；`background.rs` BLUE56-D01 10 行注释收缩为 3 行（保留 log-20260730-18 风险分析引用）；`status.rs` `governance_modules` 删 `"reloadable_policy": false` 键 | §11:未接线即删除。blue55/56 曾声称"PolicyReloader 接线完成"，实际从无生产接线——本轮如实收敛 |
| 2 | **PolicyEvaluator 恒 None/恒空扩展点字段**（-271 行） | **删除**：`approval_engine`/`drift_protection`/`approval_learner`（无 setter、无外部赋值，仅 `needs_reexamine` 死分支读取）；`policies` 映射 + `PolicyFn` 类型 + `register_policy`/`deregister_policy`/`merge_reloadable_policies`（零调用，映射恒空）；`protected_invariants` + `register_protected_invariant` + `protected_path_blocked` + `collect_string_values`（writer 零调用，热路径白拿一把读锁）；`needs_reexamine`（零调用）；`evaluate()` 删除 step-0 恒空表扫描（少一把热路径读锁） | §11/§13。**保留** `safety_checker` + `injection_detector`：server_builder.rs:248-254 注释明确为"designed extension point 故意不接线"（默认阈值会误伤合法工具调用，保守配置才允许接）——删除会破坏记录在案的设计决策，实事求是 |
| 3 | **锁表文档矛盾**（§18） | **修正为一致**：evaluator 锁表 9→**6**（删 reloader/policies 2 把 + policies 读写 2 项后为 rule_engine/budget/runtime_control/guard/security_governor×2）；`harness_bus/mod.rs` "7 locks"→"6 locks"（消除两处文档互相矛盾）；`default_security_policies` 文档删 PolicyReloader 引用 | 文档与代码一致 |
| 4 | **cache_strategy.rs 三套决策逻辑**（-87 行） | **合并为单决策路径**：`handle_hit`/`lookup_decision`/`should_serve_cache_hit`/`should_refuse_cache_hit` 行为等价（conf>0.95 && !exec → Hit；conf>0.95 && exec → Refused；else Miss），唯一外部入口是 `decide_from_entry`（chat_phases.rs:937）。合并后直接内联决策，**消除 f64→f32→f64 双重转换**（L2 余弦本就 f32，L1=1.0 恒 Hit）。测试改真实断言（Hit 携带 response 的断言保留） | §8:重复逻辑合并 |
| 5 | **transport_factory.rs cfg 镜像块**（-64 行） | **抽取 `downgrade_optional`**：`initialize_cache` 与 `initialize_vector_store` 的 non-postgres 尾部镜像块（块顺序相反、config_path allow 处理不同）统一为共享 helper（local-only 降级 None+警告；server profile 透传错误），`cfg(not(backend-postgres))` 门控；`cfg_attr` 与函数级 `#[allow(unused_variables)]` 两种风格统一为后者 + 命名参数 | §8:镜像块抽取 |
| 6 | **TaskBudget 默认值三处复制** | **单一权威**：`hardening.rs` 新增 `impl Default for TaskBudget`（120_000/3600/256/256）；`types.rs` `ExecutionPolicy::default()`、`default_harness_bus`、`config_aware_harness_bus` 三处字面量全部改用 `TaskBudget::default()`（worker_factor=1 分支等价）；`ExecutionPolicy` 手动 Default 转 `#[derive(Default)]`（clippy derivable_impls 提示）。**复核不改**：`TimeoutPolicy.default_timeout=120s` 与 `AgentExecutionPolicy.timeout=120s`、`DispatchPolicy.max_retries=2` 与 `AgentExecutionPolicy.max_retries=2` 为不同域语义（调度 vs 单代理）的巧合相等，强行共享常量会耦合无关策略 | §8:值去重；巧合相等不耦合 |
| 7 | **get_agent_policy 冗余分支** | **合并**：`else if is_security { default_timeout } else { default_timeout }` 同值两分支收敛为单 else；`is_security` 仍用于 review_level/max_tool_calls/audit_level | 冗余分支清理 |
| 8 | **video_processor.rs 错误变体错用** | **修正 7 处**：帧提取路径（`extract_frames` 回退、`probe_duration` ×2、`extract_frames_via_ffmpeg` ×4）误用 `AudioExtractionFailed` → 全部改 `FrameExtractionFailed`（该变体此前无人使用）；同步修正测试匹配臂。音频路径 `extract_audio` 保留原变体 | §13:变体语义修正 |
| 9 | **`optimization_modules` 白名单**（复核项） | **复核保留，不改**：`workflow_optimizer`/`cost_optimizer` 指向真实存在的模块（`orchestration/workflow_optimizer.rs` 被 `optimization_bus.rs` 引用、`optimization/cost_optimizer.rs` 的 `CostEstimator` 被 OptimizationBus 使用）；白名单是配置防错门，删除合法名会让引用它们的配置校验失败。`optimization_modules` 值无运行时消费方是历史遗留的独立问题，不属本轮死代码 | 实事求是:删白名单是行为回归 |

## 二、环境问题修复（非产品代码）

- `e2e_governance_audit_verify_returns_chain_shape` 失败：`~/.goon/audit_chain.ndjson` 第 878 行是**撕裂写入**（一行内两个 JSON 对象，列 568 处 extra data），来自此前被 kill 的测试进程。
- **基线同失败**（`git stash` 验证，非本轮引入）。`HashChainAuditor` 对 tamper-evident 链硬报错是正确安全行为，未改产品代码。
- 处置：残留文件改名备份 `~/.goon/audit_chain.ndjson.bak-round34`（可逆），测试回到预期"fresh"态。

## 三、验证证据(原则 §16)

```
=== 编译 ===
cargo check 4 profile(local/simple-server/multi-users-server/full) → 零警告零错误
cargo check --workspace(gui/sdk/crates)                            → 零警告
cargo clippy --all-targets -- -D warnings (4 profile)              → 零警告
=== 测试 ===
cargo test --all-targets → 全绿(20 个 test binary 全 ok, 0 failed)
   lib: 1667 passed / 0 failed
   e2e_integration: 8 passed / 0 failed(环境修复后)
=== 生成物 ===
scripts/gen-provider-catalog.py --check → 双输出 OK(37 providers)
=== 代码量 ===
14 files changed, +111 / -852(净 -741 行);删除 1 个源文件(src/governance/reloadable_policy.rs,322 行)
```

行为一致性说明:
- #1/#2 全部为零调用点删除(grep 全仓核实),生产路径行为不变;`evaluate()` 少 2 把读锁(恒空表扫描),热路径微加速。
- #4 决策阈值语义保持 f32 比较(与旧 `lookup_decision` 一致),L1 恒 1.0 Hit、未知层级恒 Miss,测试断言与真实行为逐字匹配。
- #6 默认值不变(120_000/3600/256/256),`config_aware_harness_bus` 在 worker_factor=1 下与 `TaskBudget::default()` 逐值相等。
- #8 帧提取错误变体语义修正,消息文本不变,仅变体名正确化。

## 四、完成率回写(原则 §9)

- 修复项:9 项(删除 3、合并/去重 4、修正 2)。
- 复核保留项:#9 optimization_modules 白名单(真实模块名,防错门,删除即回归)。
- 上轮日志计数澄清:第33轮日志写"删除 3 个源文件",git 实为 4 个(council quorum 2 个 + hub/client.rs + capability_bus/act.rs),均已含在 33 轮提交内,无残留。
- 遗留问题数:0 项(第34轮复核清单全部处置)。
- 收敛判据:4 profile + workspace + clippy 全目标零警告、全测试套件 0 failed、生成物一致。第35轮可继续但预计无新发现(死链已清零,剩余为低收益风格项)。
