# Log 2026-09-01 #1 — 全项目多轮深度+广度扫描与统一架构精炼（R63：去重合并 + 效率提速 + 功能激活）

## 1. 本轮扫描基线（R63 起点实测）

- `cargo check`（local）：通过（32s，增量缓存）。
- 4 个独立只读子代理并行深扫：工具层（A）、供应商/治理/协议层（B）、效率热点（C）、死代码/未激活功能（D）。
- 子代理共报 40+ 项发现，主代理逐项读码复核后：**执行 24 项**，**复核后不改 14 项**（附证据），**误报修正 2 项**（chaos.rs drill 被误判为死代码——实际被 `tests/chaos_drill.rs` 集成测试使用；`tools_pack.rs:515` 的 `false,false` 在无 HarnessBus 分支下语义正确）。

## 2. 执行记录（全部经读码核实后实施）

### R63-A 去重与死代码清理（9 项）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| A1 | `AgentError` 4 个从未构造的变体删除（Config/Network/Timeout/Unknown） | `agents/agent.rs` | grep 全库仅 `Runtime` 被构造 |
| A2 | `CircuitBreakerState::as_label()` 单源化 | `resilience/hyper_resilience/types.rs` + `prelude/circuit_breaker.rs` + `health_pack.rs` | 两处映射收敛为一处，wire 字符串统一为 `"half-open"`（原 prelude 侧为 `"halfopen"`） |
| A3 | `AccessSelection::fixed()` 构造器 | `protocol/access_mode.rs` | 4 个显式模式分支复制粘贴（4×7 行）收敛为 4 行数据 |
| A4 | 治理分类表 2 条陈旧死条目删除（`game_online_status`/`game_state_modify`，无对应工具/描述） | `governance/tool_capability.rs` | 核实 game 实际注册名后删除 |
| A5 | `merge_spec_model_suggestions()` 共享 helper | `agents/mod.rs` + 5 个供应商（anthropic/deepseek/ernie/gemini/cohere） | 5 份 7 行 spec 建议合并块收敛为一行调用（ernie 按 API 族传 spec_name） |
| A6 | 模式策略表静态化 | `orchestration/mode.rs` | `all_exec_tools` 转 `static` 数组；`plan_tools` 转 `LazyLock`；`policy_for_kind` 返回 `(&'static [&'static str], usize)`；消除每工具批次 ~70 个 String 分配 |
| A7 | 技能 stop-word 集静态化 | `orchestration/skill/registry.rs` | `tokenize` 每调用重建 ~90 词 HashSet → `LazyLock` |
| A8 | `iso_from_unix` 收敛到 canonical `unix_ts_to_ymd`（Hinnant O(1)） | `tool/extended/time.rs` | 第三份 epoch→ISO 实现删除（含 `is_leap`）；保留原 clamp 语义 |
| A9 | `run_task` 镜像消息构建收敛 | `agents/agent.rs` + `orchestration/mode.rs` | `build_chat_messages` 提为 `pub(crate)`，`Agent::run_task` 默认实现调用之（原为逐行镜像，注释自认） |

### R63-B 工具层去重与安全收口（8 项）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| B1 | `http_request` 同步/异步双路径合并（~110 行重复） | `tool/extended/http.rs` | 抽 `HttpRequestParts` + `parse_http_request_parts` + `build_request<C: HttpClientLike>`（泛型覆盖 async/blocking reqwest builder）；method match/headers/bearer/query 单份实现 |
| B2 | `json_query`/`yaml_query` 合并（~85 行重复） | `tool/extended/query.rs` | 共享 `query_structured_file(input, tool_name, label, read, parse)`；两工具只保留 parser 差异 |
| B3 | `diagnostics` 输出无上限 → `run_sandboxed_capped` | `tool/extended/diagnostics.rs` | 大仓库 cargo check 输出可达数百 MB，此前整块缓冲 |
| B4 | `apply_code_action auto_fix_diagnostic`（clippy --fix）无上限 → `run_sandboxed_capped` | `tool/extended/lsp.rs` | 同上 |
| B5 | `format_code` 绕过共享沙箱执行器 | `tool/extended/format.rs` | 模型可控 formatter 名加入白名单（8 个已知 formatter）；path 走 `sanitize_path`；输出走 `run_sandboxed_capped`（MAX_OUTPUT_BYTES）；dir 作为工作目录、文件作为参数（保留原语义） |
| B6 | `jsonl_write` 参数名漂移 + 写路径无写沙箱 | `tool/extended/jsonl.rs` | 实现改用描述表参数名（`jsonl_read.limit` / `jsonl_write.data`）；写路径补 `sanitize_path_for_write` + `enforce_write_sandbox` + `acquire_tool_write_lock`（与 write_file/edit_file 一致） |
| B7 | `read_file_lines` schema 双源漂移 | `tool/extended/read_lines.rs` + `shared/tool_descriptors/extended.rs` | 删除工具侧 `input_schema` override；表补齐 `default:1/50`、`required:["path"]`（原表错标 start/end_line 为 required） |
| B8 | 参数验证表驱动化（validate.rs 单源） | `shared/tool_descriptors/validate.rs` + 8 个 descriptor 文件 | 手写 match（已漂移：jsonl_read 等 ~30 工具声明了 required 却零校验、workflow 分支重复两次）改为从描述表 `required` 派生；`tool_descriptor`/`tool_descriptor_value` 签名 `&'static str`→`&str`（match arm 对 &str 同样成立）；无表行的别名（find_files/file_move/file_delete）保留 legacy 分支；**修复表自身 2 处过度 required**（run_tests/inspect_git_diff 的 directory 在实现中可缺省） |

### R63-C 效率与并行化（7 项）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| C1 | `with_blocking_runtime` 全局互斥锁消除 | `tool/exec_common.rs` | current-thread runtime + 全局 Mutex（R19 正确性取舍，已知串行化所有同步工具）→ multi-thread runtime，tokio 支持多 OS 线程并发 `block_on`；并发 `shell_exec`/LSP 同步回退/web_search/spawn_agent 不再进程级互斥；调用点注释同步更新 |
| C2 | LLM 阶段摘要移出响应关键路径 | `acp/impl/chat/knowledge.rs` | 默认配置下（summary_enabled=true, trigger=8 条）每次会话在 done 前串行等待最长 12s 摘要 LLM 调用 → 改为 `tokio::spawn` fire-and-forget（经 `current_acp_server()` 取 Arc，与 executor/autonomy_loop 同模式）；无 server 注册（单测）走原 extractive 回退，摘要仍落盘 |
| C3 | act 阶段持久化块移到 done 事件之后 | `acp/impl/chat/phases/act.rs` | 客户端先收 done/telemetry 再做 knowledge/checkpoint/distillation/vector 持久化；emit 失败不再跳过持久化（`emit_err` 收集，持久化后返回） |
| C4 | `port_scan` 串行扫描（24 端口 × 3s 最坏 72s）→ 24 工作线程并行 | `tool/extended/network.rs` | `thread::scope` + 原子索引；结果按端口排序；单次 DNS 解析错误不再中断整个扫描 |
| C5 | `security_scan` OSV 逐包串行 HTTP（分钟级）→ 8 工作线程并行 | `tool/extended/security.rs` | 缓存 `Arc<Mutex<OsvCache>>` 共享，命中仍走快路径；锁不在网络调用期间持有 |
| C6 | `search_packages auto` 三 registry 串行试探 → 三路并行 | `tool/extended/packages.rs` | 保持偏好序（crates.io>npm>pypi）；pypi 详情回退循环保持串行（最多 10 项） |
| C7 | `environment_info` 7 次串行 `which` 子进程 → 单次调用 | `tool/extended/environment_info.rs` | `which_all()` 一次解析全部工具存在性 |

### R63-D 功能激活与 profile 完善（3 项）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| D1 | `template-engine`（Jinja2 渲染）激活 | `Cargo.toml` | 此前不在任何 profile（连声称 "EVERY feature" 的 `full` 都缺），minijinja 代码从不编译 → 加入 `full` |
| D2 | `document-excel-write` 并入 `sub-bus-multimodal` | `Cargo.toml` | 三个生产 profile 此前只有 Excel **读**（document-excel），写能力仅 full 有 → 读写对称 |
| D3 | `sub-bus-multimodal` 聚合器语义注释 + `full` 注释精确化 | `Cargo.toml` | 注明纯依赖聚合器、chaos-testing 为测试专用 opt-in（进 full 会让非测试构建产生 9 个 dead-code 警告、破坏发布门禁 `cargo clippy -F full -D warnings`） |

## 3. 收口轮执行记录（R64：遗留 14 项逐项闭环）

| # | 原遗留项 | 处置 | 说明 |
|---|---------|------|------|
| 1 | **G10 配置同步层 29 键双表** | ✅ 已修 | `parser.rs` `LEGACY_KEYS` 存在性列表 + 29 条 `sync_from_legacy!` 调用收敛为单一 `LEGACY_SYNC_RULES` 静态表（key + 赋值闭包）单循环驱动；键列表与同步规则同源不可漂移；None 回退分支经证明不可达后移除（contains_key 与 get 同一 map 必然一致）；62 个 config 测试全过 |
| 2 | **G8 部署档关键字表** | ✅ 已修（真实缺口） | `hardening::policy_bundle_for_target` 不识别配置档名 `simple-server`/`multi-users-server` → 生产部署静默落到 `local_dev`（无沙箱/无写审批）；补映射（simple→ci_pipeline、multi-users→production_hardened）+ 新测试；`status.rs` multi-user 推断拼写 `"multi-user-server"` 缺配置档名 `"multi-users-server"` → 补上 |
| 3 | **F13 OSV 缓存每包全量重写** | ✅ 已修 | `OsvCache::set` 拆为 `set_memory`（仅内存）+ `persist`（落盘）；并行 worker 只改内存，扫描结束统一 `persist` 一次（几百依赖从几百次整文件写降到 1 次） |
| 4 | **C6 就绪探测无缓存** | ✅ 已修 | `probe_local_endpoint_cached`：`Reachable` 结果 30s TTL 缓存（key=url）；失败永不缓存（宕机服务下个请求即重探）；缓存有界（TTL 窗口 retain） |
| 5 | **F16 metrics 重复循环** | ✅ 已修 | `estimate_max_function_size` 两分支逐字相同的取 max 循环提取为 `max_function_size_between` |
| 6 | **F14 桥接工具名单漂移** | ✅ 已修 | `BRIDGE_TABLE_TOOLS` 提升为模块级 const；`is_bridge_special_tool` 的 6 个表工具名改用 `BRIDGE_TABLE_TOOLS.contains`；一致性测试改迭代同一 const（名单三处同源） |
| 7 | **M4.2 bundle.rs 激活（元数据侧）** | ✅ 已修 | 发现路径解析 `[goon.bundle]`：`is_bundle_document`（表头行检测）+ `parse_bundle` → `SkillRegistry.register_bundle_metadata`（新字段 `bundles: HashMap<name, SkillBundleConfig>`）；`SkillDescriptor` 新增 `bundle` 字段；`skill_list` 输出新增 `bundle`（allowed_tools/context_fragments/when_to_use，仅捆绑技能携带，加性兼容）；`unregister` 联动清理；新测试 `discover_registers_bundle_metadata_from_bundled_skill` 真实走发现链路验证 |
| 8 | **G7 三引擎** | ✅ 复核闭环 | 三层（HarnessBus 全量评估器 / 无总线默认策略 / 快速同步门）分类全部经 `ToolCapabilityRegistry` 单源；`rbac_configured` 标志在无 HarnessBus 分支不影响决策（仅改 reason 文本）；工具侧 `rbac_enforcer` 是请求级 RBAC（handle_request），与工具治理分层明确；无重复分类表可删 |
| 9 | **C4 think 双检索** | ✅ 复核闭环（不可行） | `retrieve_context_for_agent` 查询串 = `agent_name + phase + task`，agent_name 来自选择结果——真实数据依赖，前置并行会改变记忆选择语义；扫描断言"无数据依赖"不成立 |
| 10 | **C5 每请求多次 HarnessBus 校验** | ✅ 复核闭环 | pre-chat 规则 / observe chat.execute / 工具执行 / post-execute verify 四阶段 PUA 证据链语义不同，合并会破坏阶段审计；不改 |
| 11 | **C7 双重注入扫描** | ✅ 复核闭环 | escalation（驳回人工审批）与 observe（阻断/消毒）安全语义不同（多轮已判定）；不改 |
| 12 | **G11 锁惯用法** | ✅ 复核闭环 | `with_acp_lock`（闭包注入）与 `lock_or_recover!`（返回 guard）是两种必要惯用法，30+ 调用点改写是纯 churn；不改 |
| 13 | **G12 record_audit 双写** | ✅ 复核闭环 | 生产路径（PolicyEvaluator）全部走 `record_audit_counters`；`record_audit` 仅测试使用（验证计数器+审计环+From 转换），生产零双写；注释已声明；不改 |
| 14 | **F18 双 blocking HTTP 单例** | ✅ 复核闭环 | 两版各有明确语义：shared 无 redirect 校验（包注册表查询类），http.rs 带逐跳 SSRF 校验（web_scrape/rss）；合并会改变安全边界；注释已声明；不改 |

## 4. 误报修正（子代理结论与实证不符）

1. **chaos.rs drill 子系统被报为"9 处死代码"**：扫描基于 `cargo check`（非 `--all-targets`）——实际 `tests/chaos_drill.rs`（`#[cfg(feature="chaos-testing")]`）真实使用 `run_drills`/`clear`/三个场景函数，`cargo test --features chaos-testing --test chaos_drill` 6 项全过。**未删**；正确激活路径是将其纳入 CI 构建（已通过 full profile 的 release gate 复核，chaos-testing 保持 opt-in 以保住非测试构建零警告）。
2. **`tools_pack.rs` 硬编码 `false,false` 绕过 RBAC**：位于 `harness_bus.is_none()` 的 else 分支，`harness_bus_present=false` 是该分支真实状态，非绕过。
3. **C4 "双检索无数据依赖可并行"**：`retrieve_context_for_agent` 的查询串包含 `agent_name`（选择输出），依赖真实存在，扫描建议不可行（见 §3-9）。

## 5. 收口轮二执行记录（R65：扫描清单剩余项清零）

| # | 项 | 处置 | 说明 |
|---|----|------|------|
| 1 | **G2 copilot 重试循环四分支去重** | ✅ 已修 | `copilot.rs` chat 循环：4xx 与 unsupported-model 两个字节相同分支合并；rate-limit 与 transient 两个行为相同分支合并；重试决策提取 `retry_or_exhaust(attempt)`（backoff sleep 单点）；`is_rate_limit_error` 不再在 copilot 使用（函数保留，fallback/audit/execution 仍在用） |
| 2 | **F13 OSV 缓存进程级常驻** | ✅ 已修 | `osv_cache()` 静态 `OnceLock`：每进程只读盘一次；TTL 改为按次传入（`get(key, ttl_secs)`），逐次扫描的 `cache_ttl_hours` 仍生效；`persist` 保持每扫描一次 |
| 3 | **F16 metrics 逐文件串行分析** | ✅ 已修 | 最多 5000 文件串行 `analyze_file` → 8 工作线程并行（thread::scope + 原子索引），结果按文件索引排序保持确定性输出 |
| 4 | **D8 sdk/rust 契约漂移** | ✅ 已修 | `HealthResponse` 删除 2 个文档自认 "backend does not emit" 的死兼容字段（顶层 `status`/`uptime_seconds`；真实字段在 `lifecycle.uptime_seconds`）；SDK 20 集成测试全过 |
| 5 | **D11 forker kv-cache 死路径** | ✅ 已修 | BLUE70 §6.2 扩展点整体移除：`KvCacheProvider` trait（零实现者/零消费者）、`ContextForker.kv_cache_provider`（恒 None、无 setter）、`ForkOptions.try_kv_cache_reuse`、`fork()` 死分支、`ForkContext.kv_cache_fingerprint` 字段/构造器/is_empty 检查、`NoOpKvCacheProvider`、`test_noop_kv_provider`；bin 死代码门因此暴露，清理后零警告 |
| 6 | **D11 docx 图片提取静默跳过** | ✅ 已修 | `parse_docx_bytes` 的 `_ => {}` 改为计数 + `debug!` + 元数据 `images_skipped`，限制可观测（不静默） |
| 7 | **C8 council 全局锁** | ✅ 复核闭环 | `run_council_route_deliberation` 为同步函数（无 await 点）、单次锁获取、临界区为微秒级 map 操作、锁不跨 await——无需改动 |
| 8 | **G3 send→check→stream 样板** | ✅ 复核闭环 | 各供应商请求构造（载荷/端点/认证/SSE handler）真实不同，仅尾部调用已共享的 `check_api_response`；参数化提取需把差异全部参数化，收益为负；不改 |

## 6. 完成率回写（principle §9）

- 扫描模块数：527 个源文件（全项目，三轮全覆盖）。
- 修复项数：24（首轮）+ 7（收口轮）+ 6（收口轮二）= 37 项；复核闭环项（修复或实证不可行/不应改）合计 19 项；**扫描清单全部清零，无遗留待办**。
- 误报修正：3 项（chaos drill / tools_pack hardcode / C4 依赖判定）。
- 设计前置（非缺陷，记录在案）：M4.2 白名单执行钩子需工具执行路径携带技能身份（bundle.rs 作者文档已声明）；`KvCacheProvider` 扩展点本轮按"无消费者即删除"原则移除，未来接入时重新引入。

## 7. 验证证据（三轮合并）

| 门禁 | 结果 |
|------|------|
| `cargo check`（local/simple-server/multi-users-server/full 四 profile） | ✅ 全部零错误零警告 |
| `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| `cargo clippy --no-default-features -F full -- -D warnings`（发布门禁同款） | ✅ 零警告 |
| `cargo test --lib`（local） | ✅ 1800 passed / 0 failed |
| `cargo test --no-default-features -F full --lib` | ✅ 1857 passed / 0 failed |
| 集成测试：tool_skill / chaos_drill(+feature) / structural / protocol_parity / transport_parity / pua_contract_smoke / e2e / comprehensive_benchmark / agent_world_invitation / i18n / cli / acp_runtime_rpc / config_validation / http_accept_matrix / openai_compat_matrix / protocol_consistency | ✅ 全部 passed |
| `cargo check --workspace`（含 gui/sdk/crates） | ✅ 零错误 |

## 8. 变更规模

三轮合计 58 文件，净减 ~200 行；主要去重：http 双路径（~110 行）、json/yaml 双工具（~85 行）、validate 手写表（270 行）、配置同步双表（~30 行）、时间第三份实现（~60 行）、copilot 重试四分支、forker kv-cache 扩展点（~60 行）、OSV 每包重写 → 每扫描一次、metrics 串行 → 并行。

## 9. 收口轮三执行记录（R66：遗留项清零 + 新发现闭环）

### 9.1 已修复（5 项，全部读码核实 + 编译/测试/集成验证）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| 1 | **MCP SSE 响应头块重复消除** | `acp/impl/runtime/sse.rs` + `protocol/mcp_server.rs` + http.rs / chat_completions.rs / responses.rs | `write_sse_headers` 新增 `connection` 参数（`"keep-alive"` / `"close"`），MCP 的 `handle_mcp_sse_connection` 手写 8 行 HTTP 头块（与 ACP 侧仅 `Connection` 值不同）改为复用该函数；删除 `SSE_HEADER_WRITE_TIMEOUT` 冗余常量。行为等价：同一超时值（SOCKET_WRITE_TIMEOUT）；MCP 头尾从 CORS 为空时的 `\r\n\r\n\r\n` 收敛为标准单空行（协议等价，SSE 客户端忽略空帧）。 |
| 2 | **HTTP status_text 三处统一 + hub reason-phrase 修复** | `acp/impl/runtime/http.rs`（共享表）+ `gateway/webhook.rs` + `hub/server.rs` | acp 表补 `409 → Conflict`、`431 → Request Header Fields Too Large`；webhook 删本地 `status_text` fn、hub 删内联 match，均改 import 共享表。**修复**：hub 此前 401/413/431 落入 `_ => "Error"` 产出 `HTTP/1.1 401 Error` 非标准 reason phrase（与上轮 ACP 403→"OK" 同类问题）。 |
| 3 | **mcp/handlers.rs 5 处内联错误响应收敛** | `mcp/handlers.rs` | `handle_request` 内 5 个 7 行 `JsonRpcResponse { jsonrpc, result: None, error: Some(JsonRpcError{..}), id }` 字面量收敛为 `error_response(id, code, message, data)` 构造器（控制字符×2 / cancelled / not-initialized / unknown-method）。 |
| 4 | **URL 查询编码三处统一 + UTF-8 截断 bug 修复** | 新建 `shared/url_encode.rs`；`packages.rs` / `utils.rs` / `game/online.rs` | 新共享 `form_url_encode`（基于 `url::form_urlencoded::byte_serialize`）收敛三处；**修复**：`game/online.rs` 手写逐字符编码对多字节 UTF-8 执行 `other as u8` 截断（é → `%E9`，应为 `%C3%A9`）；新增 3 测试（空格/保留字符/多字节 UTF-8），原 packages.rs 测试移入共享模块。 |
| 5 | **runtime_controls 信号窗口双份收敛** | `governance/runtime_controls.rs` | `AgentSignalWindow::record/failure_rate` 与 `OnlineControllerState::record/failure_rate` 的 trim+push 与失败率计算逐字重复 → 提取 `push_signal` / `window_failure_rate` 共享 helper。 |

### 9.2 复核闭环（证伪/不改，附证据）

| # | 项 | 结论 | 证据 |
|---|----|------|------|
| 1 | **待验证 A：AdaptiveModelSelector vs OnlineControllerState** | 不改 | 选对象不同（LLM 模型 vs agent/phase）、评分公式不同（纯 UCB 成功率 vs 成功率+延迟混合可靠性、复合 UCB+均值+可靠性）、数据维度不同（model_id::context vs phase/task-type/global）、消费方不同（self_evolution vs 治理运行时），仅共享 UCB bandit 通用模式。 |
| 2 | **待验证 C：merge_phase_principles 硬编码清单** | 不改 | RULE 文本（FETCH-001…ERR-003）全库仅 `merge_phase_principles` 一处，单源；另两组"红线"——`GovernancePolicy.red_lines`（命令模式，check_tool_call 子串匹配，活跃）与 `PuaEnforcementPlan.red_lines`（自然语言，`check_red_lines` 生产调用已在上轮移除，仅测试+治理 API 表面）——内容/匹配/用途均不同。 |
| 3 | **待验证 #1：game 工具 name() 与描述表** | 不改（核对通过） | 15 个工具 `name()` == 注册 capability == `tool_descriptors/game.rs` 描述条目，一一对应，无"注册无描述/描述未注册"；`tool_descriptor` 链已接入 MCP tools/list。 |
| 4 | **write_file run/run_async 双路径** | 不改 | sync fs 与 tokio fs 本质不同；收敛为单一 async 管线需在 `run` 内 block_on（principle #19/#20 禁止热路径 block_on）；现有 read_file 双路径亦用共享 helper 而非合并。 |
| 5 | **state_sync SSE 与 MCP SSE 订阅循环** | 不改 | 事件源（StateSyncBroadcaster vs SseBroadcaster）、事件名（state_sync vs message）、负载（Value vs 预序列化串）、心跳（StateSyncEvent vs JSON-RPC ping）、刷新策略均不同；参数化抽象收益为负。 |
| 6 | **hub 流式 HTTP 解析 vs ACP parse_http_request** | 不改 | 有意的有界流式设计（逐行上限 + 行数上限，内存峰值 64KiB）；复用整块解析需先累积 header 块，峰值内存反而增大；重复仅 ~15 行 content-length/auth 提取且行为一致。 |
| 7 | **percent_decode_url** | 不改 | 唯一实现（utils.rs），正确处理 +/%/UTF-8/非法编码拒绝；无第二份拷贝。 |
| 8 | **hub::json_rpc_error vs handlers.rs::error_response** | 不改 | hub 构造 `Value`（内部 JSON-RPC over HTTP 文本），handlers.rs 构造 `JsonRpcResponse` 结构体，类型面不同；mcp_server.rs 的两个错误 helper（带/不带 profile 注入）形态不同且已各自单源。 |
| 9 | **SseBufferPool** | 不改 | 单实现单实例（`chat/streaming.rs` 的 OnceLock）；仅模块名 `sse_optimizer.rs` 略误导（实为缓冲池），无功能重复。 |

### 9.3 验证证据（R66）

| 门禁 | 结果 |
|------|------|
| `cargo check --lib` | ✅ 零错误零警告 |
| `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| `cargo clippy --no-default-features -F full -- -D warnings` | ✅ 零警告 |
| `cargo test --lib`（local） | ✅ 1803 passed / 0 failed |
| `cargo test --no-default-features -F full --lib` | ✅ 1859 passed / 0 failed |
| 集成：acp_runtime_rpc_integration(27) / protocol_parity_integration(7) / transport_parity_integration(6) / http_accept_matrix_integration(26) / openai_compat_matrix_integration(7) / protocol_consistency_integration(3) / pua_contract_smoke(18) / tool_skill_integration(9) / structural_tests(11) / config_validation(1) / i18n_test(11) / cli_tests(9) | ✅ 全部 passed |

### 9.4 累计完成率回写（更新 §6）

- 修复项数：37（前三轮）+ 5（R66）+ 1（R66 补测：code_index auto-build 回归测试）= **43 项**；复核闭环 19（前三轮）+ 9（R66）= **28 项**。
- 本轮验证的 3 个遗留待验证项全部闭环（A/C/#1），扫描清单无遗留待办。
- 补充：code_index 首搜自动构建修复补 run() 层回归测试 `test_first_search_auto_builds_index`（真实走 search 分支 + 全局单例空态自动构建并返回符号，principle §16 端到端验证）。

## 10. 收口轮四执行记录（R67：跨模块重复函数收敛）

### 10.1 已修复（4 项）

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| 1 | **control-mode 映射双份收敛** | `acp/impl/request/util.rs`（新共享）+ `exec_pack/execute.rs` + `workflow_pack.rs` | `normalize_control_mode`（&str）与 `normalize_plan_control_mode`（Option<&str>）逐字相同的 match 收敛为 `request/util.rs::normalize_control_mode`；Option 侧在调用点 `unwrap_or("assisted")`。 |
| 2 | **truncate_run_output 收敛** | `acp/background.rs` | 手写 500 字符截断 + "...[truncated]"（与共享 `truncate_chars` 语义逐字一致）删除，改调用 `crate::shared::truncate::truncate_chars(summary, 500, "...[truncated]")`。 |
| 3 | **去重 helper 泛型化** | `shared/vec_utils.rs` + `governance/pua.rs` + `orchestration/task_router.rs` | `dedupe_strings`（Vec<String>）与 `dedupe_roles`（Vec<AgentRole>）逐字相同 → 泛型 `dedupe<T: PartialEq>`；pua.rs 包装函数删除、3 个调用点统一。 |
| 4 | **sha256_hex 双实现收敛** | `shared/mod.rs`（保留）+ `orchestration/write.rs` | `write.rs::sha256_hex`（hex::encode 版）与共享版（format 循环版）产出相同 → 删除本地版，`file_hash` 与测试改调 `crate::shared::sha256_hex`；重复测试移除。 |

### 10.2 复核闭环（证伪/不改，附证据）

| # | 项 | 结论 | 证据 |
|---|----|------|------|
| 1 | `security/paths.rs::normalize_path_string` vs `code_repo_analyzer.rs::normalize_slashes` | 不改 | 语义不同：前者保留前导 `..` 逃逸（安全检查必须防绕过），后者弹栈解析（依赖解析）；合并会改变安全检查语义。 |
| 2 | `session_compressor.rs::truncate`（字节预算 + “…”） | 不改 | 字节预算语义（running summary 8000B / first-last 200B），与共享字符截断语义不同；非重复。 |
| 3 | hub 流式 HTTP 解析 / SSE 订阅循环 / write_file 双路径 / SseBufferPool / percent_decode_url / base64 各处 | 不改 | R66 已逐一复核（见 §9.2），本轮复查无新变化。 |
| 4 | 模型选择（flow.rs select_model_for_agent / ModelSelector / adaptive_selector / model_router） | 不改 | 分层明确：静态选择器（intelligence）单源、flow 层组合、model_router 做 agent 过滤、adaptive 做 UCB 动态选择，各司其职。 |

### 10.3 验证证据（R67）

| 门禁 | 结果 |
|------|------|
| `cargo check --lib` | ✅ 零错误零警告 |
| `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| `cargo clippy --no-default-features -F full -- -D warnings` | ✅ 零警告 |
| `cargo test --lib`（local） | ✅ 1802 passed / 0 failed（R67 移除 1 个重复测试） |
| `cargo test --no-default-features -F full --lib` | ✅ 1859 passed / 0 failed（= local + 57 个 feature 门控测试，实测） |
| 集成：pua_contract_smoke(9) / protocol_consistency(26) / config_validation(3) / cli_tests(9) / tool_skill(11) | ✅ 全部 passed |

### 10.4 累计完成率回写

- 修复项数：43（至 R66）+ 4（R67）= **47 项**；复核闭环 28（至 R66）+ 4（R67）= **32 项**。
- 跨模块重复函数（control-mode / truncate / dedupe / sha256_hex）四类全部单源化，无遗留待办。
