# 第35轮 — 收敛复核：死双实例清理 + 空模块删除 + 扩展点锚定复核 (2026-08-06)

第34轮完成后按收敛判据继续复核。本轮聚焦：**fat-binary 死代码分析信号**（bin 目标可达性分析全仓零警告即为强收敛证据）、残留死双实例、空模块、以及上轮扩展点决策的一致性复核。

## 一、处理明细

| # | 项 | 处置 | 依据 |
|---|----|------|------|
| 1 | **PuaFeedbackCollector 死双实例**（HarnessBus.feedback_collector 字段 + storage_path 整链） | **删除，收敛为单一权威**：`HarnessBus.feedback_collector` 字段**从未被读取**（grep 全仓零 `\.feedback_collector` 引用）；生产读写全部走全局静态 `pua_feedback_collector()`（trace_pack.rs，`.goon/learning`）。连带删除：`storage_path` 参数（`HarnessBus::new`/`default_harness_bus`/`config_aware_harness_bus` 三处）+ server_builder.rs 的 `<config_dir>/governance` 计算块 + mod.rs 的 `PathBuf` import + 10 处测试调用点更新。行为：无功能变化（死实例从未被读），消除一个从不落盘的实例与目录 | §11:未接线即删除。与第33轮 AlertManager 双实例统一为同型修复 |
| 2 | **capability_bus/orchestration.rs 空模块**（8 行） | **删除**：纯 doc 注释、无任何项、零引用（`capability_bus::orchestration` 无消费方，与 `orchestration_bus` 无关）；删 `pub mod orchestration;` 声明。其历史说明（MultiChannelTransport 移除记录）已存于日志 | §10:全项目清理空模块。其余小文件（keyring_ref/vec_utils/execution_recorder/constants/evaluation/reputation/mcp tools/re_exports）逐一核实均有真实内容 |
| 3 | **evolution_loop 字段复核**（初删 → 恢复保留） | **保留（恢复），记录锚定机制**：初删 `GovernanceServerDeps.evolution_loop` 后，bin 目标（fat binary，main.rs 直接声明全部模块）出现 **16 条 self_evolution 死代码警告**（`fix_compile_errors`/`assess_risk`/`SandboxExecutor::commit`/`GIT_TIMEOUT`/`MetricsPoint`/`cycle_id` 等叶子 API）。机制：该字段是**文档化 pub 扩展点**，其类型引用在 bin 的死代码分析中充当 evolution 子系统公开 API 的存活锚点；删除后 rustc 将子系统从未调用的叶子 API 全部剪枝报告。判定：与第34轮保留 `safety_checker`/`injection_detector` 标准一致（文档化扩展点保留）；这些叶子 API 是 evolution agent 的**设计能力**（如"fix compile errors"是其文档化职责），批量删除属过度清理。恢复字段并在 doc 中补记锚定作用 | §1:实事求是。删"死字段"前必须区分"死引用"与"API 锚点" |

## 二、复核保留项（非死代码，记录依据）

- **时间戳助手**（`now_millis`/`current_timestamp_ms` ×5 模块）：全部 1 行委托 `shared::timestamps::now_ts_ms` **单一源**，属域内命名适配，非逻辑重复，保留。
- **trace_pack 静态单例**（trace_events/error_response_ids/tool_budget_trackers/mcp_audit_logger/pua_feedback_collector）：全部活跃（各 2-5 处消费）。
- **council/quorum**：存活且被 `council_deliberation.rs`/`capability_bus/core.rs` 使用；第33轮已删 multi-round deliberation，当前为已收敛形态。
- **hardening::AuditLogger**（MCP 审计路径）：测试证实经 From 转换写入全局 canonical sink，是薄适配非双路径。
- **ServerBuilder/ModelServerDeps/CacheLayer/GovernanceServerDeps 字段**：逐一核实全部被 `build()` 消费或运行时读取（provenance_ledger/pua_enforcement_plan/memory_persistence 等均有活跃消费方）。
- **`Agent::chat_once` 默认返回 Err**：7 个真实 agent 全覆盖写，默认值有 doc 说明（直写 `chat` 的测试 agent 无需实现），是 trait 安全默认非 stub。

## 三、验证证据(原则 §16)

```
=== 编译 ===
cargo check --all-targets                                            → 零警告零错误
cargo check 4 profile(local/simple-server/multi-users-server/full)  → 零警告
cargo check --workspace(gui/sdk/crates)                             → 零警告
cargo clippy --all-targets -- -D warnings (4 profile)               → 零警告
=== 测试 ===
cargo test --all-targets → 全绿(20 个 test binary 全 ok, 0 failed)
=== 生成物 ===
scripts/gen-provider-catalog.py --check                              → 双输出 OK(37 providers)
=== 代码量(本轮) ===
7 files changed(+/- 归并),净删 ~36 行;删除 1 个空源文件(capability_bus/orchestration.rs,8 行)
```

行为一致性说明:
- #1 移除的是从未被读取的实例与参数链;生产 PUA 学习数据读写路径(全局静态 `.goon/learning`)不变。
- #3 恢复字段后 bin 目标回到零警告;该字段保持 `None` 赋值不变(真实 loop 在 `start_background_tasks()` 内运行),无行为变化。

## 四、完成率回写(原则 §9)

- 修复项:2 项(删除死双实例链、删除空模块)。
- 复核保留项:6 项(#3 扩展点锚定 + 5 组复核)。
- 收敛信号:fat-binary bin 目标(从 `main()` 全可达分析)零死代码警告 + lib/workspace 零警告 + 全测试绿——三轮(33/34/35)死链清理后未再发现新的整链死代码。
- 遗留问题数:0 项。第36轮可继续但预计进入低收益风格项(如多模块 1 行时间戳适配、巧合相等默认值),建议在需要时收尾。
