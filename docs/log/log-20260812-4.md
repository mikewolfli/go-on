# Log 2026-08-12 #4 — 全项目多轮深度+广度扫描与统一架构精炼（backend 专项，第 33–35 轮）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则（§8 禁占位 / §9 完成率回写 / §10 多轮收敛 / §11 禁死代码 / §13 禁假修复 / §14 完整闭环 / §16 验证证据 / §18 文档与代码一致 / §19-20 block_on 清零 / §21 独立验证）。

用户指令：多轮超级深度+超级广度扫描；反复深度分析功能模块重复/冗余并合并精炼；核查已完成/未完成项并补齐继续实施；链路功能重复冗余全面分析合并；全链路完整性扫描（中/大收益可重构或补齐接入激活）；反复扫描直到连续 2 轮无新发现；缺陷/改进须反复确认真实性与价值，不凑数量。**本轮只处理 backend（src/、crates/、config/、tests/），gui、vscode-addon、文档（除本日志）一律不动。**

## 1. 执行计划

| 阶段 | 内容 |
|------|------|
| P0 | 基线确认（git 工作树干净 / cargo check / lib 测试基线 1586） |
| P1 | 第 1 轮：5 个只读子代理并行分域扫描（入口/配置/CLI / ACP 实现内部 / 智能自进化 / 治理安全可观测 / 多模态工具测试） |
| P2 | 逐项独立验证（§21：grep + 通读 + 编译实证）并修复 |
| P3 | 第 2 轮：2 个子代理全新角度（panic/数值安全 + 未深读子系统 hub/fault_tolerance/resilience/observability/schema/agents） |
| P4 | 第 2 轮发现独立验证并修复 |
| P5 | 第 3 轮：1 个子代理收敛验证（本轮全部改动面回归 + 全新角落 audio_processor/sse_optimizer/copilot/skill 执行） |
| P6 | 第 3 轮发现验证并修复（含 1 项 P1 二次修复） |
| P7 | 全量验证矩阵（cargo test --lib / clippy -D warnings / 4 profile / 受影响集成测试）并回写 |

## 2. 本轮记录

- 创建时间：2026-08-12。基线：log-20260812-3.md（R32 收尾，1586 测试全绿）。
- 共完成 **3 轮完整扫描**（R33 五域 / R34 两全新角度 / R35 收敛验证），修复 **60+ 项**（含 R35 验证出的 1 项 P1 二次修复与 1 个 flaky 测试修复），新增/强化回归测试 10+。
- 收敛判定：R35 收敛轮仍发现真实问题（vote bulkhead 二次缺陷、autonomy 汇总调用无界、skill_import enabled 失效等），**未达成"连续 2 轮零发现"** —— 如实记录（§5）。

## 3. 执行记录

### 验证矩阵（收尾时全绿）

- `cargo test --lib`：**1586 passed / 0 failed**
- `cargo clippy --all-targets -- -D warnings`：**0 警告**
- 4 profile（local / simple-server / multi-users-server / full）`cargo check --no-default-features --features X --lib --bins`：**全部零错误**
- 受影响集成测试（i18n / config_validation / structural / protocol_consistency / tool_skill / cli / comprehensive_benchmark / transport_parity）：**全部通过**
- 改动面：**56 文件，+1200 / -382 行**

### 第 1 轮（R33，5 域并行扫描）

#### A 域 — 入口/配置/CLI/报告（src/cli、src/main、src/core/config、src/schema、src/protocol）

1. **A2-1 P2 `phase_max_inflight`/`global_max_inflight`/`review_min_response_chars` 校验/报告但从不生效**：全项目唯一并发限制是硬编码（ToolExecConfig 10 / DrainGuard 128）。修复：`global_max_inflight`（跨 phase 取最大值，clamp 1..10000）经 `ServerBuilder.request_inflight_cap` 接线进 DrainGuard 信号量（低内存/zed-config 配置现在真实生效）。
2. **A2-2 P2 阶段词汇分裂**：报告引擎与自适应推断硬编码 `planning/coding/review/delivery`，而官方配置模板用 `think/act/check/done` → `--status` 对默认配置谎报"review/delivery missing"、自适应推断从不触发。修复：`crate::config::phase_name_for_kind` 语义别名解析（两种词汇都接受），report.rs 循环/存在性检查/超时与 inflight 检查、chat.rs `infer_adaptive_phase` 全部改用解析器，返回值转成配置真实阶段名。
3. **A3-1 P3 validator 错误消息撒谎**：越界报"必须是数字"、非法枚举报"必须是布尔"、数组形状报错不符。修复：新增 3 个 i18n key（`phase_option_out_of_range` / `must_be_array` / `invalid_value`），5 处错误点与 6 个断言测试全部更新（en-US / zh-CN / zh-TW 三语言）。
4. **A3-2 P3 `i18n_default_language` 字段文档过度承诺**：实际只驱动 prompts.* 的 lang 默认值（已接线），全局 locale 由 env 驱动。文档诚实化（§18），不改行为。
5. **A3-3 P2 `--low-memory` 计算出的 inflight 上限被丢弃**：`estimate_safe_limits` 第三返回值从未应用。修复：low-memory 时把 `global_max_inflight`/`phase_max_inflight` 收敛到安全值（只降不升）。

#### B 域 — ACP 实现内部（chat/runtime/helpers/agents/communication）

6. **B2-1 P2 `run_agent_collecting` 流式超时后 `task.await` 无界**：120s/600s 只界接收循环，接缝处 join 无界（reqwest 无超时、半开连接可永久挂起）。修复：join 加 120s 界，超时 abort 任务并带部分响应发 done 返回。
7. **B2-2 P2 autonomy_loop 同类无界 + 无 per-chunk 超时**：`timeout_duration=None` 时 select 的 timeout_fut 是 pending → recv 永久阻塞；round 超时后 `chat_task.await` 无界。修复：per-chunk 120s 超时 + join 加界 + abort。**R35 又发现收尾汇总 LLM 调用同样无界**（见 57 条）。
8. **B2-3 P2 vote bulkhead permit 在 filter 闭包内被立即 drop**（限流形同虚设）→ 修复为随 job 传入；**R35 验证发现仍失效**（闭包参数 drop 先于 join_all 轮询）→ 改为在 async 块内 `let _permit = permit` 持有（P1 二次修复，与 fallback.rs 同模式，见 56 条）。
9. **B2-4 P2 重复 `done` 流事件**：cache-hit（stream_cache_response）与 fallback（run_agent_collecting）路径各发一次，完成块又发第二次（客户端收到双 done）。修复：`stream_done_emitted` 标记（cache-hit/fallback/vote 置位，autonomy 不发）。
10. **B3-1 P3 messenger AtLeastOnce 验证形同虚设**：满 inbox 静默丢消息但 `inbox_size>0` 报成功。修复：`deliver_single` 返回是否入队，`deliver_with_check` 按本次消息验证。
11. **B3-3 P3 双审 reviewer 超时不 abort**（对比 run_agent_collecting 已修）：补 `abort_handle` + `.inspect_err` abort。
12. **B3-2 P3 SpawnAgentTool ExecutionGovernor 死块**：fresh budget（depth 2<10、active 0<128）永不拒绝，"spawn" 树注册无其他消费者。删除死块（§11），SpawnGuard 才是真实上限。
13. **B3-4 P3 `handle_execution_errors` 契约被丢弃**：全失败时响应仍 `done:true`，`requires_user_action/action` 字段无人消费（客户端收不到）。贯通需穿透 ActOutput→reflect→assembler 多层，为低价值高改动，**记录未改**（客户端仍显示错误提示文本）。

#### C 域 — 智能/自进化/记忆

14. **C1 P1 进化循环 propose 把 prose 当补丁目标路径**：`Report::new(analysis.suggested_approach)`（"Profile the hot path..."）→ `generate_patch` 必然 `fs::read_to_string` 失败 → analyze→propose→apply→verify 全链路惰性，自进化永不产出补丁。修复：按 trigger 推导真实 `.rs` 目标（ManualRequest→src/lib.rs、DeadCodeDetected→module），非 .rs 目标诚实返回 ProposalUnavailable。
15. **C4 P1 sandbox 迭代预算 fetch_sub 回绕**：耗尽后下一条目存 u64::MAX，安全上限静默消失。修复：`fetch_update` 耗尽永久拒绝。
16. **C5 P2 consciousness 触发器每请求发**：awareness>0.6 时每个 chat 请求产生 PerformanceRegression trigger（C1 修复后若开启进化=每请求自动改代码）。修复：10 分钟限流（bridge 增字段）。
17. **C2 P2 reinforcement_bus evict_lru 不更新 `state_max_q`**：最佳 (s,a) 被逐后陈旧峰值永久偏置 Q 学习，索引本身无界。修复：逐出后按剩余条目重算该 state 的 max、空 state 剪除。
18. **C3 P2 continuous_learning 写-only 循环 + 死配置**：semantic_patterns 只插不读且 TF-IDF 每周期每记忆重建（无界）；`max_tasks`/`curriculum_stages`/`tasks_per_stage` 零读者。修复：插入改走 1024 上限 FIFO 助手；删除 3 个死配置字段；docstring 诚实化（§18）。
19. **C3-7 P3 进化桥每请求 warn**（默认 evolution 关闭时 bridge 未初始化）：`send_triggers_to_evolution` 空列表早退 + warn→debug。
20. **C6 P2 token_cache 缓存截断响应**：客户端中途 drop 流时部分输出入缓存，下次同请求命中截断答案。修复：收集任务标记 truncated，截断/panic 一律不入缓存（R35 补 panic 路径 `unwrap_or((空, true))`，见 60 条）。

#### D 域 — 治理/安全/可观测

21. **D2-1 P2 mTLS CN 白名单绕过**：CA 签发但 subject 无 `CN=` 的证书落 `"unknown"` 即通过白名单（注释只允许"无证书"语义）。修复：`cn_allowed(cn, presented_cert)` 区分"未出示证书（放行）"与"出示了证书但 CN 不可提取（白名单配置时拒绝）"，测试同步。
22. **D2-2 P2 RBAC principal 完全来自客户端参数**：`roles:["admin"]` 附在任何工具参数即获 Admin（enforcer 接好后可达）。修复：固定 harness/["user"] principal，忽略参数中身份字段并留 debug 日志。
23. **D2-4 P2 SecurityAdvisor alerts 无界**：每告警 push、digest 全量序列化、从不清理。修复：1000 条上限 + 超限丢弃最旧 + 日志。
24. **D2-5 P3 CircuitBreakerSnapshot 错报 totals**：`total_failures`=连续失败数、`total_successes`=总请求数、`last_state_change`=每次轮询的 now。修复：HRE breaker 新增真实 `last_state_change_ms`（transition_breaker/probe/reset 打点），快照 `total_failures=total-successes`、`total_successes=successes`。
25. **D3-6 P3 `extract_url` 双 scheme 取错**：`https://` 后出现时掩盖更早的 `http://`。修复：取最早出现位置。
26. **D3-7 P3 IdempotencyCache LRU 淘汰活值**：热 key 重复插入堆满队列副本，下个插入弹掉热 key 当前值。修复：重插前 retain 去旧队项。
27. **D3-8 P3 protected-dir 检查基于原始字符串**：`../../etc/x`、`sub//etc/x` 前缀绕过。修复：词法归一化（含 `..` 解析）后检查；**R35 补相对穿越**（前导 `..` 保留 + 段包含检查，见 62 条）。

#### E 域 — 多模态/工具/测试

28. **E2-1 P2 `parse_bytes` 无大小上限**（唯一被生产调用的 API；50MB 上限只在死掉的 `parse()` 上）。修复：parse_bytes 同 50MB cap。
29. **E2-2 P2 `process_file_uri` 先整读后查 + video/document 无 cap**（`data:` URI 同缺）：修复为 metadata 先查、全模态（image 10MB/audio 25MB/video 500MB/document 50MB）统一 cap；`process_video` 落盘前查。
30. **E2-3 P2 ffmpeg/ffprobe 无超时**：三处命令包裹 120s `tokio::time::timeout`（tokio Command 无 .timeout() 方法，实测确认）。
31. **E2-4 P2 video `max_frames:0`（文档"unlimited"）被 `max(1)` 钳成 1 帧**：仅 max_frames>0 时才传 `-frames:v`。
32. **E3-2/E3-3 P2/P3 RepoAnalyzer**：克隆缓存无界（每条目钉住整仓磁盘）+ `repo:/` 可走全盘 + walk 无总量界。修复：16 条 FIFO 缓存、拒绝文件系统根、5 万文件 walk 上限。
33. **E3-1 P3 音频转写字面 `\n`**（两处 `\\n` 格式串）：改真换行。
34. **E3-4 P3 `ProtocolVersion::from_u16(0)` 与文档矛盾**（应归 V1）：补 0→V1 分支。
35. **E3-5 P3 基准测试空断言**：`min_dimension_score()>=0` 恒真、qualitative `>=50 || ==0` 恒真。修复：qualitative 精确 0.0、measured 必须 >0、至少一个 measured 维度；删除无用的 `min_dimension_score`。

### 第 2 轮（R34，2 个全新角度）

#### 角度 A — panic/数值安全审计（138 个 unwrap/expect、619 处索引全量复核）

36. **P2 vector HNSW `top_k * 4` 溢出**（top_k 来自请求/阶段配置无上界，≥2^62 debug panic / release ef 污染）：`saturating_mul`。
37. **P3 drain deadline 溢出**（`Instant::now() + 2e10s` 配置可 panic）：DrainGuard 钳制 5..3600。
38. **P3 `complexity` u64→u8 截断**（256→0 选到最宽松 lazy-load 策略）：clamp 后转换。
39. **P3 dns_lookup port u16 截断**（70000→4464 查错端口）：1..=65535 校验并返回结构化错误。
40. **P3 pdf_split start_page u32 截断**（4294967297→1 绕过范围检查）：先 clamp 再转。
41. **P3 anthropic max_tokens u64→u32 截断**（2^32+100→100）：clamp。
42. **P3 `retry_backoff_secs` 移位溢出**（`1u64<<attempt` attempt≥64 panic）：`checked_shl`。
43. **P3 pg_migrate `duration_since(UNIX_EPOCH).unwrap()`**：`unwrap_or_default`。
44. **P3 skill 扫描 worker `join().expect`**（worker panic 会炸掉请求线程）：降级为空结果 + warn。
45. **P3 geo 坐标 `1e999`→+Inf 污染 centroid/NaN**：`is_finite` 校验。
46. **复核安全面（未改）**：markdown 渲染器 char 边界、split_for_streaming 字节切分、HNSW entry_point 不变量、token_bucket 时间差、i18n 模板替换等 30+ 项均确认安全。

#### 角度 B — 未深读子系统（hub/fault_tolerance/resilience/observability/schema/agents）

47. **P2 SseDecompressor 多 chunk gzip 损坏 + identity 空输出**（openai/groq/llama/mistral/perplexity 默认开启压缩；reqwest 无 gzip feature 不发 Accept-Encoding → provider 回 identity → 阈值跨越后 `MultiGzDecoder` 报错吞掉 → **流式输出全丢**；真 gzip 多 chunk 同样损坏）。修复：魔数探测（identity 直通）+ gzip 全量缓冲 flush 时一次性解压（16MiB 输出/64MiB 输入双上限），6 个测试按新语义重写 + 新增 identity/单字节探测用例。
48. **P2 fault_tolerance 恢复计数器 `len()` 碰撞**（逐出删低 ID，恢复后 `fault-{len}` 可能已存在被静默覆盖）：`max_id_suffix`（最大现存 ID+1）。
49. **P3 `LatencySpike { delay_ms }` Debug 持久化无法反序列化**（恢复降级为 Crash，恢复动作从 ScaleUp 变 RestartNode）：改 serde 持久化 + 旧格式回退。
50. **P2 InProgress/Failed 计划永久卡死节点**（崩溃于 execute 与 complete 之间永不恢复）：恢复时 InProgress→Pending（SQLite 与 JSON 双路径）。
51. **P3 平台内存查询失败→假 Critical 拒启**（macOS vm_stat 失败而 hw.memsize 成功 → free=0）：`query_failed` 哨兵归 Unknown。
52. **P3 hub request-line 无界读取**（仅 header 有界）：与 header 同上限循环读取 + 越界 431。
53. **P3 `register_arc` 淘汰任意代理**（HashMap 迭代序 ≠ 插入序，"Evict oldest" 注释是谎言）：新增 `agent_order` FIFO（R35 补重复注册去重，见 59 条）。
54. **P3 LivePerformanceFeed 按模型 map 无界**（模型名来自请求 options 可为任意串）：256 模型上限。
55. **P3 skill 执行 `timeout_secs * 2` 溢出**：`saturating_mul(2)`。

### 第 3 轮（R35，收敛验证：全改动面回归 + 全新角落）

56. **P1 vote bulkhead 二次缺陷**：`join_all` 闭包参数 `_permit` 在返回 future 时即 drop（早于轮询）→ 限流仍失效。修复：async 块内 `let _permit = permit` 持有（与 fallback.rs 正确模式一致）。
57. **P2 autonomy_loop 收尾汇总 LLM 调用无界**（`agent.chat().await` + `rx.recv()` 排水无任何界，`timeout_duration=None` 默认时挂死整个循环）：spawn + abort_handle + 120s 界 + 四分支处理。
58. **P3 `extract_data_uris` 畸形 `data:` URI 触发 `break`**（丢弃其后所有合法 URI）：`continue` + 前进到终止符集。
59. **P3 `register_arc` 重复注册产生队列重复**（首个副本被逐后 map 仍满，可超 MAX_AGENTS）：重插前 retain 去重。
60. **P3 token_cache 收集任务 panic 默认缓存空响应**：`unwrap_or((空, true))` 视为截断不入缓存。
61. **P2 skill_import `enabled` 从不生效**（`--enabled=false` 导入仍注册可执行）：按 enabled 门控运行时注册，禁用导入保持休眠元数据。
62. **P3 protected-dir 相对穿越**（`../../etc/x` 归一化塌成 `etc/x` 逃过检查）：前导 `..` 保留 + 段包含 `/etc/` 匹配。
63. **P3 flaky 测试修复**：`hnsw_remove_repairs_entry_point` 断言 entry_point==Some(0)，但 HNSW 入口由 `random_level()`（fastrand）决定，并行跑约 30% 概率翻车 → 改为断言修复不变量（对任一被选入口）。
64. **复核安全（未改）**：R33/R34 全部修复经逐文件通读确认（去重/钳制/上限/接线均正确），audio_processor / planning 上下文 / council 无新缺陷；pptx/excel 解压无行数上限（有外层 50MB 输入界，记录）、SseBufferPool 保留已增长容量（≤32×峰值帧，记录）、copilot 429 重试吞真实错误（文案级，记录）。

## 4. 完成率回写（principle §9）

| 项 | 状态 |
|----|------|
| 扫描轮次 | R33 五域并行（入口配置 / ACP 内部 / 智能自进化 / 治理安全可观测 / 多模态工具测试）+ R34 两全新角度（panic 数值安全 / 未深读子系统）+ R35 收敛验证（改动面回归 + 全新角落） |
| 修复项 | **63 项**（R33:35 / R34:20 / R35:8，含 1 项 P1 二次修复与 1 个 flaky 测试） |
| 新增/强化回归测试 | 10+（sse_compressor 重写 6 + 新增 identity/单字节探测、mTLS CN 语义、断路器快照真实时间戳、validator 新消息 6 断言、hnsw 入口修复不变量） |
| 验证矩阵 | cargo test --lib 1586/0 ✅；clippy 0 ✅；4 profile 零错误 ✅；8 个受影响集成测试全过 ✅；改动 56 文件 +1200/-382 |
| 恒值/谎言清理 | 3 处谎言注释（"Evict oldest"、protected-dir "canonical"、i18n 字段文档）；2 个配置开关接线（global_max_inflight→DrainGuard、--low-memory inflight）；1 个死块删除（SpawnAgentTool governor）；3 个死配置字段删除（continuous_learning）；1 个失效功能激活（evolution propose 真实目标）；1 个损坏子系统修复（SseDecompressor） |
| 收敛判定 | **未达成严格"连续 2 轮零发现"**：R35 收敛轮仍发现真实问题（P1 vote bulkhead 二次缺陷、P2 汇总调用无界、P2 skill_import enabled）。已全部修复并以全绿矩阵收尾，如实记录 |
| 完成率 | 本会话计划项 100% 执行（扫描→独立验证→修复→全量验证→回写） |

## 5. 说明（如实记录）

1. **收敛判定**：principle §10 要求"连续 2 轮无新发现"。本轮 R33/R34/R35 三轮均有真实新发现；R35 作为收敛轮仍抓到 P1 二次缺陷（vote bulkhead）与 2 项 P2 —— 严格收敛未达成。以 1586 测试绿 / clippy 0 / 4 profile 干净 / 8 集成测试通过为稳定基线收尾。要达成 §10 需再开一轮独立扫描（本会话预算已尽）。
2. **验证方法（§21）**：3 轮子代理报告共 80+ 项发现，逐一 grep/通读确认后才修复；R35 对 R33 全部改动面逐文件回归，抓到 1 处"修了但没修对"（bulkhead 闭包参数语义，P1 二次修复）——这正是 §21 要求独立验证的价值。每项修复后全量 lib 测试 + clippy + 4 profile + 受影响集成测试。
3. **行为变化提示**：`global_max_inflight` 配置现在真实限制进程内并发请求（此前是摆设）；`--low-memory` 的 inflight 收敛现在生效；阶段词汇兼容（think/act/check/done 与 planning/coding/review/delivery 报告/推断一致）；openai/groq/llama/mistral/perplexity 代理的流式输出修复（此前压缩路径在真实流式下输出丢失/损坏）；`skill import --enabled=false`（默认）不再注册为可执行；`sse_compress` 开启时 gzip 输出改为流结束后一次性交付（正确性优先，无中间增量）。
4. **遗留（记录未改，均有依据）**：B3-4 执行错误契约未贯通响应组装器（多层改动、无消费端）；request_signing 仅有时钟偏移窗无重放缓存（opt-in 且无 sign() 助手）；chaos 场景重跑为 no-op（feature 门控、默认不启用）；fault_tolerance Failed 计划为文档化操作员介入语义；pptx/excel 解压无行数上限（外层 50MB 输入界）；SseBufferPool 容量保留；`set_enabled(true)` 不会重新注册禁用导入的技能（与既有 enable 路径一致，未扩行为）。
5. **未改动**：gui/、vscode-addon/、docs/（除本日志）；未提交任何 git commit；全部改动在 backend（src/、crates/、config/、tests/）。
