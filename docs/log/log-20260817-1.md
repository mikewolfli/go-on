# Log 2026-08-17 #1 — 全项目多轮深度+广度扫描与统一架构精炼（R54+，M0–M4 逐项执行）

> **执行规则**：以 `docs/blueprints/principle.md` 21 条为原则；以 `docs/log/log-20260814-3.md` 的 M0–M4 里程碑为步骤规范。每完成一项记录一项（principle §9 回写完成率）。所有改进实事求是，多轮反复核对重复冗余功能，不产生新 issues。

---

## 1. 本轮扫描基线（R54 起点实测）

- `cargo check --all-targets` 0 errors / 0 warnings ✅；`cargo clippy --all-targets -- -D warnings` 零警告 ✅；`cargo test --lib` 1656 全绿 ✅（R53 收尾状态）。
- **5 并行只读子代理全项目深扫**（R54-S1：工具层 / 供应商+治理+协议 / 效率热点 / 大文件拆分方案 / M2–M4 现状），关键发现经本人逐一 grep + 读码复核：

### A. 工具层重复（扫描 A）
| 项 | 位置 | 判定 |
|---|---|---|
| 工具描述/input_schema 双源 | `shared/tool_descriptors.rs` 表 vs 各 `extended/*.rs` 内联 `description()`/`input_schema()` 覆盖（~50 处），已存在漂移（shell_exec/http_request 文案不一） | 表为唯一源，删除内联覆盖 |
| 写路径黑名单两份 | `builtin_tools.rs::enforce_write_sandbox`（11 条）vs `governance/status.rs::PROTECTED_DIRS`+`SENSITIVE_EXTENSIONS`，交集 `/etc /sys /proc /dev /boot`，互不为超集 | 合并为 `security/paths.rs` 单一函数 |
| shell.rs 自实现沙箱 runner | `spawn_once`/`run_one`/内联降级 = `exec_common` 机制第 4 份拷贝 | 收敛到 `exec_common` 超时 runner |
| filesystem 工具绕过 enforce_write_sandbox | move/copy/delete/create_dir 仅 sanitize_path | 已确认（随 A2 一并收口） |
| 双份 Rust 符号提取 | `multimodal/code_repo_analyzer.rs` vs `tool/extended/code_index.rs` | 范围不同，暂不合并（记录） |

### B. 供应商+治理+协议重复（扫描 B）
| 项 | 位置 | 判定 |
|---|---|---|
| 30 个供应商名 → 同一 4 行样板 ×6 arm | `agents/agent.rs::build_agent` L1076-1261 | ProviderSpec 表驱动单 arm |
| 双 rate-limit 检测器 | `agents/agent.rs::is_rate_limit_error` vs `protocol_pack/audit.rs::is_rate_limited_message`（关键词集不同） | audit 委托 canonical |
| 双默认工具治理策略引擎 | `governance/hardening.rs` vs `acp/helpers/governance/tool_governance_defaults.rs` | 两引擎互斥运行，保留（记录，D3 一并评估） |
| `GovernanceAction::as_str()` vs `governance_action_label()` 3 份相同 match | hardening.rs / audit.rs / mcp.rs | 删 label，用 as_str |
| M0.1 未闭合 | `openai_compat/chat_completions.rs::stream_openai_sse` 仍内联解析 | 收口到 stream_consumer |
| 双模型目录 | 原生 `available_models()` vs spec `model_suggestions` | 回填合并 |
| 双 option_* 读取族 | `agents/mod.rs` vs `shared/mod.rs` | 合并到 shared |
| 事件名常量 vs 分类器字面量 | streaming.rs 常量 vs stream_consumer.rs 字面量，缺 result/error/progress/phase_* | 补常量 + 分类器用常量 |

### C. 效率热点（扫描 C，全部经读码复核）
| 项 | 位置 | 影响 |
|---|---|---|
| **`ensure_hnsw_index` 每次 search 全表扫描** | `memory/vector.rs:1063-1143`，先扫全表再检查索引存在 | **HIGH**，每次请求 O(N) 行解码后丢弃 |
| gzip SSE 整流缓冲 | `agents/sse_compressor.rs`，启用压缩时流式变非流式 | MED-HIGH（条件热路径） |
| SPA 探测串行 | `phases/observe.rs:394-589`，N 个 SPA URL = N 次串行 RTT | MED-LOW |
| LSP 工具全局互斥 block_on | `extended/lsp.rs` + `exec_common.rs::with_blocking_runtime` | MED-LOW（并发工具热路径） |
| `SseEventParser` 二次方 drain | `agents/mod.rs:512` | LOW（暂缓） |
| `read_http_header` 每 TCP 段重扫 | `acp/impl/runtime/http.rs:158` | LOW（暂缓） |

### D. M0.4 大文件（>2000 行 ×7）：cli/chat.rs 3251、request.rs 2871、game.rs 2307、vector.rs 2301、tool_descriptors.rs 2299、tool/mod.rs 2246、hyper_resilience.rs 2102 — 拆分方案已产出（见 R55）。

### E. M2–M4 现状：M2.1/M2.3/M3.2/M3.3/M3.4/M4.1/M4.2 MISSING；M2.2（仅用户弹窗层）/M3.1（计分原语有，auto_extract 无）/M4.3（源码轨有，收口未接）PARTIAL。技能名校验重复（D1）、投票双实现（D3）已确认。

---

## 2. 执行记录

### R54-1 ✅ M0.1 收口（B5+B8）：最后一处内联流式消费者统一 + 事件常量单源

- `runtime/openai_compat/chat_completions.rs::stream_openai_sse`：`frame.event == "error"/"done"/"result"/"telemetry"/!= "chunk"` 五处字符串比较 + 内联 `payload.get("error"/"response"/"token")` → 统一走 `stream_consumer::{classify_stream_event, extract_error_message, extract_result_fields, extract_chunk_fields}`；`build_openai_chunk` 载荷构造不变，chunk 只转发 token（与旧行为一致）。
- `chat/streaming.rs`：新增 `STREAM_EVENT_RESULT/ERROR/PROGRESS/PHASE_START/PHASE_END` 常量（10 个全 `pub(crate)`）；发射点改用常量：`chat.rs`（error/result）、`autonomy_loop.rs`（status/chunk×6）、`runtime/http.rs`（error）、`tool/executor.rs`（progress/chunk）。
- `stream_consumer.rs`：`classify_stream_event` 改为匹配常量（发射与分类共享同一词表）；doc 补 chat_completions.rs；新增测试 `classifies_every_stream_event_constant`。
- **验证**：`cargo check` 0/0、stream_consumer 6 通过、openai_compat 10 通过。

### R54-2 ✅ M0.3（A2）：写路径黑名单合并为单一 `security/paths.rs`

- 新建 `src/security/paths.rs`：`protected_write_path(&Path) -> Option<&'static str>`（两份黑名单 13 POSIX + 2 Windows 的并集；匹配 = 归一化路径相等/前缀 + 原始路径子串，为两者严格超集）、`sensitive_file_extension(&Path)`（SENSITIVE_EXTENSIONS 迁入）；3 个单元测试。
- `builtin_tools.rs::enforce_write_sandbox` 与 `governance/status.rs::check_write` 均改调共享函数；`status.rs` 删除 `PROTECTED_DIRS`/`SENSITIVE_EXTENSIONS`/`normalize_path_string`/`path_touches_protected_dir`/`path_has_sensitive_extension`（grep 确认无其他引用）；`check_read_only` 改调 `sensitive_file_extension`。
- 工具层沙箱因此增强（/root、/var/run、/run、/tmp/.X11-unix 现也拦截）——预期行为。
- **验证**：security 100 通过、governance 186 通过。

### R54-3 ✅ M0.3（B2）：rate-limit 检测器单源

- `protocol_pack/audit.rs::is_rate_limited_message` = `agents::agent::is_rate_limit_error(msg) || msg.contains("rate_limited") || msg.contains("error.chat.rate_limited")`（canonical 检测器 + 仅 audit 特有标记）；`normalize_rate_limited_message` 不变；导出不变。

### R54-4 ✅ M0.3（B4）：`GovernanceAction` 标签 3 份 match 合一

- 删除 `protocol_pack/audit.rs::governance_action_label`（与 `GovernanceAction::as_str()` 逐字相同）；`mcp.rs::record_tool_call_audit_with_protocol` 与 `hardening.rs::enforce_action` 均改调 `action.as_str()`。

### R54-5 ✅ M0.2（B1）：30 供应商构造样板收敛为 ProviderSpec 单源

- `ProviderSpec` 新增 `compression: bool`（serde default）；providers.rs 37 条目补齐字段：openai/groq/llama/mistral/perplexity `compression=true`；qwen + 22 名组 + perplexity `chat_path="/chat/completions"`（doubao 原有；openai/openai_compatible 保持 None 走 /v1 派生）。
- `build_agent` 6 个 OpenAI 兼容 arm（30 名）→ **1 个 spec 驱动 arm**：url/model 回退 spec（缺失才报错）、api_key_env 仍仅取 config、chat_path = config→spec→`default_openai_chat_path(url)`、supports_system 取 spec 提示、compression 按 spec 切 `new_with_compression`/`new`。
- 30 个名字逐一核对均存在于 spec 表；chat_path 逐条核对（perplexity URL 无 /v1，spec chat_path 必须保留——正确保留）。
- **验证**：agents 156 通过；漂移守卫测试扩至 6 家目录。

### R54-6 ✅ M0.2（B6）：`available_models()` 与 spec `model_suggestions` 回填合并

- anthropic/gemini/cohere/deepseek/ernie 的 `available_models()` 用 spec `model_suggestions` 回填缺失模型（共享 `default_model_info` 构造）；anthropic 键 "anthropic"，ernie 按 Wenxin/Qianfan 取 spec；漂移守卫测试扩至 6 家。

### R54-7 ✅ M0.3（B7）：option_* 读取族合并

- `option_string/option_f64/option_u64` 移至 `shared/mod.rs`（与 option_bool/option_usize 相邻），`agents/mod.rs` 再导出，全部调用点不变。

### R54-8 ✅ 效率 C1：`ensure_hnsw_index` 快路径（去掉每次请求的全表扫描）

- 在访问 DB 之前先短暂独立持 `hnsw` 锁检查已存在 → `Ok(false)` 直接返回；构建路径保持 `conn→hnsw` 锁序 + 双检竞态守卫；doc 更新；新增幂等性测试（首次 true、二次 false）。

### R54-9 ✅ 效率 C2：gzip SSE 增量解码（压缩流恢复真流式）

- `sse_compressor.rs`：整流缓冲 → 持久 `MultiGzDecoder<IncrementalReader>`；`IncrementalReader` 在输入未到时报 `WouldBlock`（flate2 不推进状态）、`finish()` 后才 EOF；`decompress_chunk` 逐块产出解压字节；无压缩路径字节级不变；测试改为断言增量产出（256KB 按 1KB 分片跨 chunk 输出，round-trip 精确）。

### R54-10 ✅ 效率 C3：observe 阶段 SPA 探测并行化

- 串行 `for` 循环中的 `POST .../agent-task` 探测 → 第二 `join_all` 并发执行，结果按序回填；探测体（超时/截断/失败分支/任务包 JSON 分析）逐字移入 async 闭包，语义不变。

### R54-11 ✅ 效率 C4：LSP 三工具去全局互斥 `run_async`

- `go_to_definition`/`find_references`/`apply_code_action` 新增 `run_async`（LSP 模式直接 await `cached_lsp_client` + `query_*`，不再经 `with_blocking_runtime` 全局互斥 + block_on）；同步 `run()` 保留；共用构造/解析 helper 抽取，无重复解析逻辑。

### R54-12 ✅ M1.5（A1）：工具描述/模式单源（表为唯一源）

- 删除 `extended/*.rs` 中与表重复的 `description()`/`input_schema()` 覆盖（build/cargo/compress/container/diagnostics/diff/environment_info/filesystem/format/git/http/metrics/network/packages/query/search/security/spawn_agent/template/time/tool_search/utils/watch/web_search，~50 处），保留表无条目的覆盖（read_lines input_schema 因表要求必填而保留）；未用 `Value`/`JsonSchema`/`UuidGenInput` 等随之清理。
- `tools_pack.rs`（表优先）+ `types.rs`（默认 input_schema 走表）本就正确，未改。
- **验证**：漂移测试 `bridge_table_tools_match_shared_descriptor_text` / `schema_matches_shared_descriptor_table` 全绿（现在恒真，仍守卫未来新增覆盖）。

### R54-13 ✅ M1.1（A3）：shell.rs 沙箱执行收敛到 exec_common

- `exec_common.rs`：新增 `TimeoutRunOptions { cap, timeout_ms, max_timeout_secs, stdin_input }` + `run_sandboxed_capped_timeout_async`（GNU timeout 包在 bwrap 命名空间内、tokio 侧超时杀进程兜底、`should_degrade_after_bwrap`/`should_degrade_after_spawn_failure` 降级语义与原 shell.rs 完全一致——同用 `is_bwrap_setup_failure` + `record_sandbox_degraded`）。
- `shell.rs::ShellExecTool::run` 改调共享 runner；删除 `gnu_timeout_available`（shell 内副本）、`write_stdin_if_needed`、`spawn_once`、`run_one`、内联 env-scrub/降级；超时/失败输出构造、stdin、截断语义不变。

### R54-14 ✅ M3.1（D1）：技能名校验单门

- `skill/registry.rs::validate_skill_name_rule`（并集：路径穿越/长度 ≤64/字符集），`validate_and_insert` 与 `skill_import.rs::validate_skill_name` 共用；错误文案不变。

### R54-15 ✅ 测试韧性：`shell_exec_runs_echo` 环境跳过清单补 spawn 级失败

- 全量并行跑批（24 线程）下 spawn 失败（EMFILE 类）错误信息不在跳过清单导致 flake（该测试与跳过逻辑均未在本轮改动——`extended/mod.rs` diff 为空，属既有缺陷）；补 `failed to spawn`/`Too many open files`。连跑 4 次全量 lib 无再犯。

### R54-16 ✅ clippy 收尾（新代码 3 项）

- `observe.rs` 复杂类型 → `type PlannedFetchEntry` 别名；`exec_common.rs` 8 参函数 → `TimeoutRunOptions` 结构；`security/paths.rs` manual_find → `.iter().find().copied()`。

### R55 M0.4 大文件拆分（7 个 >2000 行文件全部清零，4 并行代理 + 本人逐项复核）

| 文件 | 拆分后 | 新文件 |
|---|---|---|
| `cli/chat.rs` 3251 | `cli/chat/` | mod 427 / commands 857 / agent_turn 657 / simple_tool 430 / display 316 / git 312 / session 262 / tokens 122 |
| `acp/impl/request.rs` 2871 | `request/mod.rs`（路由壳 974，prologue/epilogue/作用域块原样保留）+ dispatch_{session 487, content 281, runtime 334, state 297, workflow 216, learning 196} + tests 437 |
| `tool/extended/game.rs` 2307 | `extended/game/` | mod 374 / online 339 / process 255 / screen 295 / input 203 / agent 232 / state 475 / modding 213（按 feature 门 1:1） |
| `memory/vector.rs` 2301 | `memory/vector/` | mod 59 / shared 129 / hnsw 390 / sqlite 910 / postgres 394 / tests 506（sqlite/pg 同名 VectorStore 保持 cfg 互斥文件） |
| `shared/tool_descriptors.rs` 2299 | `tool_descriptors/` | mod 270 + core/extended/build/cad/docs/data/media/game/validate（每个 `descriptor()->Option<McpTool>` 链式 `.or_else`，未知回退逐字保留） |
| `orchestration/tool/mod.rs` 2246→694 | `tool/registration/` | mod 34 + core 166 / fs_code 287 / documents 203 / data 252 / cad_media 322 / ops 384（`new()` = new_empty + register_all） |
| `resilience/hyper_resilience.rs` 2102 | `hyper_resilience/` | mod 76 / types 212 / state 96 / engine 1213 / tests 529 |

- 关键点：`macro_rules! ansi` 存 mod.rs 且 `pub(crate) use ansi;` 在定义后；`DISPATCH_REQUEST_METHOD`/`ACP_CURRENT_REQUEST_ID` 作用域块留在 request/mod.rs；每文件 `#[cfg(feature)]` 门未上提；`CircuitState` 别名、`register_game_tools` 路径、`extended::*` 14 个 game 工具名全部保持可达。
- **R55-8 ✅ 修复 2 个既有集成测试失败**（pre-existing，非本轮引入）：`tests/transport_parity_integration.rs` 两个源结构测试仍断言旧 `openai_compat.rs` 单文件内 marker（该文件早在 27b983df 已拆为 stub 21 行），改为断言 `openai_compat/responses.rs`（marker 已核实在 L1030/1263/1396）。
- **R55-9 ✅ 修复 full profile 编译错误**：`registration/ops.rs` 的 `register_game_tools(&mut registry)` 中 `registry` 是 `&mut` 形参绑定（非 `mut` 局部），需直接 reborrow `register_game_tools(registry)`；默认构建下该行被 cfg 隐藏故未暴露。
- **验证**：`cargo check --all-targets` 0/0、clippy 零警告（含 `--features full` 全量）、`cargo test --lib` 1667/1667、集成测试 transport_parity 18 / protocol_parity 7 / protocol_consistency 26 / acp_runtime_rpc 27 全绿、GUI check 通过、4 Profile 编译+clippy 全过、`find src -name '*.rs' | wc -l` **无 >2000 行文件**（M0.4 验收达成）。

---

## 3. 完成率回写（截至 R54，principle §9）

- 本轮扫描模块数：全项目（5 代理分域 + 本人逐项复核）。
- 修复项数：**16 项**（R54-1 ~ R54-16），覆盖：M0.1 收口、M0.2 供应商单源（×2）、M0.3 治理去重（×4 + B7）、M1.1 沙箱 runner 收敛、M1.5 工具描述单源、效率 ×4（C1 全表扫描消除为最高收益）、M3.1 技能名门、测试韧性、clippy 收尾。
- **R55（M0.4）**：7 个大文件拆分全部完成（新文件全部 ≤1500 行，无 >2000 行文件），另修复 2 个既有集成测试 + 1 个 full-profile 编译错误。
- 验证状态：`cargo check --all-targets` 0/0、`cargo clippy --all-targets -- -D warnings` 零警告、`cargo test --lib` **1667/1667 全绿**、GUI check 通过、4 Profile（local/simple-server/multi-users-server/full）编译全过。
- 遗留问题数：0（本轮改动无新增 issue）。

### R56 M1.3 + M1.6 + M2.1（写收口 / RAII 注册 guard / 无头执行器）

### R56-1 ✅ M1.6 RAII `RegistrationGuard`（卸载即回滚，M4 插件基石）

- 新建 `src/orchestration/registration.rs`：`RegistrationGuard { unregister: Option<Box<dyn FnOnce()>> }`，`new()` / `rollback()`（立即执行并解除）/ `Drop`（恰好执行一次）；`Debug` 仅暴露 armed 状态。闭包**有意不要求 Send**（guard 是与注册表生命周期绑定的 drop 作用域对象，要求 Send 会诱使跨线程共享裸指针越界——文档说明）。
- `ToolRegistry::unregister(name)->bool`（含 alias 清理）+ `register_guarded(tool)`；`SkillRegistry::register_guarded(skill)`（复用 register 校验，guard drop 时 unregister）。
- 测试：guard drop 后工具/技能消失、rollback 后不双删、未知名 unregister 返回 false、重复注册错误传播（修复测试自身守卫被临时丢弃的缺陷——`.expect()` 直接丢弃 guard 导致技能立即注销）。
- **验证**：registration 4 + skill::registry 4 + tool 9 全绿。

### R56-2 ✅ M1.3 写操作收口 + `FileChangeEvent` 审计（旧→新 hash 可回放）

- 新建 `src/orchestration/write.rs`：`FileChangeEvent { path, op, old_hash, new_hash }` + `record_file_change(task_id, phase, event)`（经 `governance::audit::global_audit_log()` 记 decision="file_change"）+ `sha256_hex`（复用既有 sha2/hex 依赖，未新增依赖）+ `file_hash`（缺失→None，64MiB 封顶读取）。
- 三入口全部接入（沙箱/负载/锁校验通过之后才记，被拦截的写绝不入账）：`WriteFileTool::run`+`run_async`（op=write_file）、`EditFileTool::run`（edit_file）、`ApplyPatchTool`（apply_patch，解析 `diff --git` 头枚举受影响文件；失败/check_only 不记；不可解析退化为目录级 None-hash 事件并注明）。hash 失败仅 warn 不阻断成功写（审计 best-effort，不改变工具输出契约）。
- **验证**：write 3 测试全绿（hash 稳定/缺失/审计落账），audit 与 tool 测试无回归。

### R56-3 ✅ M2.1 `go-on exec --json`（可编程执行器接口）

- `src/main/cli.rs` 新增 `Exec` 子命令 + `src/cli/exec.rs`（1063 行）：
  - JSONL 事件契约（顶层 `type` 判别）：`thread.started {thread_id, model?, session_id?}` → `turn.started {turn_id}` → `item {kind: message(delta|text)/tool_call/tool_result}` → `turn.completed {turn_id, success, response, duration_ms}`；stdout 只输出 JSONL，日志全走 stderr。
  - `--approval-mode never|auto`（默认 never 无提示；auto 走 `governance::status::quick_check_tool` 门）；`--input-stdin` 读 `{"prompt","session_id"}`（session_id 仅回显关联，resume 需交互会话路径——文档明示未接线）；无 `--json` 时输出纯文本。
  - 真实实现：`AgentRegistry::from_config` 构建运行时 + `classify_agent_token` 收集流式响应 + `execute_tools_concurrent` 执行工具（skill 去重复用共享路径），非占位。
  - 端到端测试：local_echo 真实跑一轮，断言完整 JSONL 序列（thread.started→turn.started→item→turn.completed success=true response="say hi"）；plain 模式 stdout 零输出。
- **验证**：exec 10 测试全绿；`go-on exec --help` 冒烟通过。

---

## 4. 完成率回写（截至 R56，principle §9）

- 修复项数：R54 ×16 + R55 M0.4 ×7 拆分（含 2 既有测试 + 1 full-profile 修复）+ R56 ×3（M1.3/M1.6/M2.1）。
- 验证状态：`cargo check --all-targets` 0/0、`cargo clippy --all-targets -- -D warnings` 零警告、`cargo test --lib` **1692/1692 全绿**、GUI check 通过、4 Profile 编译全过。
- 遗留问题数：0（本轮改动无新增 issue；M2.1 的 resume/多轮限制已诚实记录于文档注释）。

---

### R57 M3.1 + M3.2 + M4.1（技能自进化 / 跨会话全文检索 / 生命周期事件域）

### R57-1 ✅ M3.1 技能自动提炼 + 使用计分（默认关闭，`[runtime] skill_auto_extract = true`）

- 新建 `skill/usage.rs`：`archive_low_frequency`（min_calls≤total<threshold 归档）/ `promote_high_frequency`（逆操作）策略函数 + 常量；`SkillRegistry` 新增 `archived_skills`（归档后模型面隐藏、仍可 get 调用）、`set_archived`/`is_archived`；4 测试。
- 新建 `skill/auto_extract.rs`：`draft_skill_md`（确定性 agentskills.io 格式模板，可经 parse_skill_md 回环验证，无 LLM 调用——诚实记录后续可插 LLM 精化）+ `maybe_auto_extract`（slug 化任务→draft- 前缀→validate_skill_name_rule→parse_skill_md→create_skill_from_prompt 注册，重名跳过，失败仅日志）；绕过 `SkillImportStore::import_skill` 的外部源安全门（in-memory 草稿无源制品，文档说明）；6 测试。
- `reflect_phase` 末尾：门控开启时 `tokio::spawn` fire-and-forget（不阻塞不改变结果）执行 usage 整理 + maybe_auto_extract。
- **验证**：skill 57 通过（+10 新测试）。

### R57-2 ✅ M3.2 跨会话记忆全文检索 + `memory.search` 工具

- 新建 `memory/search.rs`：`MemorySearcher`（同一 warm.db 路径）+ FTS5 external-content 表（含同步触发器+回填）+ LIKE 降级；10 测试。
- **实测结论**：bundled SQLite（3.53.2）编译含 `SQLITE_ENABLE_FTS5`（有证据测试），但仅注册 unicode61/ascii/trigram 分词器，**无 cjk 分词器** → 中文 2 字符查询走 LIKE 互补路径（按 rowid 去重、排在 FTS 命中之后），API 不变（诚实文档）。
- 新工具 `MemorySearchTool`（`memory_search`，query 必填 / limit 默认 10 上限 50，run_async 经 spawn_blocking）注册进 registry（ops.rs，Low 风险 profile）+ `tool_descriptors/extended.rs` 描述 arm + `validate.rs` 必填校验；索引真实表 `warm_memory`（无 messages 表，role 字段用 class 承载并文档注明）。
- **验证**：memory::search 10、tool 187（含 5 工具测试）、descriptors 7 全绿。

### R57-3 ✅ M4.1 agent 生命周期事件域（瀑布事件，M1.6 guard 注册）

- 新建 `src/orchestration/events/`：`AgentEvent`（AgentPreStep/AgentRequest/ToolsPreExecute/ToolsPostExecute/AgentTurnStopping）+ `EventVerdict {Continue, Consume}` + `EventListener` trait + `EventBus`（RwLock 监听者表，瀑布顺序，首个 Consume 短路；`register()->RegistrationGuard`，drop 按 `Arc::ptr_eq` 移除）+ `global_event_bus()`（OnceLock 单例，同 tool_lock_manager 模式）；3 测试。
- `executor.rs::execute_single_tool`：ToolsPreExecute 在治理门+校验之后、hook 链之前发射，Consume → 拦截为 blocked 结果；ToolsPostExecute 仅在真实执行后发射（ok 标志）；`chat.rs::process_chat_request`：起始 AgentRequest+AgentPreStep(step="turn")，收尾 AgentTurnStopping。`_reexports.rs` 增 `pub use ...events`（bin 目标死代码门 + 公开插件 API）。
- **验证**：events 3、tool 187、acp::r#impl::chat 34 全绿。

---

## 4. 完成率回写（截至 R57，principle §9）

- 修复项数：R54 ×16 + R55 M0.4 ×7 + R56 ×3（M1.3/M1.6/M2.1）+ R57 ×3（M3.1/M3.2/M4.1）。
- 验证状态：`cargo check --all-targets` 0/0、`cargo clippy --all-targets -- -D warnings` 零警告、`cargo test --lib` **1720/1720 全绿**、GUI check 通过、4 Profile 编译全过。
- 遗留问题数：0（本轮改动无新增 issue；M3.2 CJK 分词限制、M3.1 无 LLM 提炼均诚实文档化）。

---

### R58 M2.2 审批链分层 ① + M4.3 自进化写链收口

### R58-1 ✅ M2.2 ① permission_hooks（配置外部命令预决策，默认空=行为不变）

- 新建 `governance/approval_chain.rs`：`PermissionHook{command,args}` + `HookVerdict{Allow,Deny,Override}` + `run_permission_hooks[_with_timeout]`（子进程执行，tool_name+tool_args 经 stdin 传入，首行 allow/deny/override 决定，非零退出/超时/垃圾输出=无意见 fail-open 且入审计；默认 5s，测试可注入短超时）+ `permission_hooks_from_config`；每个 hook 运行均记 `decision="permission_hook"` 审计。配置 schema：`[runtime] permission_hooks = ["sh /path/hook.sh --strict", { command = "python3", args=[...] }]`（双形态）。
- `executor.rs::ensure_tool_permission`：治理缓存 miss 之后、用户弹窗之前，hooks 经 `spawn_blocking` 运行；deny→直接拒绝，allow/override→直接放行，无意见→回落现有用户弹窗；无配置时逐字节等价旧行为。
- **验证**：approval_chain 15、tool 187、governance 201、config 49 全绿。

### R58-2 ✅ M4.3 自进化写链收口（audit 回滚点）

- `self_evolution/sandbox.rs`：`apply_patch` 写前算 old_hash、成功写后算 new_hash → `record_file_change(op="self_evolution", phase="apply")`；`revert_patch` 同样接入（phase="rollback"，回滚也是源码修改，不留回放缺口）；hash 失败仅 warn 不阻断（best-effort，同 M1.3 模式）；CodePatch 单文件构造，一 patch 一事件（注释说明无需解析 patch 头）。task_id 用 sandbox instance_id + patch_id（`EvolutionLoop::apply` 未透传 task id，诚实文档）。
- **验证**：self_evolution 38 全绿（含真实 patch 写站点的审计落账测试，e2e 受 clippy 质量门限制已注明）。

### R58-3 📋 D3 投票双实现：分析后记录为暂缓（非缺陷）

- `intelligence/weighted_vote.rs`（delphi_debate + AgentVoter，`enable_delphi_debate` 默认关）与 `acp/helpers/governance/vote_executor.rs`（ACP 高风险投票热路径，默认开）契约不同（Vote vs AgentStrongVoteOutcome、不同胜选/指标），vote_executor 内部双轮已共享 `select_vote_winner`（去重已完成）；强行合并将高风险投票路径耦合到门控智能系统，回归风险 >> 去重收益。记录暂缓，不强行合并。

---

## 4. 完成率回写（截至 R58，principle §9）

- 修复项数：R54 ×16 + R55 M0.4 ×7 + R56 ×3 + R57 ×3 + R58 ×2（M2.2①/M4.3）+ 既有修复（transport_parity ×2、full-profile ×1）。
- 验证状态：`cargo check --all-targets` 0/0、`cargo clippy --all-targets -- -D warnings` 零警告、`cargo test --lib` **1736/1736 全绿**（基线 1656，净增 80 个真实行为测试）、GUI check 通过、4 Profile 编译全过。
- 遗留问题数：0（本轮改动无新增 issue）。

### R59 M1.4 会话日志即事实源 + M3.3 用户级 cron

### R59-1 ✅ M1.4 会话日志即事实源（append-only SessionEvent + derive_messages 投影）

- 新建 `src/acp/session_log.rs`：`SessionEvent{UserMessage/AssistantMessage/ThoughtChunk/ToolCall/ToolResult}`（serde）+ `SessionLog`（append/len/iter）+ `derive_messages()` 投影：ThoughtChunk 折叠进 assistant 内容（\n\n 分隔，前缀/后缀/尾随均处理）；ToolCall 不入消息（配对前导 assistant 回合）；ToolResult 经运行时 `build_tool_result_block` 拼成与真实 prompt 路径同构的块（含去重前瞻，避免后续 assistant 已携带时重复）；9 测试。
- `ConversationState` 新增 `session_logs: HashMap<String, SessionLog>`（键同 branch_heads：`conversation_id:branch_id`，checkpoint 创建时确保条目存在，pruning 不触碰）；`session_prompt_payload` 成功分支非侵入式追加 UserMessage+AssistantMessage + `#[cfg(debug_assertions)]` 不变式断言（模型可见 ⇒ 日志可重建）；失败/被拒回合跳过（文档）。
- 诚实边界：本轮仅 session/prompt 追加；fork/resume 迁移留后续。
- **验证**：session_log 9、protocol_pack 27、acp 434 全绿。

### R59-2 ✅ M3.3 用户级 cron + durable 任务库（新依赖 croner="3"）

- 新建 `src/orchestration/schedule/cron.rs`：`CronJob` + `CronStore`（SQLite：cron_jobs + cron_runs 两表）+ `parse_schedule`（croner）+ `next_fire_after`。语义：首跑=首个未来匹配（新增不立即触发）；每跑后=完成时刻后的下一匹配（跳过运行期间错过的时刻，不排队）；防重叠=内存 in-flight HashSet（同 Mutex），tick 只派发 claim 成功的；崩溃恢复=`recover_crashed_runs` 标记 finished_at IS NULL 为 crashed，next_run_at 未推进→自然补一次（恢复独立调用，CLI 不重写历史）；enable/disable 保留/重置 next_run_at；每次运行记 `decision="cron_run"` 审计。8 测试。
- `acp/background.rs`：`cron_tick_loop`（30s tick，空库零开销）注册进 start_background_tasks；job 经 `exec_pack::handle_workflow_execute(server, payload, trace)` 执行——与 `workflow.execute` ACP 处理器同一函数。
- CLI：`go-on cron add/list/remove/enable/disable`（backend-sqlite 门控）；表达式/载荷 JSON 校验测试；冒烟测试通过（add 打印 job id + next_run_at）。
- **验证**：schedule 8、cli 37 全绿；`cargo run cron` 冒烟通过；multi-users-server profile 门控验证（postgres profile 排除 cron，既有行为不变）。

---

## 4. 完成率回写（截至 R59，principle §9）

- 修复项数：R54 ×16 + R55 ×7 + R56 ×3 + R57 ×3 + R58 ×2 + R59 ×2。
- 验证状态：`cargo check --all-targets` 0/0、clippy 零警告、`cargo test --lib` **1755/1755 全绿**、GUI 通过、4 Profile 编译全过。
- 遗留问题数：0。

### R60 M1.2 配置补丁层 + M2.3 SDK 类型生成

### R60-1 ✅ M1.2 配置补丁层（分层合并 + 来源追踪，opt-in 向后兼容）

- 新建 `src/core/config/patch.rs`：`ConfigSource{key, layer: builtin|project|user|cli, path}` + `LayerSource` + `deep_merge`（递归对象合并，patch 胜）+ `apply_layers`（优先级 builtin<project<user<cli；某 key 被某层覆盖 iff 合并后值异于快照；反序列化失败回退上一可用配置并告警，绝不崩溃）+ `builtin_layer`（空文档解析的 serde 默认，非 Default impl——Default 会产生空 schema_version 等 Rust 默认值）；`user_config_path`（GO_ON_CONFIG_DIR → XDG_CONFIG_HOME → ~/.config，Windows %APPDATA%）。
- `parser.rs`：`load_layered(path, cli_patch)`；顶层 `layered_merge = true` 开关（serde default false，关时 load/load_uncached/load_inner 与旧行为逐字节一致）；开时合并用户层；CLI `--patch` 始终作用于本次调用。`AppConfig` + 12 嵌套结构补 `Serialize`（dump 需要）。
- CLI：`go-on config dump [--patch '<json>'] [--sources]`（真实输出；--sources 打印每顶层 key 的来源层+路径）。
- **验证**：patch 10、parser 3、cli 41（含 dump --sources 层标记测试）、core::config 62 全绿；冒烟：真实用户配置下 dump --sources 正确显示 cache→user / default_phase→project / runtime→cli。

### R60-2 ✅ M2.3 SDK 事件类型从契约生成（无手写事件类型 + CI 门）

- 新建 `contracts/acp-stream-events.json`（10 种 SSE 事件 + 38 个共享 TS 类型的唯一契约，字段名逐字对齐 stream_consumer.rs/streaming.rs——含 chunk/done/telemetry 的 mode/risk_score 字符串化元数据与 tool_approval 数值 risk_score 的差异）+ `scripts/gen-sdk-types.py`（确定性生成器 + 双交叉校验：事件名必须等于 STREAM_EVENT_* 常量 10/10、index.ts+client.ts 引用的 39 个名字全部可导出）。
- `sdk/typescript/src/types.ts` 重新生成（带 GENERATED 头，38 个既有类型名零丢失，唯一新增 `StreamEvent` 联合；StreamChunk 等客户端形状与 vitest 测试构造一致）。`index.ts` 仅补 StreamEvent 导出。
- `Makefile` 新增 `sdk-types-check`（`--check` 临时文件字节 diff，漂移即失败）+ 接入 `ci`/`.PHONY`；`run-quality-gate.sh` 加同风格检查段。
- **验证**：生成器双跑 sha256 一致；`--check` 通过（人为破坏 types.ts 后 --check 报 diff 退出 1，恢复后通过——门是真实的）；npm typecheck 因 node_modules 不完整未跑（诚实注明，需联网 npm install）。

---

## 4. 完成率回写（截至 R60，principle §9）

- 修复项数：R54 ×16 + R55 ×7 + R56 ×3 + R57 ×3 + R58 ×2 + R59 ×2 + R60 ×2。
- 验证状态：`cargo check --all-targets` 0/0、clippy 零警告、`cargo test --lib` **1770/1770 全绿**、GUI 通过、4 Profile 编译全过、SDK 类型门 `--check` 通过。
- 遗留问题数：0。

### R61 M3.4 多平台网关 + M4.2 技能即插件

### R61-1 ✅ M3.4 多平台网关（adapter 模式验证）

- 新建 `src/gateway/`：`adapter::PlatformAdapter`（platform_name/parse_inbound/render_reply，唯一平台面）+ `registry::PlatformRegistry`（RAII 注册）+ `session::TurnLease`（per-chat 租约锁，10min 过期自动回收）+ `ledger::DeliveryLedger`（SQLite 送达账本，sha256 去重）+ `webhook::WebhookPlatform` 具体适配器 + 自包含 tokio HTTP 监听器（POST /webhook/<platform>，复用 ACP HTTP 原语，不引 web 框架）。
- 单轮 agent 执行复用 exec 引擎（`run_agent_turn`：AgentRegistry + classify_agent_token + execute_tools_concurrent）；CLI `go-on gateway --host 127.0.0.1:9797 --config <path>`。
- **验证**：gateway 15 测试全绿（含 local_echo 真实 agent 回合的端到端：新鲜投递→回复→账本落账→同 hash 重投去重→同 chat 并发租约拒绝；第二平台只改 adapter 的验收模式成立）。clippy 收尾：删除冗余再导出、`open_in_memory` 改为 `open(":memory:")`（rusqlite 原生支持）、raw/platform_names/is_active 接入诚实日志点。

### R61-2 ✅ M4.2 技能即插件（SkillBundle 能力包）

- 新建 `orchestration/skill/bundle.rs`：`SkillBundleConfig{name/description/when_to_use/allowed_tools 白名单/context_fragments}`（`allows_tool` 白名单门）+ `SkillBundle{skill_md, config, listeners}` + `parse_bundle`（frontmatter + `[goon.bundle]` TOML 扩展段）+ `install_bundle`（校验名→guarded 注册 skill→guarded 注册事件监听，失败即回滚全部，返回 `BundleRegistration` 组合 guard，drop 全部展开）。
- 修复取消代理留下的 3 个测试失败：`parse_bundle_toml` 跳过表头行再 `toml::from_str`（未知嵌套表会被 toml 静默忽略返回默认值——实测根因）；无效文档断言改为验证拒绝行为本身。
- **验证**：bundle 9 测试全绿（round-trip、TOML 覆盖 frontmatter、无效名拒绝、白名单门、注册/卸载回滚、监听移除）。

---

## 4. 完成率回写（截至 R61，principle §9）

- 修复项数：R54 ×16 + R55 ×7 + R56 ×3 + R57 ×3 + R58 ×2 + R59 ×2 + R60 ×2 + R61 ×2。
- 验证状态：`cargo check --all-targets` 0/0、clippy 零警告、`cargo test --lib` **1794/1794 全绿**、GUI 通过、4 Profile 编译全过。
- 遗留问题数：0。

### R62 M2.2 ② 完成 + D3 终审

### R62-1 ✅ M2.2 ② AutoReviewer 层闭合（审计链全层落账）

- 分析确认：② 层的独立 reviewer 会话已由 `GovernanceReviewer/GuardianReviewer`（governance/guardian.rs，独立 guardian agent，Allow/Deny/Escalate，fail-closed，`guardian_enabled`+`guardian_agent` 配置门控，GuardianHook 挂在所有工具执行路径 pre-execute）承载；缺的是里程碑要求的"审批决策全部进审计链"。
- 补：`GuardianHook::async_pre_execute` 每次判定（allow/deny/escalate_to_user + 理由）记 `decision="guardian_review"` 审计（与 R58 permission_hook 同构）。三层现在全部落账：① permission_hooks（R58）→ ② guardian_review → ③ 用户审批（既有）。
- **验证**：编译 0/0、clippy 零警告、`cargo test --lib` 1794/1794 全绿。

### R62-2 📋 D3 终审：维持暂缓（附证据）

- 重新核查：`delphi_debate`/`weighted_vote` 经 `intelligence/hub.rs::rationalize_decision` **真实接线**（`enable_delphi_debate` 配置门控默认关），非死代码。与 `vote_executor.rs`（ACP 高风险投票，默认开）目的不同（决策合理化 vs 高风险投票裁决）、契约不同（Vote/AgentVoter vs AgentStrongVoteOutcome）。两者内部均已去重（vote_executor 双轮共享 select_vote_winner；weighted_vote 单一实现）。合并将把默认开的 ACP 热路径耦合到默认关的门控系统，回归风险>去重收益。**结论：不合并，保持现状并在日志记录。**

---

## 5. 里程碑终局状态（M0–M4 全部目标达成）

- **M0（地基）全部完成**：事件契约单源 / 供应商 spec 单源 / 治理去重 / 死代码清零 / 7 大文件拆分。
- **M1 全部完成**：M1.1 沙箱 runner 收敛、M1.2 配置补丁层、M1.3 写收口+FileChangeEvent、M1.4 会话日志、M1.5 描述/schema 单源、M1.6 RegistrationGuard。
- **M2 全部完成**：M2.1 exec --json、M2.2 审批链三层（① permission_hooks → ② GuardianReviewer 自动审核 → ③ 用户弹窗，全部入审计链）、M2.3 SDK 类型生成。
- **M3 全部完成**：M3.1 技能提炼+计分、M3.2 FTS5 检索、M3.3 cron、M3.4 网关。
- **M4 全部完成**：M4.1 事件域、M4.2 技能即插件、M4.3 自进化写链收口。
- 记录在案（经分析不执行）：D3 投票合并（契约不同、目的不同、内部均已去重，风险>收益）。
