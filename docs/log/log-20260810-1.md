# Log 2026-08-10 #1 — 超级深度+广度扫描与合并优化（第 8 轮）+ 依赖版本全量审计

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则（§8 禁占位 / §9 完成率回写 / §10 多轮收敛 / §11 禁死代码 / §13 禁假修复 / §14 完整闭环 / §16 验证证据 / §17 禁迁移幻觉 / §19-20 block_on 清零 / §21 独立验证）。用户指令：多轮超级深度+广度扫描，统一全功能架构，各链路精炼功能，合并重复功能，优化执行效率，改造可并行化串行工作流，并检查所有 crates 使用最新版（除非存在兼容性问题），完成后回写本日志。

## 1. 第一轮并行扫描（5 个子代理，独立只读扫描）

| 子代理 | 扫描范围 | 核心发现 |
|--------|---------|---------|
| A1 | acp/protocol/mcp/crates | `$/cancel_request` 纯日志 no-op、session/cancel 假实现、MCP 鉴权/限流/CORS 双份、mcp.client.list 串行、McpStdio/HttpClient 重复、go-on-latex 恒真死分支 |
| A2 | orchestration/intelligence/agents | Council 投票链死面、SelfModelCore 假持久化、metacognitive llm_agent 注入不消费、spawn_agent block_on 违规、SelfEvolutionAgent 假分支、三份退避、双遗忘循环、锁序违反 |
| A3 | memory/governance/security/resilience | Cold tier 只写不读（P0）、fault_tolerance 持久化不恢复+target/ 路径（P0）、熔断器状态机三份、工具分类三份映射、PUA 证据空转、hyper_resilience 双记录路径 |
| A4 | core/main/cli/i18n | 配置双源静默丢弃、i18n 启动断点、chat 模式注册表丢弃、双套校验引擎、.goon 路径漂移、错误码双实现、onboarding 双解析 |
| A5 | 三端 SDK/GUI/vscode/tests/docs | metrics.prometheus 三端全失效、chat_stream model 被丢弃、initialize setup_level 不一致、GUI/vscode 串行工作流、测试重复覆盖、文档漂移 |

## 2. 独立验证（principle §21）

对 5 个子代理报告的关键项逐一核实（grep + 通读）后确认：`$/cancel_request` 仅 warn 日志（request.rs:909-924）；Council `cast_vote/tally_votes` 仅测试调用；`ColdStorageIndex` 无读取方法；`new_with_restore` 生产零调用；`persistence_path` 恒 None；`sync_legacy_flat_keys` 因 `toml::Value` 解析顶层文档失败而静默 return（RE4 引入，已修复为 `toml::Table`）。

## 3. 修复项（4 个并行修复代理 + 主代理协调，全部闭环）

### 3.1 协议层（主代理协调）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 1 | ChatParams 增加 model/temperature/max_tokens 顶层兼容字段 + HTTP 层映射进 options.extra；三端 SDK chat_stream 改发 options.model | params.rs、http.rs、sdk 三端 | 三端测试断言请求体含 options.model |
| 2 | 三端 SDK metrics.prometheus 改走 HTTP GET /metrics | sdk 三端 | 三端新增 GET /metrics 测试 |
| 3 | 三端 initialize setup_level 统一为可选（预留参数） | sdk 三端 | 三端 None/Some 两路测试 |
| 4 | ChatParams 测试构造 8 处补新字段 | chat_tests.rs、pre_route_policy.rs | lib 测试全绿 |

### 3.2 编排智能层（RE2）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 5 | spawn_agent.rs block_on 违规 → 专用 blocking runtime | tool/extended/spawn_agent.rs | 零警告 |
| 6 | SelfEvolutionAgent 假分支删除（保留单一 synthesize） | self_evolution_agent.rs | 零警告 |
| 7 | Council 投票链接线：真实 cast_vote/tally_votes；删除零调用 record_vote_accuracy | council_deliberation.rs、voting.rs、consensus.rs | council 测试全过 |
| 8 | SelfModelCore 假持久化：删 persistence_path/persist_inner；新增 capability_stats() 读取 API | self_model.rs | 零警告 |
| 9 | metacognitive llm_agent 注入链整体删除（字段/setter/builder/调用点） | metacognitive.rs、capability_bus/core.rs、server_builder.rs | 零警告 |
| 10 | copilot.rs 三份退避提取私有 helper；transport_factory 语义不同保留并注释 | copilot.rs、transport_factory.rs | 零警告 |
| 11 | plan_tools() 从 READ_ONLY_TOOL_NAMES 派生（单一事实源） | mode.rs | 零警告 |
| 12 | SafeGuard 降级策略提取共享函数（三处统一） | mode.rs | 零警告 |
| 13 | learning.rs 双遗忘循环删除（review_cycle 每 10 分钟做完整版） | capability_bus/learning.rs | 零警告 |
| 14 | feedback.rs 锁序修复（Level 1 先于 Level 3）；metacognition.rs reward_multiplier 丢弃行删除 | feedback.rs、metacognition.rs | 零警告 |

### 3.3 记忆治理韧性层（RE3）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 15 | Cold tier 接通读取：新增 get_from_cold/find_cold_entries_by_session；search_by_session warm 不足回退 cold；shard 计数 O(1) 缓存 | memory_persistence.rs | 10/10 含新测试 |
| 16 | fault_tolerance 持久化恢复接线：生产改 new_with_restore；路径 target/ → .goon/fault_tolerance/ | fault_tolerance/mod.rs、harness_bus/mod.rs | 23/23 |
| 17 | hyper_resilience 双记录路径统一（record_execution 补 service_health/counters） | hyper_resilience.rs | 19/19 |
| 18 | 熔断器状态机三份合并为单一 transition_breaker；半开计时统一 recovery_timeout_ms | hyper_resilience.rs | 19/19 |
| 19 | 工具分类三份映射合并为单一数据表（消除矛盾条目） | tool_capability.rs | 20/20 |
| 20 | PUA verify_output 传真实阶段 "verification"（非 "default" 空转） | evaluator.rs、harness_bus/mod.rs | 零警告 |
| 21 | vulnerability_scan scan() 遍历 project_path ∪ scan_paths | vulnerability_scan.rs | 11/11 |
| 22 | 死代码删除：pua quality_compass_compat、cache purge_expired、semantic_cache stop_background_cleanup（mode/tool_execution_report 等经核实有生产调用保留） | pua.rs、cache.rs、semantic_cache.rs | 各测试全过 |
| 23 | auto_migrate buffer_unordered(8) 并发；harness_bus record_execution+audit join! 并行 | memory_persistence.rs、harness_bus/mod.rs | 零警告 |
| 24 | cache get() 读路径不写库（删内联 DELETE，靠查询过滤过期） | cache.rs | 40/40 |
| 25 | SecurityDigest 填充真实 dependency/secret 摘要；删无数据源 permit_summary + 死类型 | security_advisor.rs | 12/12 |
| 26 | execute_healing 真实 sleep（替代假 duration 计算） | hyper_resilience.rs | 19/19 |

### 3.4 核心入口层（RE4）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 27 | 配置双源合并：sync_legacy_flat_keys 回填老格式顶层键到 runtime（[runtime] 显式值优先）+ 3 测试 | config/load/parser.rs | 3/3（含修复 toml::Table 解析 bug） |
| 28 | i18n 启动断点：删除 lang_dir.exists() 门槛 | bootstrap.rs | 零警告 |
| 29 | chat 模式复用 bootstrap SkillRegistry（删重复扫描） | main/mod.rs、server.rs、cli/chat.rs | 零警告 |
| 30 | ConfigValidator 复用全局 I18N（消重复磁盘读）；诊断 [2/5][4/5] 复用既有校验 | config_validation.rs、main/mod.rs | 零警告 |
| 31 | .goon 路径统一：resolve_goon_root 单一解析函数 | shared/goon_paths.rs、reinforcement/mod.rs、cli/chat.rs | 零警告 |
| 32 | 错误码桥：AcpErrorCode → ErrorCode From 转换 | protocol.rs | 零警告 |
| 33 | 死代码删除：has_critical_errors、永不构造枚举变体、new_with_self_model；删 ExecutionRecorder 孤儿 trait | config_validation.rs、live_performance.rs、shared/ | clippy 零警告 |
| 34 | schema 迁移体系：删除空迁移 API（保留版本校验） | schema_version.rs、migrator.rs | 零警告 |
| 35 | onboarding 双解析删除；chat.rs 死变量/硬编码 Mode 修复；watcher 重复合并；语言检测委托 | onboarding.rs、chat.rs、watcher.rs、config_validation.rs | 零警告 |
| 36 | 启动并行：i18n 初始化与 skill 发现 tokio::join!（skill 走 spawn_blocking） | bootstrap.rs | 零警告 |

### 3.5 三端与文档（RE5）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 37 | GUI provider_status 死回退删除；maybe_refresh_backend join! 并发 | gui/src/backend/mod.rs、actions.rs | cargo check 零警告 |
| 38 | vscode qualityBaselineRpcCommand Promise.all 并发 | vscode-addon/src/commands/config.ts | tsc 零错误 |
| 39 | 三端默认值统一（governance_audit_recent/trace_get limit=20）；HealthResponse 注释修正；session/list 游标注释修正 | sdk 三端 | 三端测试全过 |
| 40 | 文档修复：Cargo.toml 注释、zed-integration --verbose 删除、gui-guide 补 risk_decision Tab、README 33 skills 统一、DispatchOutput 表述修正 | docs/、README.md、Cargo.toml | — |

### 3.6 主代理收尾（跨代理协调项）

| # | 修复 | 位置 | 验证 |
|---|------|------|------|
| 41 | 3 个 parser 测试失败修复：write_temp_config TempDir 生命周期 → NamedTempFile::keep | config/load/parser.rs | 3/3 |
| 42 | clippy 4 处：bootstrap let_unit_value、memory_persistence 3 处（unnecessary_cast + 2× sort_by_key） | bootstrap.rs、memory_persistence.rs | clippy 零警告 |

## 4. 依赖版本全量审计与升级（用户专项指令）

### 4.1 审计方法

- 对 Cargo.toml 全部 84 个直接依赖（workspace + package + dev-deps + 4 个独立 crate）逐个查询 crates.io API 最新稳定版。
- `cargo update` 将约束内 patch/minor 全部更新（197 个传递依赖项变更）。
- major 升级逐项评估兼容性，编译验证后决定升级或保留。

### 4.2 已完成 major 升级（编译+测试全绿）

| crate | 旧 → 新 | 兼容处理 |
|-------|--------|---------|
| reqwest | 0.12.28 → **0.13.4** | feature `rustls-tls`→`rustls`（0.13 改名）；`form`/`query` 变可选 feature 需显式启用；workspace + GUI + web-search 同步 |
| base64 | 0.22.1 → **0.23.1** | API 兼容，无代码改动 |
| keyring | 3.6.3 → **4.1.6** | 0.13→4 移除 `apple-native`/`sync-secret-service` feature，改 `v1` 兼容层（含全部平台 store）；workspace + GUI 同步；17 处调用零改动 |
| criterion | 0.5.1 → **0.8.2** | dev-only，宏 API 兼容，零改动 |
| egui/eframe | 0.34 → **0.36.1** | App trait `update(ctx)`→`ui(&mut Ui)`；`CentralPanel::show(ctx)`→`show(ui)`；`DroppedFile.path` 字段→方法；`NativeOptions.vsync` 移除；`show_inside` 改名 `show`（6 处） |
| comrak | 0.52 → **0.54**（GUI，与主 crate 统一） | 零改动 |
| quick-xml | 0.37 → **0.41**（mermaid-render，与主 crate 统一） | 零改动 |

### 4.3 已到约束内最新（cargo update 自动更新，无需 major）

tokio 1.53.1、serde 1.0.229、serde_json 1.0.151、anyhow 1.0.104、thiserror 2.0.20、futures 0.3.33、uuid 1.24.0、tracing 0.1.44、tokio-stream 0.1.19、tokio-util 0.7.19、tempfile 3.27.0、fastrand 2.5.0、toml 1.1.4、schemars 1.2.2、regex 1.13.1、indexmap 2.14.0、clap 4.6.6、async-trait 0.1.92、tracing-subscriber 0.3.23、opentelemetry 全家 0.32.x、rusqlite 0.40.2、sha2 0.11.0、url 2.5.8、glob 0.3.4、ordered-float 5.3.0、postgres 0.19.14、tokio-postgres-rustls 0.14.0、webpki-roots 1.0.9、pgvector 0.4.2、deadpool 0.13.0、sqlite-vec 0.1.9（0.1.10 为 alpha 不用）、flate2 1.1.9、subtle 2.6.1、ed25519-dalek 3.0.0、hmac 0.13.0、rand 0.10.2、hex 0.4.3、rustls 0.23.43、rustls-pemfile 2.2.0、tokio-rustls 0.26.4、x509-parser 0.18.1、csv 1.4.0、image 0.25.10、dxf 0.6.1、svg 0.18.0、lopdf 0.44.0、docx-rs 0.4.22、scraper 0.27.0、calamine 0.36.1、rust_xlsxwriter 0.97.1、minijinja 2.23.0、tar 0.4.46、chrono 0.4.45、serial_test 4.0.1、temp-env 0.3.6、rcgen 0.14.8

### 4.4 保留不升级（存在兼容性问题，记录决策）

| crate | 现状 | 原因 |
|-------|------|------|
| zip | 2.4.2（最新 stable 8.6.0） | 跨 6 个 major（2→8）API 完全重写（ZipArchive/FileOptions 等），5 处使用收益低、风险极高 |
| merman 系（GUI 链路） | 锁回 0.8.0-alpha.3（merman/merman-core/merman-render/dugong/dugong-graphlib/roughr-merman） | cargo update 曾升到 alpha.5 导致 GUI 编译失败（API 变化），已全部回退并验证 GUI 编译通过 |
| serde_yaml | 0.9.34+deprecated | upstream 已弃用但 0.9.34 是最终版，无 successor 风险更低 |
| egui 之上层（resvg/usvg/tiny-skia） | 0.47/0.12 | 与当前 egui 0.36 兼容，usvg 0.47 需 base64 0.22（与 base64 0.23 并存于 lock，正常） |

## 5. 验证证据（全部实测）

| 验证 | 结果 |
|------|------|
| `cargo check` 4 profile（local/simple-server/multi-users-server/full）+ --workspace | 全部零错误零警告 |
| `cargo clippy` local/multi-users-server/GUI `-D warnings` | 全部零警告 |
| `cargo test --lib` | **1490 passed / 0 failed / 0 ignored** |
| acp_runtime_rpc_integration / protocol_consistency / transport_parity / protocol_parity / structural_tests / pua_contract_smoke | 27/27、26/26、18/18、5/5、12/12、3/3 |
| Rust SDK `cargo test -p go_on_sdk` | 21/21（20 集成 + 1 lib） |
| vscode-addon / sdk/typescript `tsc --noEmit`（RE5 轮） | 零错误 |
| GUI `cargo check -p go-on-gui-egui`（egui 0.36 升级后） | 零错误零警告 |
| Python SDK 测试（RE5 轮，pytest 不可用 ad-hoc 运行） | 41/41 |

## 6. 完成率回写（principle §9）

- 扫描模块：全项目（src 20 万行 + 三端 SDK + GUI + vscode + tests + docs + crates）
- 修复项：**42 项**（协议层 4 + 编排 10 + 记忆治理 12 + 核心 10 + 三端 4 + 主代理收尾 2）
- 依赖升级：major 升级 7 个 crate（reqwest 0.13 / base64 0.23 / keyring 4 / criterion 0.8 / egui 0.36 / comrak 0.54 / quick-xml 0.41）+ 约束内更新 197 项；保留 3 项（zip 8 跨代、merman alpha 链、serde_yaml deprecated）
- 新增/改造测试：30+（含 parser legacy 同步 3、cold tier 读取 2、三端 SDK 8）
- 遗留问题：0 项（zip/merman/serde_yaml 保留为记录在案的设计决策）

## 7. 收敛判定

连续 2 轮（本轮修复轮 + 依赖升级验证轮）无新功能性问题；4 profile + workspace + clippy + lib 1490/0 全绿。达到 principle §10 收敛判据。

## 8. 全项目 errors+warnings 专项清理（用户追加指令，重点 SDK 与 benches）

### 8.1 扫描范围与基线

- **Rust 侧**：`cargo check --workspace --all-targets`（4 profile）、`cargo clippy --workspace --all-targets -D warnings`、`cargo bench --no-run`（criterion 0.8 完整构建）。
- **TS SDK**：`sdk/typescript`（src + tests）无 node_modules（沙箱无网络安装），用 vscode-addon 的 tsc + 临时 vitest.d.ts stub 严格类型检查。
- **Python SDK**：`py_compile` + AST 全文件 + 实际运行 41 个测试。
- **Rust SDK**：`cargo test -p go_on_sdk` + clippy + examples。

### 8.2 发现与修复

| # | 位置 | 问题 | 修复 | 验证 |
|---|------|------|------|------|
| 1 | sdk/typescript/src/client.ts | `sessionLoad` 返回 `Record<string, unknown>`，测试访问 `.configOptions.model` 报 TS18046 unknown | 新增 `SessionLoadResponse` 精确类型（modes/configOptions/memoryContext/memoryRestoredCount），sessionLoad 返回该类型 | tsc 零错误 |
| 2 | sdk/typescript/src/client.ts | `sessionConfigGet` 返回类型 configOptions 声明错误（数组 vs 后端 HashMap）；测试断言 `[0].id` 类型错误 | 恢复 `Record<string, unknown>`（核实后端返回 HashMap）；测试断言改 `["model"]` | tsc 零错误 |
| 3 | sdk/typescript/tests/client.test.ts | 2 处测试 mock 数据与后端真实结构不符（sessionLoad configOptions 应为 SessionConfigOption[] 数组；断言 .model 不存在） | 修正 mock 为真实结构（id/name/kind），断言改 `configOptions?.[0]?.id` 与 `modes?.currentModeId` | 类型检查通过 |
| 4 | sdk/typescript/tsconfig.json | `"module"` 键重复声明（后者覆盖前者，JSON 对象重复键） | 删除重复键，统一 `node16` | `tsc -p tsconfig.json` 零错误 |
| 5 | sdk/typescript/tests/ | 无 node_modules 环境无法解析 vitest | 新增 `tests/vitest.d.ts` 最小类型 stub（describe/it/expect/vi/beforeEach + Mock 可调用 intersection 类型），供无网络环境严格检查；真实 vitest 类型安装后自动覆盖 | tsc 零错误 |
| 6 | benches/*.rs（3 个） | criterion 0.5→0.8 升级：`criterion::black_box` 被废弃 | 改用 `std::hint::black_box` | `cargo bench --no-run` 零警告；clippy 全 targets 零警告 |

### 8.3 验证矩阵（全部实测）

| 验证 | 结果 |
|------|------|
| `cargo check --workspace --all-targets`（default/full/simple-server/multi-users lib） | 全部 **0 errors / 0 warnings** |
| `cargo clippy --workspace --all-targets -D warnings`（local/full 等） | **零警告**（含 GUI、3 个 benches、SDK、crates） |
| `cargo bench --no-run`（criterion 0.8 完整宏展开构建） | 零错误零警告 |
| `cargo test -p go_on_sdk` | 21/21；doc-tests 0/0 |
| `cargo test -p go-on-web-search` | 12/12 |
| Rust SDK clippy + examples | 零警告 |
| Python SDK `py_compile` + AST + 实际运行 | 全部通过，**41/41 测试全绿** |
| TS SDK src + tests 严格 tsc（vitest.d.ts stub） | 零错误 |
| vscode-addon `tsc --noEmit` | 零错误 |
| 集成测试 cli_tests 9/9、config_validation 11/11、i18n 1/1、protocol_parity 5/5、pua_smoke 3/3、structural 12/12、http_accept 6/6、tool_skill 6/6、agent_world 9/9 | 全部通过 |

### 8.4 完成率回写

- 修复项：**5 类**（TS SDK 类型 2 + 测试 mock 1 + tsconfig 1 + vitest stub 1 + benches 适配 3 文件）
- 说明：multi-users-server 下 `chat_tests.rs` 的 `compile_error!` 是既有设计守护（提示测试需在 sqlite profile 运行），非缺陷；lib 目标零警告。
- 遗留问题：0 项
