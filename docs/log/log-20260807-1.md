# Log 2026-08-07 #1 — 全项目多轮深度+广度扫描与统一架构精炼

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则，对本项目进行多轮超级深度扫描 + 超级广度扫描，直到连续 2 轮无新发现为止。目标：

- 统一全功能架构、各链路精炼功能、统一重复功能（合并去重）
- 优化全项目执行效率、提高节点执行速度、改造可并行的串行工作流
- 以统一架构/平台/链路、加速精炼优化为目标
- 全局考量，收益中/大时考虑重构
- 全面更新文档（README、CHANGELOG、cookbook）以匹配最新实际架构

## 1. 扫描计划

项目规模：445 个 Rust 文件 / 205,604 行（src/），gui+sdk+vscode-addon 三端，crates×3，tests/contracts，scripts。

| 轮次 | 范围 | 状态 |
|------|------|------|
| 第 1 轮 | 6 并行子代理分区扫描（A: acp / B: orchestration / C: governance+security+observability+fault_tolerance+resilience+hub / D: agents+memory+intelligence / E: mcp+protocol+multimodal+core+shared+schema+i18n+cli+main / F: 三端+crates+scripts+tests+docs） | 待开始 |
| 第 2 轮 | 依据第 1 轮发现修复 + 深度验证 | 待开始 |
| 第 3 轮 | 广度验证 + 文档一致性 | 待开始 |
| 第 4 轮 | 收敛确认（连续 2 轮无新发现） | 待开始 |

## 2. 第 1 轮扫描派发

按 principle.md §10 惯例派出 6 个并行子代理，每区主题：
1. 重复功能/冗余模块（跨文件重复实现、转发壳、重复常量/助手）
2. 性能瓶颈（串行可并行、重复计算、每次请求重建、同步阻塞）
3. 死代码（无调用者的 pub 函数、无生产者的指标/字段）
4. 链路缺失（已实现未接线、幻影 RPC、未激活配置）
5. 假修复/文档与代码不符（原则 §13/§15/§18）

### 第 1 轮扫描结果汇总（A-E 区回报，F 区被取消重派）

A 区（acp）：A1 投票结果整体丢弃（ActOutput 恒 None）；A2/A3 status_pack 自证一致门 + 17 个幻影功能就绪门；A4 锁健康空集恒 healthy；A5 ChatParams 3 死字段；B1-B3 转发壳/死参数；C1-C4 性能；D1-D7 清理项。

B 区（orchestration）：H1 ToolExposure::Deferred 全链失效（tool_search 恒空）；H2 ArtifactLayer 360 行只写不读；H3 EvolutionLoop 假审批；H4 mode.rs 白名单与注册表脱节（edit_file 被过滤）；H5 plan_output 只写不读；H6 startup_context.enabled 不生效；M1-M8 重复/死代码。

C 区（governance/security/observability/fault_tolerance/resilience/hub）：fault_tolerance 引擎生产零数据源空转；content_safety 585 行生产零调用；security→AlertManager 转发永不触发；SecurityGovernor 双套默认策略；HarnessBus/Drift 死方法；审计双记录；tool_capability 映射不一致；RuntimeMetrics::snapshot 恒零；多份 P95 实现；audit 手写日期算法。

D 区（agents/memory/intelligence）：communication 消息子系统只写不读；act_phase 双语义缓存双查询双写；DistributedMemoryBus 发得出收不进；2-3 套 agent 选择逻辑并行打分；run_code_quality_scan 同步子进程假门禁；SelfModel/Metacognitive/Discovery 大量测试专用 pub 面；keyring 探测两份拷贝；health 端点同步 keyring 探测。

E 区（mcp/protocol/multimodal/core/shared/schema/i18n/cli/main）：schema_version 迁移系统假骨架；三套配置验证层并存启动跑两遍；cli/chat.rs 平行 chat 管线治理旁路；HTTP client 到处自建；video_processor 串行三重读盘；sampling 实现与声明漂移；CLI --phase/--verbose 死参数。

待 F 区回报后交叉归类、定修复批次。

### F 区（自行扫描，子代理两次被取消）

三端 SDK 契约问题：
- Node `workflowExecute(workflow, params)` 传 `{workflow, params}`，后端 `workflow.execute` 只读 `params.task`（exec_pack/execute.rs `params_task`）→ 传参无效
- Node `traceGet(traceId)` 传 `trace_id`，后端 `trace.get` 只读 `limit`（config_handlers.rs:106）→ 传参无效
- Node `governancePlanGet(planId)` 传 `plan_id`，后端 `governance.plan.get` 不读任何参数（plan.rs:9）→ 传参无效
- Rust SDK 缺 session 系列方法（TS/Node 有 sessionNew/sessionPrompt/sessionClose/sessionList/sessionResume/sessionSetMode/sessionSetConfigOption）
- TS SDK 缺 configReload（Rust/Node/Python 有）
- Python SDK 缺 session 系列方法
- Node SDK `traceGet` 与其余三端 `trace_get(limit)` 签名不一致

已确认正常项：后端路由 /v1/responses、/v1/models、/v1/state/events、/health/ready、/chat/stream 均存在（http.rs）；verify-zed-integration.sh 端口 8090 正确；README 7 sub-bus 描述与 Cargo.toml 一致（local=6+distributed-memory）；CHANGELOG 1.5.0 与 Cargo.toml 匹配。

## 3. 修复批次规划（第 1 轮发现交叉归类）

| 批次 | 范围 | 收益 |
|------|------|------|
| 1 | A1 投票接线 + B5/A2/A3 自证一致门 + A4 锁健康 | 高 |
| 2 | 性能：C1+C2 PUA/is_known、C3 cache_clear 锁、C4 vector_context、video_processor 并行、evaluator p95/锁 | 中高 |
| 3 | 契约：Node SDK workflowExecute/traceGet/governancePlanGet、Rust/TS/Python SDK 补齐缺失方法 | 高 |
| 4 | 去重：M2 HTTP client 统一、audit 双记录、P95 多份实现、keyring 两份拷贝、转发壳收敛 | 中 |
| 5 | 死代码：A5 ChatParams 死字段、B3 死参数、M3/H2/H3 决策项、CLI --phase/--verbose、sampling 对齐 | 中 |
| 6 | 链路：H4 mode.rs 白名单、H6 startup_context.enabled、schema_version 诚实化、E 验证层收敛 | 中高 |
| 7 | 文档：README/CHANGELOG/cookbook 同步 + 本轮日志收口 | 中 |

## 4. 执行进度

### 批次 1（A 区链路/假修复）✅ 已编译
1. **A1 投票接线**：act_phase 的 vote 5 元组提升到函数级作用域并写入 ActOutput（vote_winner/vote_report/used_multi_model_vote/used_multi_agent_vote/review_required），reflect_phase 的 risk_decision/routing_diagnostics 消费者恢复真实值
2. **A2/A3 release_readiness 诚实化**：删除 dual-track 自证一致假检查（summary/detail 复制比对恒 true）；删除 70+ 个幻影功能门（全部报告同一聚合值、无消费者）；保留真实门（stability/security/provider/reproducibility/observability/multi_user/sla/skill/blue27-35）
3. **A4 锁健康**：status_pack 空集不再产生恒 healthy，改报 not_monitored（与 diagnostic_pack 契约一致）
4. **B5 universal/phase 双轨 reconcile 恒 0**：删除假 reconcile，reconciliation_ok 保留为常量（行为等价，消除伪装检查）

### 批次 2（SDK 去重 + 三端契约）✅ TS tsc 通过
1. **删除 sdk/nodejs**（用户确认与 sdk/typescript 重复）：vscode-addon 硬依赖 go-on-sdk-typescript；TS SDK 补 configReload 方法（对齐后端 config.reload）；health() 返回类型修正（ServerStatus 无信封，移除 ApiResponse 包裹）；更新 README×2 + cookbook overview×3
2. 待验证：Rust SDK 缺 session 系列（后续轮补）

### 批次 3（性能）✅ 零警告 + 1639 lib 测试全绿
1. **C2 is_known 去冗余**：删除 `AcpMethodNames::ALL` 线性扫描（21 项全部已含于 ACP_METHODS），is_known 直接 binary_search；生产常量降为 test-only（消除 dead_code）
2. **C1 PUA 热路径**：task_context（infer_task_type/file_count/risk_score）改为惰性构建，仅在 PUA 违规错误路径与 stage 分支计算（普通请求不再做关键字扫描/小写化分配）
3. **C3 cache_clear 双锁合一**：单次写锁内计数+清除
4. **C4 vector_context 重复读取消除**：load_phase_summary_with_settings 复用已解析 settings，避免二次 autotune 锁+settings 重建
5. **A5 ChatParams 死字段删除**：requirement_contract/plan/execution_decision_candidate（零生产读取），同步更新 10 个构造点

### 批次 4（去重 + 链路）✅ 零警告
1. **M2 OSV client 复用**：query_osv 改用 shared blocking client 单例（连接池）+ 请求级 15s 超时（原每包新建 client）
2. **M6 video_processor 并行化**：extract_frames + extract_audio 用 tokio::join! 并发（原串行三重读盘）；analyze_scene 单次遍历（原两次全量求和）
3. **M4 CLI 死参数删除**：--phase/--verbose（生产零读取），同步 main/mod.rs 文档
4. **H4 mode.rs 白名单修正**：diff→file_diff、run_build→build_run、lint_code→lint_run、add_dependency→dependency_add（4 个假名），补上缺失的 edit_file（CLI Edit/FullAuto 恢复精确文本编辑能力）
5. **H6 + L1 startup_context 接线**：main/server.rs 不再硬编码 enabled:true，改读 config.startup_context；合并两份同名 StartupContextConfig 为单一类型（core/config 版加 io_timeout_ms）
6. **H1 + L4 schema_version 诚实化**：migrator 不再把未迁移配置伪造标记为 CURRENT；删除从未构造的 MigrationResult 死类型；register_builtin_migrations 注释冒充实现改为诚实空体+文档
7. **D-10 keyring 两份拷贝合并**：health.rs 删除逐字复制的 keyring_lookup_accounts/keyring_env_fallback_candidates，复用 agent.rs（pub(crate)）单点
8. **C-P3 check_permission 性能**：新增 RbacEnforcer::default_tenant_id（O(n) 取最小），替代每工具调用 tenant_ids() 全量克隆+排序
9. **C-P3 policies_count 轻量获取**：新增 SecurityGovernor::policies_count()，harness 热路径不再 profile() 全量快照
10. **C-重复 tool_capability 映射统一**：pdf_merge/pdf_split→Write、skill_execute/spawn_agent→Shell、cargo_check→Read（operation/action 两侧一致，消除同工具双门判定漂移），测试更新为正确语义
11. **C-P2 审计双记录消除**：SecurityGovernor::record_audit 拆出 record_audit_counters（仅计数），harness evaluator 改用后者；每请求同 sink 审计从 2 条（evaluate/verify_output 各双写）降为 1 条（harness 层持有）
12. **C-P0 security→AlertManager 假转发修复**：新增 AlertManager::report_direct（事件直记，绕过 rule_matches_metric 的 security.* 前缀不匹配），server_builder 安全告警改用；security advisor 告警首次真正进入 recent_alerts/webhook
13. **D-5 code_quality 假门禁修复**：run_code_quality_scan 移出 async 热路径（spawn_blocking），质量门真正生效（health_score < 0.5 拒绝补丁），新增 SandboxError::QualityGate
14. **D-14 Agent::on_message 删除**：全库零调用者零 override 的 no-op 默认处理器（§13），随 trait 移除
15. **E-M5 env_override 名不副实修正**：文件头补实际职责文档；source_precedence 声明去掉不存在的 "env" 优先级（文档欺骗 §18）；第 3 份 keyring 拷贝删除（复用 agent.rs）
16. **B-H2 ArtifactLayer 删除**：零生产写入、profile 恒零（假指标 §13）、与已接线的 ArtifactLedger 功能重叠；删除 orchestration/artifact.rs + harness_bus 字段/方法 + lifecycle 引用（约 600 行含测试）
17. **B-H1 Deferred 机制接线激活**：34 个长尾工具（game/cad/stl/obj/dxf/svg/barcode/gltf/iges/ply/gcode/gpx/geo/image）标注 ToolExposure::Deferred，tool_search 从恒空结果变为真实可发现；ACP/CLI 列表不受影响（用 registry.names()），MCP 过滤独立
18. **B-M3 workflow 智能匹配死代码删除**：route_task_with_workflow + map_phases_to_roles（task_router.rs）、match_workflow + new_with_profile + profile/profile_mut + WorkflowRegistryProfile + TaskContext（workflow_registry）全部无生产调用者；生产用 find/list/register 保持
19. **P95 实现优化**：LatencyQuantileEstimator::p95 + performance.rs 快照改用 select_nth_unstable（O(n) 部分选择替代 O(n log n) 全排序，修复注释声称与实现矛盾的 §18）；4 份实现因数据形态不同（原始向量/水库采样/直方图桶）保留

保留项：record_trace_event 三层转发（trace_pack 私有 mod 可见性桥，必需）；estimate_p95/extract_header_value/security_baseline 转发壳（命名空间别名 + RPC handler 模式，有真实调用者）

## 5. 第 2 轮扫描与修复

### 第 2 轮验证结果
验证子代理核查第 1 轮 24 项修复：全部闭环，5 项残留已补修；性能扫描发现 P0 级问题；广度扫描发现约 90 个死方法/API。

### 补修批次（第 2 轮）
1. **P0-1/P0-2 AppConfig 进程级 mtime 缓存**：release.readiness 单请求 6× AppConfig::load 消除（展示性 load 命中缓存）；startup/config.reload 用 load_uncached 强制新鲜；lifecycle_handlers 同函数 2 次 load 合并为 1 次（P0-2 明确 bug）
2. **A4 函数级契约**：summarize_lock_health(&[]) 空集直接返回 not_monitored（不再依赖调用方绕过），测试更新为验证新语义
3. **B-M3 文档残留**：workflow_pack.rs 注释不再引用已删的 route_task_with_workflow
4. **status_pack 硬编码真值**：zombie_reap/node_dependency_graph/auto_detection 改为真实来源（agent_registry/flow_manager 接线状态）
5. **B5 纯度**：reconciliation_ok 改为显式 true（不再依赖客户端 reconciliation_threshold），删除死建议分支
6. **M2 残留**：packages.rs 私有 BLOCKING_HTTP_CLIENT 改共享单例（第 3 个 HTTP client 统一点）
7. **A1-A3 harness_bus/drift/hardening 死代码**：check_red_line/self_rationalization_profile/decide_work_grade_for_task、resolve_alert/get_active_alerts/get_alerts_by_severity、check_with_feedback/allowed_operations_at_level/SandboxCheckResult、IdempotencyCache 3 方法删除；连带 policy.rs WorkGrade 全家（仅被 decide_work_grade_for_task 使用）约 140 行删除
8. **通信死 API**：✅ messenger recv/peek/wait_for/subscribe_notify/with_capacity/cancel_subtree 删除（inbox_size 保留——AtLeastOnce 投递验证路径必需）；bus cancel_subtree/remove_subtree/health/send_at_least_once + CommunicationHealth 删除；messages_received 恒零计数器删除（§13），status.rs 引用同步；测试同步更新
9. **memory vacuum 系列**：✅ 4 份 vacuum（SQLite×2 + PG×2）+ PG health_check 全删除（零调用）
10. **world_model 死配置字段**：✅ state_retention_ms/confidence_threshold 删除（生产零读取）
11. **B3 sampling 声明收敛**：✅ ACP_METHODS/平台白名单/PUA stage 三处删除 mcp.sampling.createMessage 声明（ACP 桥恒拒绝该能力，不应声明为可用方法）；原生 MCP server 的完整实现保留（未广告）
12. **B1 start_drift_monitor 惰性化**：✅ 从同步构造器 HarnessBus::new 移除自动 spawn（非 tokio 上下文会 panic + 多实例泄漏），server_builder 启动期显式调用一次
13. **文档修正**：✅ voter_impls "five"→four（实际 4 个）；session_compressor 注释谎报 session_manager.rs → 诚实标注 test-only 触发逻辑
14. **B2 i18n set_language 接线**：✅ Language::detect_system 优先读 GO_ON_LANG（与 config_validation 统一）；config.reload 后重新检测并按需 set_language（运行时切换语言，此前仅启动时检测）

保留项（§11 pub API 面豁免）：self_model 16 方法、metacognitive 14 方法、discovery 6 方法、capability_bus 28 方法（均为 pub 公开 API 面，删除破坏模块完整性，与 hub design-reserve 同类决策）

## 7. 第 3 轮修复（收敛验证子代理发现）

验证结果：第 2 轮 13 项全部闭环；新发现 3 项（1 中 2 低）。
1. **F1 注释冒充实现 + 恒零计数器**：✅ governor.rs 两处声称 cancel_subtree 存在的注释删除；CommunicationMetrics.cancellations 恒零计数器删除（§13）；status.rs fork_isolation zombie_reaped_count 硬编码 0 删除；status_pack 注释修正
2. **F2 task_plan 死导出集群**：✅ 删除 4 个 persist 死函数、4 个非 bus recommend 包装、recommend_reattach_modules_from_policy_history、4 个零构造结构体（CheckpointSummaryArtifact/WorkflowOptimizationPolicyArtifact/WorkflowWorkGradeArtifact/PipelineUnifiedMetricsArtifact）；TaskExecutionSummary 保留（活跃函数 recommend_agent_order_from_execution_history 的读模型）
3. **F3 健康检查重复计算**：✅ build_runtime_healthcheck_report 加 2s TTL 缓存（同请求 release.readiness/self_model 不再重复 keyring 探测 + SQLite/vector 计数）

## 6. 第 3 轮收敛验证

验证结果：第 2 轮 13 项全部闭环；新发现 3 项（1 中 2 低）→ 第 3 轮修复完成（见上节）；clippy 零警告 + 1590 lib 测试全绿 + 4 profile 编译零警告。

## 8. 第 4 轮修复（深度收尾）

验证子代理核查第 3 轮闭环；新发现 1 个 P1（恒零指标）+ 3 个低（P2/P3/P4）。

1. **P1 AutonomyLoopReport 恒零指标**：✅ 删除 4 个恒 false/0 且无真实值来源的字段（planner_guidance_used / trace_alignment_coverage / corrective_actions_applied_total / corrective_action_effectiveness_ratio）——ACP/CLI 自治循环路径不执行纠错动作，向客户端暴露恒 0 属假指标（§13）。`contract_snapshot()` 收敛为统一执行证据契约（total_rounds/total_tools/final_phase/total_duration_ms/stop_reason），chat.rs agent_attempts JSON 同步；workflow.execute 路径的 runtime execute 契约（`build_runtime_execute_autonomy_contract`）保留真实修复轮次计数（独立契约，有真实值）；7 处构造点 + 1 处 parity 测试 canonical keys 同步；blue43/blue44 文档描述改为指向 runtime execute 契约
2. **P2 safeguard_approval 取消 token 失真**：✅ 取消时返回 `Some(0)` 作 estimated_prompt_tokens → 改为真实 `estimated_prompt_tokens`（agent 已消费输入 tokens 才生成响应）；`Option<usize>` 标志改 `bool`；删除未用 `_response` 参数
3. **P3 build_cli_principles 缓存 key**：✅ 技能数量 → 内容指纹（name+description 按名排序哈希），技能描述变更也能触发原则重建（此前仅数量变化生效）；用标准库 DefaultHasher，无新依赖
4. **P4 skill dedup 双实现合并**：✅ 抽出共享 `dedup_skill_calls`（`orchestration/tool/mod.rs`），CLI chat 与 ACP run_agent_collecting 统一；**修复 ACP 版行为 bug**（原实现 dedup 时丢弃全部非技能工具，CLI 版正确保留）

验证：clippy 零警告；1590 lib 测试全绿（dispatch_stream_forwards_chunk_and_done 单跑通过，为并行 flaky 时序测试非回归）；4 profile 编译零警告。

## 9. 第 4 轮收敛验证

第 5 轮收敛确认子代理核查：第 4 轮 P1-P4 全部闭环（编译零警告 + 4 项独立验证通过）；新发现 11 项（2 高 + 6 中 + 3 低），未达收敛判据 → 进入第 5 轮修复。

## 10. 第 5 轮修复（假指标/恒零字段/值域统一）

1. **H1 circuit_breaker_open 告警恒零**：✅ background.rs 告警评估改读 `HyperResilienceEngine.profile().open_circuits`（与 Prometheus 导出器同源，真实值可触发）；`RuntimeMetrics::snapshot()` 4 个非自有信号字段（cache_hit_rate/circuit_breaker_open_count/memory_usage_bytes/cpu_usage_percent）加注释诚实标注（由 get_status/导出器从外部来源填充），不再伪装成 RuntimeMetrics 产出
2. **H2 serial_work_ms 恒零**：✅ workflow.execute（execute.rs）与 task.execute（task.rs）两处 `WorkflowLearningEvent` 接线 `execution_report.serial_work_ms` 真实累加值（task.rs:562 有真实数据）；chat 蒸馏路径（knowledge.rs）无逐次时序数据，注释标明 0=无数据
3. **M3 stop_reason 值域统一**：✅ `record_autonomy_loop_stop_reason` 映射 chat 值域（"completed"/"tools_executed"→complete，"all_tools_failed"→failed），completion_ratio 真实化；删除无生产者的 escalated 死桶（静态变量+分支+输出字段）
4. **M4 fallback_unhealthy_ratio 分子分母错配**：✅ 删除（分母是 requirement 决策数，与 council fallback 事件无关）；保留真实计数 fallback_unhealthy_agent_total
5. **M5 build_change_bundle test_coverage 恒零**：✅ 删除（无测试数据源，零消费方）
6. **M6 org_policy.exceptions.active_total 恒零**：✅ 删除（无追踪源）
7. **M7 clarification 恒满分/恒零**：✅ knowledge.rs 恒满分 1.0 → 0.0（无澄清评估，假满分拉高 avg_quality）；execute.rs 加注释（gate 通过后无澄清轮是真实 0）
8. **L8 cpu_usage_percent 恒零**：✅ get_status 接线 perf_snapshot.cpu_usage_percent 真实值（与 memory 同源）
9. **L9 契约字段语义**：✅ build_runtime_execute_autonomy_contract 调用处加注释（total_rounds=1+修复轮、total_tools=子任务数，字段名镜像 ACP 契约形状）

验证：clippy 零警告；1590 lib 测试全绿；编译零警告（含 Send 约束修复：告警循环内 profile() 移至锁前避免 MutexGuard 跨 await）。

## 11. 第 5 轮收敛验证

第 6 轮收敛确认子代理核查：9 项中 7 项闭环；**发现 2 项实际未修**（M7 knowledge.rs 恒满分 1.0、H2 knowledge.rs 注释缺失——上轮编辑失误 old/new 文本写反导致静默失败，教训：子代理逐文件验证不可省）+ 2 项新问题（parallel_speedup 恒值污染并行度推荐、i18n 重复格式化）+ 1 项保留（WorkflowLearningEvent 两路径构造已分化）。→ 进入第 6 轮补修。

## 12. 第 6 轮补修（上轮静默失败 + 新发现）

1. **M7 澄清指标（优化为真实推导）**：✅ 先补修恒满分 1.0 → 0.0（上轮静默失败）；经评估后采用更优方案——**接入 `resolve_learning_clarification_metrics`（与 task.execute 路径同源）**：蒸馏路径从 task 最新需求契约（latest-clarification.json）推导 rounds/quality_score/requirement_change_count（derive_clarification_quality_score 由 missing_fields+ambiguity 加权），无契约时如实回退 0；三路径（workflow/task/distill）统一澄清指标语义，消灭恒 0 稀释与恒 1.0 假满分；成本与蒸馏路径已有 ledger I/O 同级；H2 补修：knowledge.rs serial_work_ms/critical_path_ms 补注释（distillation 无逐次时序数据，0=无数据）
2. **N3 parallel_speedup 污染并行度推荐**：✅ `recommend_parallelism_from_learning_bus` 按 source 过滤（仅统计 workflow.execute/task.execute，蒸馏事件恒值占位 speedup 不再稀释真实加速比）；recommend_failure_strategy/work_grade 用真实 subtasks 字段，保留
3. **N4 i18n 重复格式化**：✅ 抽 `format_template` helper（escape→替换→还原 10 行去重），get_formatted/tf 共用
4. **N5 WorkflowLearningEvent 构造重复**：保留（execute.rs 与 task.rs 的 clarification 指标来源已分化）

验证：clippy 零警告；1590 lib 测试全绿；编译零警告。

## 13. 第 6 轮收敛验证

第 7 轮验证（亲自扫描，扩大范围）：
1. **文档一致性（§18）**：✅ 更新 README×2 统计表（删 Node.js SDK、Rust LOC ~203K、测试 ~1.7K=1590 lib+139 集成、技能 30+）；cookbook index.md 删 Node.js 条目；contracts/cross-client-sync.md 删 Node SDK 行；.gitignore 删 sdk/nodejs 条目；CHANGELOG×2 新增 Round 40 条目；blue68/69 为历史蓝图快照（2026-06）保留
2. **集成测试遗漏（第 5 轮删字段未同步）**：✅ tests/acp_runtime_rpc_integration.rs 删除断言已删的 test_coverage 字段（assert_blue22_change_bundle_shape，2 个调用点）；该文件 27 测试全绿
3. **假 CLI 参数/假测试（§13）**：✅ tests/acp_runtime_rpc_integration.rs 删除不存在的 `--verbose` 参数（服务器因此永远起不来，5s 超时）；tests/cli_tests.rs 的 test_cli_verbose_mode_output 是假测试（测不存在的 --verbose，靠 clap 报错输出碰巧通过）→ 替换为 test_cli_help_lists_known_flags（真实参数回归门禁）；cli_tests 9 测试全绿
4. **环境限制记录**：openai_compat_matrix_integration 6 个测试在沙箱环境因 zed-sandbox-proxy 拦截 IP literal 目标返回 511（Proxy-Status 证实），无法在沙箱运行；非代码回归（同文件 TcpStream 直连的 acp_runtime_rpc_integration 27 全过）；CI/本机环境应正常

全量验证：`cargo test -p go-on --tests` → lib 1590 + 集成全绿（仅 openai_compat_matrix 6 项环境限制失败）；clippy 零警告。

## 14. 第 7 轮收敛验证

第 8 轮验证（第二轮扫描，扩大至三端与全部构建配置）：
- GUI（24K 行）：cargo check 零警告；无死代码/TODO/占位符；端口引用均为合理设计值（默认 8090、dev/stage/prod 预设、代理自动检测列表、provider 默认 URL）
- vscode-addon：tsc --noEmit 零错误；无 TODO/FIXME；runtime 协议层结构清晰（framed/jsonRpc/reconnect/sseStream/heartbeat）
- sdk/typescript：tsc --noEmit 零错误
- 4 profile（local/simple-server/multi-users-server/full）cargo check：全零警告；cargo check --workspace：通过
- **新发现：0 项**

第 9 轮验证（第三轮扫描，剩余区域 crates/contracts/config）：
- crates（go-on-latex/go-on-web-search/go-on-mermaid-render）：workspace 编译零警告
- contracts/：无已删字段/nodejs 引用
- config/：无过时键（auth_enabled/http_bind_addr/strict_mode 旧键均已迁移为 entry_auth_enabled/acp_http_bind_addr/production_strict）
- **新发现：0 项**

## ✅ 收敛判定

第 8、9 轮连续 2 轮零新发现，达到收敛判据。全项目 9 轮扫描修复完成：
- 修复项累计约 50 项（假指标/恒零字段/假满分、死代码/死 API、重复功能合并、串行并行化、三端契约对齐、SDK 去重、集成测试同步、文档一致性）
- 验证基线：4 profile + workspace 编译零警告；clippy -D warnings 零警告；lib 1590 全绿；集成测试全绿（openai_compat_matrix 6 项为沙箱代理 IP-literal 拦截环境限制，非代码问题）
- 遗留：0 项中高严重度问题；历史蓝图（blue68/69）与历史日志保留 sdk/nodejs 记录（时间快照性质）
