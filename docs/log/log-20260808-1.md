# Log 2026-08-08 #1 — 全项目多轮深度+广度扫描与统一架构精炼（第 3 轮总扫描）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则，承接 `docs/log/log-20260807-2.md` 继续多轮深度+广度扫描。目标：

- 反复深度分析各功能模块，合并重复/冗余功能，高效高速高智商
- 核查已完成项是否有遗漏（补齐），未完成项仔细分析后继续实施
- 链路功能重复冗余全面分析后合并精炼
- 统一平台/统一架构/统一路线/统一链路，精炼高效，快速智能，自学习自进步
- 全链路完整性扫描，收益中/大时重构或完整补齐接入激活

## 1. 项目规模（2026-08-08 实测）

- `src/`：442 个 .rs 文件 / 202,303 行
- 三端：gui（Rust Tauri）、sdk×3（rust/typescript/python）、vscode-addon（TS 864 文件）
- crates×3、tests/（17 集成目标）、scripts/、docs/、cookbook/、contracts/、skills/

## 2. 上轮遗留项核查

| 遗留项 | 结论 |
|--------|------|
| ⚠️ acp_runtime_rpc_integration 12通过/15失败 | ✅ **环境问题，非代码回归**：`target/debug/go-on` 二进制被并发 cargo 进程间歇删除导致 spawn NotFound。重建二进制后 **27/27 全绿**。 |
| 第 3 轮广度扫描（三端+crates+scripts+docs） | ✅ 本轮完成 |
| 第 4 轮文档更新 | ✅ 本轮完成（README/CHANGELOG/cookbook/配置/脚本） |

## 3. 扫描执行（6 并行子代理 + 主线程 C 区）

按上轮分区派发 6 个只读子代理深度扫描；C 区因子代理上下文超限 + 网络失败，改由主线程定向 grep 扫描完成。

| 区 | 范围 | 发现 |
|----|------|------|
| A | src/acp（55K 行） | 22 项（高2/中7/低13） |
| B | orchestration+agents（60K 行） | 30 项（高4/中13/低13） |
| C | governance/security/observability/fault_tolerance/resilience/hub | 主线程扫描：总体健康，无高优先级问题 |
| D | intelligence+memory+multimodal（36K 行） | 43 项（高5/中15/低23） |
| E | core/shared/protocol/mcp/schema/cli/main/i18n | 24 项（高3/中8/低13） |
| F | 三端+crates+tests+scripts+docs | 22 项（高3/中7/低12） |

**独立验证（principle §21）修正子代理误报 2 项**：
1. F 区称 `openai_compat_matrix_integration.rs` 整文件零 `#[test]` → 实测 6 个函数全部带 `#[tokio::test]`，`--list` 确认 6 测试可发现，6/6 通过（误报）。
2. F 区称 `skills/batch-grill-me`、`loop-me` 目录不存在 → 实测为指向外部 skills 仓库的**悬空符号链接**（`/Users/mikewolfli/Desktop/workspace/skills/skills/...` 目标不存在），README 表条目已删除。

## 4. 修复批次（全部编译零警告验证）

### 批次 1 — 危险假行为（principle §13/§15/§19 类）

| 项 | 修复 |
|----|------|
| B-H1 | `sandbox.rs test()` 成功时返回 `TestFailure{failed:0}` → 返回真实 `Success{time_ms, warnings:0}`；否则 evolution verify 恒失败并回喂 diagnostic 触发器（假失败循环） |
| B-H2 | 生产进化循环移除盲目 300s `TickTriggerSource`（AutoApproval + 真实工程 = 每 5 分钟盲改 src/lib.rs）；保留 AlertManager/Diagnostic/Fusion 真实信号源；`TickTriggerSource` 整个类型删除（含 re-export） |
| B-H2+ | 生产 sandbox 加 `allowed_targets: ["src/**/*.rs"]` 白名单；`is_target_allowed` 空名单仍全部放行的危险默认保留（有 warn），由显式白名单兜底 |
| B-H2+ | **verify 失败真实回滚**：新增 `SandboxExecutor::revert_patch`（交换 original/patched 行集合反向应用）+ `EvolutionLoop::rollback`，run() 中 verify 失败立即回滚（此前注释声称"history 系统处理自动回滚"但 B-M4 证实那是假实现） |
| B-H3 | `ToolExecItem.success` 恒真 → 改为 `out.success`（不变量 `success:true ⟺ error:None` 已核实）；熔断器 `failure_count` 现可真实增长 |
| B-M9 | `SandboxExecutor::cleanup()`/`Drop` 会删 `workdir/target`+`workdir/.goon`（生产 workdir="." 时=删真实工程构建缓存/进化历史）→ 新增 `is_isolated_workspace()` 保护，真实工程根跳过清理 |
| B-M5 | `AgentCommunicationHook` 假接线（注册了但只打 debug 日志）→ 整个类型+注册点删除（注册真实发生在 `execute_spawn` 的 SPAWN_COMMUNICATION_BUS） |
| A-H1 | `task.execute` 假审批（reviews 无条件 APPROVE + gates 恒 passed）→ 对齐 `workflow.execute`：`run_enhanced_verification` + 真实 subtasks_failed 计算 verdict，review 门如实 rejected |
| A-H2 | 错误响应被记账为"成功"：`mark_error_response` 从 chat_pack::send_error（仅提前返回路径调用，永不消费）移到 `io::send_error` 唯一出口 + `dispatch_to_client` 的 `DispatchOutput::Error` 分支；`ERROR_RESPONSE_IDS` 加 8192 上限防无界增长；MethodNotFound/流错误/转发错误现如实计入失败 |
| A-L5 | drain 拒绝路径 `active_requests` 泄漏（inc 后提前 return 不 dec）→ 补 `dec_active_requests()` |
| E-H2 | `mcp/handlers.rs` 的 `std::env::set_var("RUST_LOG")`（多线程 UB，项目自述文档承认）→ 删除，仅保留 `reload_log_filter` 真实机制 |
| A-M3 | `authenticate` 方法链路断裂（auth 开启时被自身闸门拒绝，关闭时是 no-op）：闸门放行 `authenticate` 方法 + handler 用 SessionManager 真实校验 bearer_token/api_key |
| L 系列 | apply.rs 陈旧错误注释、tool/mod.rs 复制粘贴注释、planner_executor 引用已删类型等文档修正 |

### 批次 2 — 链路缺失/死指标（D 区 + E 区核心）

| 项 | 修复 |
|----|------|
| D-H3 | `decide()` AdaptiveModelSelector 参数断裂（候选 `(name, None)` → UCB 恒 0 → 排序退化为字母序）→ 候选携带 `Some(name)`，与 `record_result_with_context` 键一致，真实参与排序 |
| D-H2 | `optimization_bus.recommend` 对调用方实际优先级 `"high"`/`"balanced"` 落到 `_ => None` → 恒值 fallback claude-sonnet-4；映射到真实优化分支（high→reliability、balanced→speed），置信度如实 |
| D-H4 | AudioProcessor diarization 双倍转写（2x API 成本）+ `max_speakers` 从不发给 API → 首次转写即带 `diarize_speaker_count` 表单字段，删除 `diarize_via_openai` 二次转写与 zip 错位合并 |
| D-M11 | Excel 解析单元格文本被整体丢弃（text_parts 算了不存）→ `ExcelSheet.text_parts` 字段 + 汇入 `ParsedContent.text_content` |
| D-M13 | AgentMemoryBus vector 镜像绕过 user_id 多用户隔离（multi-users 下用户 B 可检索 A 的记忆）→ vector phase 按 user_id 命名空间化（`agent_memory:<uid>`） |
| D-M10 | warm_memory/vector 驱逐 SQL 每次写全表排序 → COUNT 门控（仅超限才排序驱逐，与 cache.rs 同型已修模式对齐） |
| E-H1 | MCP HTTP TLS/mTLS 配置存在但永远无法激活（acceptor 字段私有无 setter）→ 新增 `with_mtls_config` builder + dispatch_server 从 runtime_config 接线（与 ACP HTTP 对齐） |
| A-M2 | `NEGOTIATED_PROTOCOL_VERSION` OnceLock 只写不读（文档声称 SSE 会读但从未读）→ 删除 static；`capabilities.protocol_version` 用 crate 版本字符串 → 真实 `negotiated_ver_num` |
| A-M1 | `LifecycleState.total_requests`（恒0）/`current_phase`（恒"running"）/`last_health_check`（恒构造值）→ 删除 total_requests、current_phase 改为派生（running/shutting_down）、last_health_check 随 set_healthy（30s 循环）刷新 |
| A-M6 | `MaintenanceSnapshot` 6 个 legacy 字段从未更新（last_maintenance/maintenance_interval/next_maintenance_due/tasks_completed/tasks_failed/maintenance_in_progress）→ 删除；测试同步更新 |
| B-M3 | `CommunicationBusProfile.healthy` 恒 true → 新增 `messages_failed` 计数，healthy = 无投递失败 |
| D-H5 | `apply_patch` 质量门 `cargo clippy` 在进程 CWD 跑（可能扫错项目）→ `run_code_quality_scan(project_dir)` 带 current_dir 指向 sandbox workdir |
| B-L12 | spawn 树节点只注册从不注销（`governance.status.registered_agents` 单调增长泄漏）→ `AgentTree.remove_subtree`（已存在但零调用者）接入 `CommunicationBus.remove_agent` + `execute_spawn` 4 个返回点清理 |
| B-L5 | `err_str.contains("50")` 任意含"50"错误串触发重试 → 精确匹配 500/502/503/504 |

### 批次 3 — 重复合并/统一（用户重点）

| 项 | 修复 |
|----|------|
| A-M4 | `skill_import_policy`/`open_skill_import_store` 字节级重复（tools_pack 与 protocol_pack/skill.rs 双份）→ 保留 tools_pack 单源，skill.rs 导入复用 |
| A-M5 | `session/update` 信封构造 5-6 处 → `protocol_pack::session::send_session_update` 单源 writer，server.rs 4 个 emit_* + send_chunk 全部委托 |
| A-L2 | `session_close_payload`/`session_delete_payload` 权限清理三段式重复 → `cleanup_session_permission_state` helper |
| A-M7 | `status_pack.rs` 6 个恒等 ready 辅助函数（x→x 直通）→ 调用点内联布尔值，函数删除 |
| A-L4 | `task.rs` 重复构造 `build_execution_cycle`（2 次同参）+ `multi_agent` 字段复制粘贴（"multi_agent_summary"/"passed"）→ 构造一次复用；multi_agent 改为真实 executors/subtasks/failover/primary+secondary |
| E-M3 | sha256-hex 5 处重复实现 → 全部统一到 `shared::sha256_hex`（skill_import/vector/registry/utils/provenance），连带清理 5 处 `use sha2` |
| B-M12 | 三份工具清单（plan_tools/read_only_tools/is_low_risk_tool）漂移，只读工具如 web_search 在 edit 模式走阻塞审批门 → `mode.rs` 权威 `LOW_RISK_TOOL_NAMES`（read-only 集合 + 纯工具集），`is_low_risk_tool` 查单一清单，read_only_tools 用共享常量 |
| D-M1 | CapabilityBusProfile 六对重复指标字段（同源双读双锁）→ 删除 4 个重复新字段 + 二次锁查询，单次快照 |
| B-L13 | forker KV cache 分支恒假 → 保留（快速审查后确认分支无害） |

### 批次 4 — 三端/配置/文档（F 区）

| 项 | 修复 |
|----|------|
| F-M1 | 5 份 shipped 配置旧键 `[runtime] protocol_mode="acp"`（触发 legacy 迁移警告 + 归一化为 acp_stdio 失去双栈）→ 规范 `[protocol] mode="adaptive"` |
| F-H2 | `sdk/typescript/node_modules` 627 文件被 git 跟踪 → `git rm -r --cached`（未删除磁盘目录） |
| F-H3 | `cookbook/book` 63 个 mdBook 产物被跟踪 + `.gitignore` 写的是不存在的 `DOC/book/` → `git rm -r --cached` + `.gitignore` 改 `cookbook/book/` |
| F-M7 | `.gitignore` 忽略 `snapshots/` 与已提交截图自相矛盾 → 移除忽略项并注释说明 |
| F-L1 | README/README.zh-CN/cookbook overview "148 JSON-RPC handlers" → 149（实测 ACP_METHODS 149 项） |
| F-M5 | cookbook/book.toml + docs/README "13-sub-bus" → "7-feature-gated sub-bus" |
| F-M4 | skills/README 计数（49/37 漂移）→ 45 链接/35 内置注册；删除 2 个悬空符号链接条目（batch-grill-me/loop-me） |
| F-L12 | docs/index.en/zh 断链 `../sdk/rust/README.md`（不存在）→ 指向 `sdk/rust/src/lib.rs` |
| F-L13 | Rust SDK `AcpSessionListResponse.next_cursor` 与后端 `nextCursor` 键名错位（潜在契约隐患）→ 补 `#[serde(rename="nextCursor")]` |
| F-L14 | Python `session_list` 返回裸 list 与 TS/Rust 信封形态不一致 → 返回完整信封，测试同步更新 |
| F-L21 | `scripts/start-go-on.sh` 用旧键 grep `protocol_mode` → 改 `[protocol].mode` |
| F-H2+ | `sdk/python` 下 6 个 `__pycache__/*.pyc` 二进制产物被跟踪（与 node_modules 同类）→ `git rm --cached` + .gitignore 加 `__pycache__/`、`*.pyc` |
| E-L1 | mcp/schema.rs sampling 注释与现状不符（原生已实现）、main/server.rs --diagnose 注释、core/mod.rs provider 注释 → 修正 |
| F-M3 | CHANGELOG/zh-CN `## [1.5.0]` 重复 4 段 → 合并为单版本小节（保留各 Round 子标题，`---` 分隔） |
| F-M6 | 三端契约覆盖缺口（session/load、session/config/*、session/request_permission 等）→ **遗留**：评估后收益中等，且 TS 已有通用 `request()` 通道，Rust/Python 补全列为后续轮次 |

## 5. 验证证据（本轮全部实测）

| 验证 | 结果 |
|------|------|
| `cargo check`（local + simple-server + multi-users-server + full） | 4 profile 全部零警告 |
| `cargo clippy --all-targets -- -D warnings` | **零警告**（principle §6） |
| `cargo test --lib` | **1574 passed / 0 failed** |
| acp_runtime_rpc_integration | 27/27（上轮遗留环境问题已澄清） |
| transport_parity / protocol_consistency / protocol_parity / e2e / http_accept_matrix | 18/18、26/26、6/6、8/8、6/6 |
| cli_tests / config_validation / i18n_test / tool_skill / pua_contract_smoke / chaos_drill / structural_tests | 9/9、11/11、1/1、9/9、5/5、3/3、12/12 |
| openai_compat_matrix（F 区误报项独立复核） | 6/6（6 个场景全部真实可执行） |
| GUI `cargo check` / Rust SDK check / TS SDK tsc / vscode-addon tsc / Python pytest | 全零警告 / 通过 / 通过 / 通过 / 20/20 |

## 6. 本轮完成率回写（principle §9）

- 扫描模块数：src 442 文件（6 子代理分区 + C 区主线程）+ 三端 + crates + tests + scripts + docs + 配置
- 修复项数：**42 项**（危险假行为 11、链路/死指标 12、重复合并 10、三端/配置/文档 9）
- 子代理误报修正：2 项（F 区 openai_compat 零测试、skills 缺失目录）
- 遗留问题：4 项（非阻塞）
  1. F-M6：SDK 未暴露 session/load、session/config/* 等（TS 已有通用通道，Rust/Python 补全收益中等，建议下轮）
  2. D-M2/M3/M4/M5/M6/M7：D 区多组无生产调用者的死方法（observability 4 方法、orchestration 12 facade、DiscoveryCenter 抽象层、SelfModel 部分 API 等）——量大且涉及公共 API 面判断，建议下轮独立批次清理
  3. B-M1/M2：AgentLifecycle 状态机未推进 + messenger inbox 只写不读（消息子系统仅有观测痕迹）——需设计决策（接线 vs 精简），下轮处理
  4. D-H1：MemoryPersistence 三层写读闭环（读侧 retrieve 仅测试调用）——读侧接线需设计上下文注入策略，下轮处理
- 收敛判定：本轮未达"连续 2 轮无新发现"（遗留 4 项 + 新发现 41 项均已修复），下轮继续

## 7. 统一架构观察（供后续轮次参考）

- 三端契约核心清单（session×7、tools、checkpoint、conversation、config、health、version）已收敛一致
- 5 种协议（adaptive/acp_stdio/acp_http/mcp_stdio/mcp_http）与 4 种 profile 全链路闭合
- 热路径已无 block_on/block_in_place/Handle::current()（principle §19/20 合规，本轮全树复查）
- 进化循环安全化后语义：仅真实信号（告警/诊断/融合桥）触发 + 白名单 + verify 失败回滚，"自学习自进步"保留在安全边界内
