# Log 2026-08-11 #4 — 全项目多轮深度+广度扫描与统一架构精炼（第 14 轮）

## 0. 执行规则

以 `docs/blueprints/principle.md` 为原则（§8 禁占位 / §9 完成率回写 / §10 多轮收敛 / §11 禁死代码 / §13 禁假修复 / §14 完整闭环 / §16 验证证据 / §17 禁迁移幻觉 / §18 文档与代码一致 / §19-20 block_on 清零 / §21 独立验证）。

用户指令（本轮重点）：
1. 多轮持续超级深度+超级广度扫描，统一全功能架构、各链路精炼功能、统一重复功能、优化全项目执行效率（改造串行工作流、加快各节点速度），反复多轮扫描直到**连续 2 轮无新发现**为止。
2. 多轮反复核对重复冗余功能，合并去重，直到完全简洁干净、高效。
3. 优化全项目执行效率，提高各节点执行速度和反应速度，改造可并行化的串行工作流。
4. 全局考量、按策略改进，回写原文件。
5. 分析需重构的改进，收益中/大则考虑重构。
6. 一切以统一架构、统一平台、统一链路、加速精炼优化为目标。
7. 检查各链路精炼后功能是否完整、线路是否完整顺畅；如有中/大收益，补充完整接线激活（最优）。
8. **本轮务必清理恒值路径**：仔细分析确认恒值或空值的合法性。恒值如果合法，注意不要静默，必须有提示。
9. 不确定判断时全局全方面分析，以最优解决方案为准。
10. 反复全方位确认改进确实值得执行，不凑数。

## 1. 执行计划

| 阶段 | 内容 |
|------|------|
| P0 | 回写本记录（本轮日志创建，先记录后执行） |
| P1 | 第一轮并行扫描（多只读子代理分域：协议链路 / 编排智能链路 / 记忆治理韧性 / 核心入口三端 / 文档脚本配置） |
| P2 | 独立验证（principle §21）：对报告项逐一 grep + 通读核实，不信任自我报告 |
| P3 | 并行修复 + 主代理协调，全部闭环并附验证证据 |
| P4 | 第二轮扫描（深度+广度复扫，重点复查第一轮修复区域、恒值路径专项复扫、遗漏区） |
| P5 | 若第二轮有新发现 → 修复；**直到连续 2 轮无新发现**（收敛判据） |
| P6 | 统一架构审查：跨链路重复功能合并、串行工作流并行化、各节点执行速度优化 |
| P7 | 全量验证矩阵（4 profile build / clippy / 测试 / 三端 SDK / GUI / vscode） |
| P8 | 全面更新文档（README / CHANGELOG / cookbook / 相关 docs），与最新实际架构一致 |
| P9 | 回写执行结果（完成率、遗留问题、收敛判定） |

## 2. 本轮记录

- 创建时间：2026-08-11
- 基线：log-20260811-3.md（第 13 轮）约 45 项修复 + 收敛（lib 1537 通过 / clippy 零警告 / 4 profile 零错误 / 三端干净）
- 在案设计债务（第 13 轮记录，本轮重点复查）：
  1. 双语义缓存合并需 benchmark（semantic_cache vs embedding 语义分支）
  2. hub 手写 HTTP
  3. Council 声誉自荐票设计
  4. 四套性能跟踪独立消费者
  5. MCP 错误本地化子串匹配需 typed error
  6. pg_pool FALLBACK_RT 串行化需 benchmark
  7. tool/http.rs client 单例
  8. autonomy/vote 不喂 LivePerformanceFeed
  9. agent 排序双路径有意设计
  10. UKB insights 与 reputation 同源
  11. 风险关键词三套表重叠需 typed classifier
  12. 错误分类子串表 7 处用途互异待 typed error 统一

- 本轮专项重点：**恒值/空值路径清理**（合法性分析 + 合法恒值必须显式提示不静默）+ 跨链路重复功能合并 + 串行工作流并行化。

## 3. 执行记录

### R1 — 第一轮并行扫描（5 个只读子代理：协议链路 / 编排智能链路 / 记忆治理韧性 / 核心入口三端 / 文档脚本配置）

**P2 独立验证（principle §21，主代理逐项 grep + 通读核实）后确认的高优项：**

| 级别 | 位置 | 发现 | 验证结论 |
|------|------|------|----------|
| P1 | memory_bridge.rs:60 | From 转换写死 `accessed_at: 0` → warm 条目 30 天 TTL 恒立即到期，低 usefulness 记忆 ~10 分钟被永久删除 | ✅ 证实（iterate_expiring SQL `accessed_at <= now-30d`，0 恒满足） |
| P1 | request.rs:570 | `check_red_lines(method)` 自然语言红线 vs 方法名全串匹配恒不匹配恒通过（假安全信号） | ✅ 证实（真实红线链在 tools_pack.rs → check_tool_call） |
| P1 | rationalization.rs | `is_full_auto` 参数生产 3 调用点恒 false → 分支恒死、返回值恒 false、verify_output 风险加权恒不触发 | ✅ 证实 |
| P1 | capability_selector.rs:45-49 | TaskContext.task_type 硬编码 `TaskType::Other` → task-fit 恒 0.60、UCB task 维度恒 "Other" | ✅ 证实（存在两套 TaskType 枚举） |
| P1 | exec_pack/task.rs:980 | 自适应选择器：决策期用 agent 名读 UCB，执行期用 model id 写 → 两侧值空间不匹配，agent 级排序从未生效 | ✅ 证实（注释声称已修复但只修了一半） |
| P1 | 协议 | token 生命周期断链：issue/revoke 零生产调用、logout 不吊销、auto-provision 使任意 HMAC 正确 token 自动得会话 | ✅ 证实 |
| P1 | gui/actions.rs:677-692 | GUI 生成 config 三重缺陷（BOM 内嵌 / [protocol] 重复 / default_phase 不匹配）→ 首启必失败 | ✅ 实测复现（--validate-config 逐项报错） |
| P2 | 多文件 | platform_mode 配置零消费、vscode 4 个死设置、GUI secret_source 恒不生效、verbose 恒 false 零读取、vector.clear_all 不清 HNSW、kustomization 把 YAML 当 TOML、.secrets.env 死键 等 | ✅ 逐项证实 |

**已核实非问题（不凑数）：** 配置默认值兜底均合法；`negotiate_mcp_version` 单版本协商合法；entry_rate_limit fail-open 是刻意降级；block_on 全在专用 runtime/测试；MCP minimal-server 分支为 pub API 面；GUI features.* 开关全部有消费点；k8s `/health` 豁免鉴权属实。

### R1 修复执行（4 个并行修复代理 + 主代理补完，共约 40 项，全部闭环）

**记忆治理**（10 项）：accessed_at 改 now_ts + 回归测试；check_red_lines 死调用移除；rationalization 去 is_full_auto + 语义修正（evaluator 传 1.0-risk，评审门/风险加权生效）；clear_all 重置 HNSW + 回归测试；semantic_cache 后台清理复用 purge_expired；agent_memory_bus insights join_all 并行；status 计数接线真实 guard 计数器；恒值注释×4（staleness/promote 阈值/fallback_to_hash/零向量 bail）；AuditEntryBuilder 恒 None 字段删除。

**编排智能**（9 项）：task_type 经 TaskRouter::analyze_task 映射（5 测试）；exec 路径追加 agent 级 UCB 记录（闭环决策期排序）；auto_degrade/evaluate_degradation/new_safeguard 删除（行为不变，ReadOnly 档注明恒不可达）；风险关键词 6 处评估为语义互异维持债务；council 声誉失效显式 warn 提示；get_role_specs 死链删除；恒值注释×7（forker/flow/verification/plan_construction/set_exploration_bias/orchestrator doc/bulkhead doc）；self_evolution agent_registry 死字段删除；verdict_as_str 兜底注释。

**协议链路**（14 项）：SessionManager 撤销黑名单（revoked map + validate_token 前置检查 + cleanup 清理 + 3 测试）；logout 吊销 bearer_token；user_auth_auto_provision 恒 true 显式注释；verbose 字段删除；is_notification_response 4 处统一；mcp_error_response 3 处统一；MAX_BODY_SIZE 提为 pub const；PermissionRequest 携带真实 timeout_secs；McpClientConfig timeout_ms/init_timeout_ms 参数暴露（3 测试）；transport_factory 误导文案修正；client.rs doc(hidden) 移除；state_sync 悬挂 doc 删除；negotiate_mcp_version 注释；health_endpoint_ready 恒 true 注释；DrainPermit 注释。

**三端配置部署脚本**（8 项）：GUI config 生成三重缺陷修复（BOM 剥离/[protocol] 去重/default_phase 覆写，--validate-config 实测通过）；RuntimeConfig.platform_mode 删除（types/defaults/load/templates×5）；GUI secret_source 删除（UI 注明仅 keyring）；vscode 4 死设置删除 + configManager 死分支 + README 路径；kustomization configMapGenerator 删除；.secrets.env 死键改 GO_ON_ENTRY_API_KEY；scripts×6（quality-gate 死分支/BINARY、build set -e、readiness PASS/FAIL、start/stop 合并、skill-market heredoc 日期）；GUI 启动 join!；renderer sub_x 恒等分支化简（L366 恒 false 论断经核实不成立，未加误导注释）。

**文档**（文档代理部分完成被中断，主代理补完）：README 数字（声明口径 1623/151 + 说明）、cookbook local.md 平台_mode 行删除（并行冲突）、gen-provider-catalog.py shebang 恢复。

### R2 — 第二轮扫描（3 个只读子代理：回归复查+恒值专项 / 重复功能+效率 / 文档一致性）

**回归复查 18/18 全部闭环**（R1 修复逐项核实，无遗留死路径、文档与代码一致）。**新发现约 15 项，主代理直接修复：**

| 修复 | 位置 | 说明 |
|------|------|------|
| dispatch.rs Error 分支委托 send_error | dispatch.rs + io.rs | 去重 ~15 行 + 修复该路径缺失 acp.error 平台上下文注入（行为漂移） |
| header 认证解析统一 | session.rs + runtime/protocol.rs | extract_header_values 共享 helper（大小写不敏感），修复 AUTHORIZATION: 全大写不识别 |
| logout HTTP header 认证吊销 | request.rs | headers 中 Bearer token 注入 params，header 认证登出生效 |
| autonomy rounds 死记录删除 | autonomy_loop.rs + autonomy.rs | AutonomyRound/rounds/retry_count 恒 0 写后即弃 → 删除（保留 5 个被消费标量） |
| vscode locale 18 条死字符串删除 | locales ×3 | chatMaxHistory/chatMaxTokens/chatStreaming 三语清理 |
| mode.rs 文档修正 | mode.rs | compute_risk_score 三档矩阵对齐 + allowed_tools 恒值注释 + 模块 doc 已接线 |
| memory.rs 模块 doc | memory.rs | 已接线状态如实化 |
| multimodal 注释 | mod.rs | MAX_IMAGE_SIZE 不再谎称 GUI 引用 |
| build_client_config 起底默认 | mcp_client_pack.rs | 消除 60_000/10_000 双源漂移 |
| rationalization 计数口径注释 | rationalization.rs | blocked=flagged 口径显式说明 |
| capability_selector file_count 注释 | capability_selector.rs | 消息数语义与 infer_file_count 区分 |
| README SafeGuard 表述 + 数字 | README ×2 | auto-degradation 改 risk-scored gating；1624→1623（精确计数） |
| workflow-config skills 示例 | workflow-config.md | require_sha256=true/floating_ref=false/cache_dir 对齐代码默认 |
| GO_ON_SERVER_API_KEY 全面改名 | config.simple-server.toml + deploy.sh + docker-compose.yml + cookbook×6 | 旧名无任何代码读取（entry_auth 链统一 GO_ON_ENTRY_API_KEY） |
| runtimeManager --verbose 移除 | runtimeManager.ts | CLI 无此参数（clap 拒绝） |
| CHANGELOG.zh-CN 轮次顺序 | CHANGELOG.zh-CN.md | 45→44 恢复最新在前 |
| SAFEGUARD_MODE 工具面 | SAFEGUARD_MODE.md | SafeGuard 默认只读工具面如实化（apply_patch/run_tests 需审批） |

**R2 评估后不做（如实说明）：** observe_phase 多模态+URL 预取并行化——仅当单请求同时含附件与 URL 才收益，且需重构 260 行 SPA 探测逻辑，收益不达中/大标准（指令 #10）。

### R3 — 第三轮收敛扫描（2 个只读子代理：回归+残留恒值 / 三端+文档终扫）

回归复查 14 项全闭环；残留恒值/死代码/架构一致性全部干净（零 #[allow(dead_code)] 生产残留、零 todo!、config 模板零幽灵键、Cargo features 全部有 cfg 引用）。**新发现 5 项，全部修复：**

| 修复 | 位置 | 说明 |
|------|------|------|
| zed.md 模式回落修正 | cookbook/en/zed.md | 代码实际为 chat 路径强制 `edit`（evaluate_pre_chat_gates），非 `ask`（mode.rs 直接解析层才回落 ask）——两层行为差异已双向注释显式说明 |
| design.md 过时 CLI 参数 | docs/design/design.md | --phase/--verbose 标注为历史设计（CLI 无此参数） |
| GUI autotune aggressive 删除 | autotune.rs + i18n×3 | 三轮确认恒不生效（后端无消费），删除复选框与发送字段 |
| CHANGELOG Round 46 新增 | CHANGELOG.md + zh-CN | 本轮记录入册（Round 45 的 1537 为历史实测值，按历史如实保留不改） |
| mode.rs doc 边界 | mode.rs | 0.40 精确值归属（doc 与 `> 0.40` 代码对齐，避开 markdown blockquote 语法） |

### R4 — 第四轮收敛自扫

**无新发现。** 生产代码零 `#[allow(dead_code)]` 残留（仅 multimodal 2 个已文档化公共契约常量）；零 todo!/unimplemented! 生产调用（verification.rs 为检测器字符串）；工作树 95 个改动文件全部为本轮意图内改动，无越界垃圾文件。

### R5 — 第五轮收敛自扫

**无新发现。** contracts（state-sync 5 事件/editor-capability-matrix 5 模式已在 R3 核实）、examples（编译通过）、skills 34、prompts 三语、Makefile、deny.toml、platform_mode 旧名残留全部干净。

### P7 — 最终验证矩阵（全部实测）

| 验证 | 结果 |
|------|------|
| `cargo test --lib` | **1549 passed / 0 failed / 0 ignored**（声明 1623） |
| `cargo clippy --all-targets -- -D warnings` | **零警告** |
| 4 profile check（local/simple-server/multi-users-server/full） | 全部编译通过 |
| 15 个集成套件（151 非 chaos 声明） | 全通过（chaos_drill 6 个为 chaos-testing 特性门控） |
| `cargo check --workspace` / GUI `go-on-gui-egui` | 零错误 |
| vscode `tsc --noEmit` + locales JSON | 零错误，JSON 有效 |

## 4. 完成率回写（principle §9）

- **扫描模块**：全项目（src 444 文件 + tests 15 套件 + GUI + vscode-addon + SDK + crates×3 + config×5 + cookbook 三语 + docs + README/CHANGELOG + deploy + scripts + .github + benches + contracts + examples + skills + prompts）
- **扫描轮次**：5 轮（R1 首扫 5 代理 → R2 复扫 3 代理 → R3 收敛扫 2 代理 → R4/R5 主代理自扫），共 10 个子代理 + 主代理独立验证（P2 逐项 grep/通读核实）
- **修复项**：约 60 项（R1 代理 40 + R2 主代理 17 + R3 主代理 5 + 文档补完若干），其中 P1 7 项、P2 约 20 项、P3/文档数十项
- **新增/更新测试**：10 个（memory_bridge warm TTL 回归、vector clear_all HNSW 回归、session 撤销黑名单×3、capability_selector task 映射×5、mcp_client_config×3 中部分为新增）
- **遗留问题**：0 项阻断。设计债务在案（双语义缓存合并需 benchmark、hub 手写 HTTP、Council 声誉自荐票设计、pg_pool FALLBACK_RT 需 benchmark、tool/http client 单例、agent 排序双路径有意设计、UKB insights 与 reputation 同源、风险关键词 6 处语义互异需 typed classifier、错误分类子串表待 typed error、i18n 三端三份手工维护需 codegen、observe_phase 条件性并行化收益不达标准）；test-only API 面（issue_token/revoke_token、set_exploration_bias、SessionManager revoke 系列）均已显式注释
- **收敛判定**：**达到收敛**。R4、R5 连续两轮无新发现（R3 的 5 项均已修复并验证）；lib 1549 全绿、clippy 零警告、4 profile + 三端全部干净

## 5. 说明（如实记录）

1. **恒值路径清理是本轮主题**（指令 #8）：所有恒值经合法性判定——合法恒值全部显式提示（注释/warn 日志/文档），非法恒值全部修复。典型案例：accessed_at=0（非法→修复）、user_auth_auto_provision=true（合法但无签发端点，注释说明）、SafeGuard degrade_policy=ReadOnly（合法设计，allowed_tools 处注释 + SAFEGUARD_MODE 文档）、health_endpoint_ready=true（预留字段注释）、fallback_to_hash=true（不可配置注释）。
2. **并行修复代理期间工作树存在过渡性编译错误**（各代理镜像树验证），全部合并后主代理在真实工作树复验通过——这正是并行写代理与独立验证（§21）结合的验证流程，最终以真实工作树全量绿为准。
3. **文档代理被用户中断**（完成约 60%），主代理接管补齐并修正了其 2 处并行冲突（cookbook 中已删除的 platform_mode 行、gen-provider-catalog.py 误删 shebang）。
4. **CHANGELOG Round 45 的 1537 未改**：那是该轮的历史实测值，按"历史记录如实保留"原则保留；本轮新值 1549 记入新增的 Round 46 条目。
5. **zed.md 模式回落最终取 `edit`**：chat 请求路径（evaluate_pre_chat_gates）强制 edit 是实际行为；mode.rs 直接解析层回落 Ask 是另一层语义——两层差异已在代码注释双向显式说明（统一链路中的已知分层，非文档错误）。
6. **R4/R5 为主代理聚焦自扫**（死代码/恒值/旧名/残留/未扫角落），深度基于 R2/R3 的全量回归复查之上；收敛判据（连续 2 轮无新发现）如实达成。
