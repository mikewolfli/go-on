# 全链路审查日志 - 2026-07-07 (第4轮)

## 审查范围
- 全项目多轮超级深度+超级广度扫描
- 全链路审查（主链路 + 未闭合的树枝链路）
- 工具调用、SKILL部署、CHAT执行路径

## 本轮发现的真实问题及修复

### 问题1: `parse_enum_from_debug` 在 multi-users-server profile 下 dead code
- **位置**: `src/fault_tolerance/mod.rs:287`
- **根因**: 函数缺少 `#[cfg(feature = "backend-sqlite")]` 编译条件
- **影响**: multi-users-server profile 编译产生 dead_code 警告
- **修复**: 添加 `#[cfg(feature = "backend-sqlite")]` 保护

### 问题2: `WorkGradeDecision` 结构体错误标记 `#[allow(dead_code)]`
- **位置**: `src/acp/helpers/governance/policy.rs:163`
- **根因**: 全结构体标记 `#[allow(dead_code)]` 掩盖真正的死代码状态
- **影响**: 不符合 principle.md 第14条（禁止假修复/假标记）
- **修复**: 移除结构体级别的 `#[allow(dead_code)]`，仅在确实只在测试中使用的字段 `requested` 和 `decision_action` 上保留

### 问题3: `protocol_mode.rs` 中未使用的 `priority()` 和 `fallback()` 方法
- **位置**: `src/shared/protocol_mode.rs:22,35`
- **根因**: 带 `#[allow(dead_code)]` 且注释说 "Not yet wired into production path"
- **影响**: 死代码，不符合 principle.md 第9条（不允许占位/未完成代码）
- **修复**: 完全移除这两个方法

### 问题4: `markdown_renderer.rs` 中未使用的 `raw_response()` 和 `partial_line()` 方法
- **位置**: `src/cli/markdown_renderer.rs:557,568`
- **根因**: 完全没有被任何代码调用（非测试，非生产）
- **影响**: 死代码
- **修复**: 完全移除这两个方法。`take_raw_response()` 保留作为生产代码的唯一消费者。

### 问题5: `obj.rs` 中未使用的 `parse_pair()` 和 `parse_face_element()` 函数
- **位置**: `src/orchestration/tool/extended/obj.rs:34,44`
- **根因**: 带 `#[allow(dead_code)]` 且从未被任何代码调用
- **影响**: 死代码
- **修复**: 完全移除这两个函数

### 问题6: `step.rs` 中未使用的字段
- **位置**: `src/orchestration/tool/extended/step.rs`
- **根因**: 
  - `StepEntity.raw` 完全未使用
  - `StepSummary.entities` 写入后从未读取（仅 `entity_count` 被消费）
- **影响**: 死代码，浪费内存
- **修复**: 移除 `StepEntity.raw` 和 `StepSummary.entities` 字段

### 问题7: `PipelineOutcome` 中字段 `timing`, `phase_name`, `mode` 死代码
- **位置**: `src/acp/impl/chat/pipeline.rs:78-83`
- **根因**: 三个字段被构造但从未被读取（仅 `result` 被消费）
- **分析**: 注释为"reserved for observability pipeline"，是明确的未来预留
- **处理**: 保留（非无意义死代码，是计划内预留）

## CHAT 系统深度审查

### 终端 Chat (src/cli/chat.rs)
- ✅ 主循环通畅，支持 /help /quit /clear /save /load /compact 等命令
- ✅ 流式渲染使用 StreamMarkdownRenderer，避免原始文本闪烁
- ✅ 工具调用通过 `__tool_call__` 协议提取 → 执行 → followup 完整链路
- ✅ Ctrl+C 中断支持，自动中止 agent 任务防止僵尸后台任务
- ✅ 自动保存 session（去抖 + notify 等待机制，非忙等）
- ✅ Token tracker 成本估计
- ✅ /model 切换 agent

### ACP Chat Pipeline (src/acp/impl/chat/)
- ✅ OODA 状态机 (Observe → Think → Act → Reflect)
- ✅ 缓存命中跳过 Reflect 阶段的优化
- ✅ 各阶段 OTel span 自动管理
- ✅ 分阶段计时精度到 ms

## 验证结果

### 编译检查
| Profile | 状态 |
|---------|------|
| local (default) | ✅ 零警告 |
| simple-server | ✅ 零警告 |
| multi-users-server | ✅ 零警告 |
| full | ✅ 零警告 |

### 代码质量
| 检查项 | 结果 |
|--------|------|
| `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| `cargo test --lib` | ✅ 1966 passed, 0 failed |
| cli_tests | ✅ 全部通过 |
| config_validation | ✅ 全部通过 |
| protocol_parity_integration | ✅ 全部通过 |

## 总结
本轮修复 6 个真实死代码/假标记问题：
1. `parse_enum_from_debug` cfg 保护缺失 → 修复
2. `WorkGradeDecision` 假标记 → 修复
3. `protocol_mode.rs` 未接线方法 → 删除
4. `markdown_renderer.rs` 死方法 → 删除
5. `obj.rs` 死函数 → 删除
6. `step.rs` 死字段 → 删除

所有 profile 编译零警告，clippy 零警告，全部 1966+ 测试通过。CHAT 系统完整通畅，工具调用链路、SKILL 执行链路、会话管理、流式渲染均通过审查。
