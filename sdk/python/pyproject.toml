[project]
name = "go-on-sdk"
version = "0.1.0"
description = "Python SDK stub for go-on ACP endpoints"
requires-python = ">=3.9"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
