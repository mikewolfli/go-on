{
  "name": "go-on-sdk-typescript",
  "version": "1.1.0",
  "description": "TypeScript (fetch-based) SDK for go-on — ACP/MCP agent orchestration runtime",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/go-on/go-on.git"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
